# Jules Task

## Task ID

task-20260920-044131

## Created

2026-09-20T01:41:31Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.2.5 — Queue UI + Cancel/Kill + CANCELLED filter + task metadata fallback

CONTEXT:
- Repo: Urania-order/jules
- Backend: smos/api/main.py, smos/core/queue.py
- Scripts: scripts/jules-task.sh, scripts/jules-complete.sh
- Frontend: frontend/index.html
- v1.2.3 added async run-next + job tracking
- TASK 01 (task-20260919-231618) created via Admin → Run Next:
    * jules-task.sh wrote minimal active_task {id, request}
    * history entry ended up with title=null, created_at=null, proposed_by=null
    * UI shows "Unnamed task", sorted to bottom
- Multiple issues to fix (7 total)

ISSUE 1 — run-next picks WRONG file (order)
- admin_queue_run_next (line ~696) sorts by st_mtime
- st_mtime = last modification, NOT insertion order
- Fix: sort by st_birthtime (macOS), fallback st_mtime

ISSUE 2 — Cancel does not kill background run-task.sh (ERRATA-0034)
- run-next spawns thread → run-task.sh
- Cancel only updates state.json — no signal to subprocess
- Fix: subprocess.Popen + RUN_JOBS[job_id]["proc"]
- POST /api/admin/queue/cancel/{job_id}  (SIGTERM → wait → SIGKILL)
- POST /api/admin/queue/kill/{job_id}    (SIGKILL immediately)

ISSUE 3 — Queue Management UI overhaul
- REMOVE READY, REVIEW columns
- ADD PENDING column (files from .jules/queue/pending/)
- RUNNING column: file cards (from running/) + task cards
- BLOCKED column: file cards (from blocked/) + task cards
- KEEP: COMPLETED
- File cards:
  * PENDING: [▶ Run Next] [🗑 Delete]
  * RUNNING: [🛑 Cancel] [☠ Kill]
  * BLOCKED: [🛑 Cancel] [☠ Kill] [🗑 Delete]
- Sorted by ctime ASC (oldest first)
- Auto-refresh every 10s

ISSUE 4 — CANCELLED tasks rendered in BLOCKED / System Overview
- frontend renderQueue (~line 2198):
    } else if (task.status === 'BLOCKED' || task.status === 'FAILED' || task.status === 'CANCELLED') {
- Fix: skip CANCELLED in renderQueue + renderRecentTasks + renderControlRoom
- Filter from Active Queue Execution

ISSUE 5 — QueueManager.list_all_tasks() includes CANCELLED + dedup by first
- File: smos/core/queue.py, function list_all_tasks() (line ~69)
- Bug 1: CANCELLED included in result
- Bug 2: dedup keeps FIRST (stale) — should keep LAST (newest)
- Fix: by_id dict, last wins, skip CANCELLED

ISSUE 6 — Fallback title/created_at/proposed_by for stale tasks
- Old history entries may have title=null, request=null, created_at=null, proposed_by=null
- Fix in smos/core/queue.py _task_from_dict():
  * If request missing: fallback to task id
  * If title missing: first line of request (or id)
  * If created_at missing: parse from task id (task-YYYYMMDD-HHMMSS)
  * If proposed_by missing: "unknown"
- Add helper _iso_from_task_id(tid)

ISSUE 7 — jules-task.sh writes FULL active_task (TASK 01 root cause)
- scripts/jules-task.sh (~line 104) writes {id, request} only
- scripts/jules-complete.sh (~line 246) copies only subset
- Result: history entries miss title, created_at, proposed_by
- Fix BOTH scripts (see below)

GOAL:

PART 1 — Fix run-next order
In smos/api/main.py admin_queue_run_next (~line 696):
  def _ctime(f):
      st = f.stat()
      return getattr(st, 'st_birthtime', st.st_mtime)
  pending_files.sort(key=_ctime)

PART 2 — Cancel + Kill (ERRATA-0034)
1. run-next: subprocess.Popen (not run), store RUN_JOBS[job_id]["proc"]
2. POST /api/admin/queue/cancel/{job_id}:
   - proc.terminate() → wait(5) → kill() if alive
   - status = "cancelled"
   - Move running/ or blocked/ → deferred/
3. POST /api/admin/queue/kill/{job_id}:
   - proc.kill()
   - status = "killed"
   - Move running/ or blocked/ → deferred/
4. Frontend: Cancel + Kill buttons per job (RUNNING + BLOCKED)

PART 3 — Queue Management UI
1. Backend: create .jules/queue/blocked/ directory (with .gitkeep)
2. Backend: GET /api/queue/files
   Returns {pending, running, blocked, completed, deferred}
   Each: {filename, size, header, ctime, job_id?, elapsed_sec?, pid?}
   Sorted by ctime ASC.
3. Backend: DELETE /api/queue/files/{folder}/{filename}
   - Move file → deferred/ (soft delete)
   - Reject path traversal
4. Frontend:
   - REMOVE: queue-list-ready, queue-list-review
   - ADD: queue-list-pending (file cards)
   - RUNNING: queue-list-running-files + queue-list-running (task cards)
   - BLOCKED: queue-list-blocked-files + queue-list-blocked (task cards)
   - KEEP: queue-list-completed
   - File card buttons per column
   - Auto-refresh every 10s

PART 4 — Filter CANCELLED from UI
1. frontend/index.html renderQueue (~line 2174):
   - Skip tasks with status CANCELLED
   - renderRecentTasks: filter out CANCELLED
   - renderControlRoom: filter CANCELLED from Active Queue Execution
2. Frontend contract test: renderQueue skips CANCELLED

PART 5 — Fix QueueManager.list_all_tasks() (smos/core/queue.py line ~69)
- Bug 1: CANCELLED included
- Bug 2: dedup keeps FIRST — should keep LAST
- Fix:
    by_id = {}
    for item in candidates:
        if not isinstance(item, dict): continue
        tid = item.get("id")
        if not tid: continue
        raw_status = (item.get("status") or "").upper()
        if raw_status == "CANCELLED": continue
        by_id[tid] = item  # last wins
    for tid, item in by_id.items():
        tasks.append(self._task_from_dict(item, TaskStatus.PENDING))

PART 6 — Fallback title/created_at/proposed_by (smos/core/queue.py _task_from_dict)
- Add fallback in _task_from_dict:
    req = data.get("request") or data.get("description") or data.get("title")
    if not req:
        tid = data.get("id", "")
        req = tid if tid.startswith("task-") else "Unnamed task"
    title = data.get("title") or req.split("\n")[0][:80]
    created = data.get("created_at") or self._iso_from_task_id(data.get("id"))
    proposed = data.get("proposed_by") or "unknown"
- Add helper:
    def _iso_from_task_id(self, tid):
        import re
        from datetime import datetime, timezone
        if not tid or not tid.startswith("task-"): return None
        m = re.match(r"^task-(\d{4})(\d{2})(\d{2})-(\d{2})(\d{2})(\d{2})", tid)
        if not m: return None
        try:
            return datetime(int(m.group(1)), int(m.group(2)), int(m.group(3)),
                           int(m.group(4)), int(m.group(5)), int(m.group(6)),
                           tzinfo=timezone.utc).isoformat()
        except ValueError:
            return None

PART 7 — jules-task.sh + jules-complete.sh write full metadata

1. scripts/jules-task.sh (~line 104):
   BEFORE:
     state["active_task"] = {
         "id": task_id,
         "request": task
     }
   AFTER:
     from datetime import datetime, timezone
     import os
     _now = datetime.now(timezone.utc).isoformat()
     state["active_task"] = {
         "id": task_id,
         "request": task,
         "title": task.split("\n")[0][:80],
         "status": "RUNNING",
         "priority": 5,
         "created_at": _now,
         "started_at": _now,
         "proposed_by": os.environ.get("COSMOS_AUTHOR", "operator"),
         "source_task": None,
         "branch": CURRENT_BRANCH,
     }

2. scripts/jules-complete.sh (~line 246):
   BEFORE:
     completed = {
         "id": task_id,
         "status": "completed",
         "session_id": session_id or active.get("session_id"),
         "branch": active.get("branch"),
         "request": active.get("request"),
         "started_at": active.get("started_at"),
         "finished_at": now,
         "result": pull_result,
     }
   AFTER:
     completed = {
         "id": task_id,
         "status": "completed",
         "session_id": session_id or active.get("session_id"),
         "branch": active.get("branch") or state.get("branch"),
         "request": active.get("request"),
         "title": active.get("title"),
         "priority": active.get("priority", 5),
         "created_at": active.get("created_at"),
         "proposed_by": active.get("proposed_by"),
         "source_task": active.get("source_task"),
         "started_at": active.get("started_at"),
         "finished_at": now,
         "result": pull_result,
     }

REQUIREMENTS:
- Files: smos/api/main.py, smos/core/queue.py,
         scripts/jules-task.sh, scripts/jules-complete.sh,
         frontend/index.html,
         tests/test_admin_api.py, tests/test_frontend_contract.py,
         tests/test_queue_reset.py (or new test_task_metadata.py)
- No shell=True
- Do not modify scripts/run-task.sh
- Reuse RUN_JOBS dict (v1.2.3)
- Preserve COMPLETED
- Create .jules/queue/blocked/ dir (with .gitkeep)

TESTS:
- test_run_next_picks_oldest_by_birthtime
- test_cancel_terminates_subprocess_graceful
- test_kill_sends_sigkill
- test_cancel_moves_file_to_deferred
- test_kill_moves_file_to_deferred
- test_queue_files_endpoint_sorted
- test_queue_file_delete_moves_to_deferred
- test_queue_file_delete_rejects_path_traversal
- test_list_all_tasks_excludes_cancelled
- test_list_all_tasks_dedup_keeps_last
- test_task_from_dict_fallback_title_from_id
- test_task_from_dict_fallback_created_at_from_id
- test_task_from_dict_fallback_proposed_by_unknown
- test_jules_task_writes_full_active_task
- test_jules_complete_copies_title_from_active
- test_jules_complete_copies_created_at_from_active
- test_jules_complete_copies_proposed_by_from_active
- frontend contract: queue-list-pending, queue-list-blocked-files,
  btn-file-cancel, btn-file-kill, btn-file-delete,
  renderQueue skips CANCELLED

TESTS:
- pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/state.json (runtime) — but jules-complete.sh may backfill
- Do not touch .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Delete is soft (move to deferred/) — never rm
- Cancel: graceful (SIGTERM → wait → SIGKILL)
- Kill: hard (SIGKILL)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started

