# Jules Task

## Task ID

task-20260922-191951

## Created

2026-09-22T19:19:52Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS — TASK 03 — CONTEXT MODEL

CONTEXT:
- Repo: Urania-order/jules
- TASK 01 (EPISTEMIC FOUNDATION) applied:
    * smos/models/models.py:69 — EpistemicStatus (enum)
    * smos/models/models.py:98 — EpistemicStatus.from_str()
    * smos/models/models.py:142 — RelationType (enum, 18 types)
    * smos/models/models.py:212 — Relation(Base) — specific MemoryNode→MemoryNode
- TASK 02 (PHENOMENON MODEL) applied:
    * smos/models/phenomenon.py — Phenomenon(Base) with to_dict()
    * smos/services/phenomenon_service.py — PhenomenonService
    * Pattern: Enum(EpistemicStatus), JSON provenance, server_default=func.now()
    * tests/test_phenomenon.py — 11 tests
- Existing relation models:
    * smos/models/models.py:212 — Relation(Base) — FROM memory_nodes TO memory_nodes (NOT generic)
        Used in: smos/services/memory_service.py:93
        Fields: from_node_id, to_node_id, type
    * smos/models/epistemic.py:32 — ClusterRelation(Base)
    * smos/models/experience.py:74 — CausalRelation(Base)
- Ontology task series:
    * TASK 01: Epistemic foundation ✅
    * TASK 02: Phenomenon model ✅
    * TASK 03: Context model ← THIS

OBJECTIVE:
Make Context a first-class object.
Context is NOT merely metadata.
Context can participate in relations.

MINIMUM FIELDS:
- id: Integer (primary key)
- name: String (required, index=True)
- description: String (nullable=True)
- components: JSON (list)
- source: String (nullable=True)
- epistemic_status: EpistemicStatus (required)
- temporal_scope: String (nullable=True)
- spatial_scope: String (nullable=True)
- created_at: DateTime(timezone=True), server_default=func.now()
- updated_at: DateTime(timezone=True), onupdate=func.now()
- provenance: JSON (nullable=True)

REQUIREMENTS:

1. Reuse existing primitives:
   - EpistemicStatus (smos/models/models.py:69) — for epistemic_status
   - RelationType (smos/models/models.py:142) — for ContextRelation.relation_type
   - Phenomenon pattern (smos/models/phenomenon.py):
       * to_dict() method
       * Enum(EpistemicStatus) column type
       * JSON provenance
       * server_default=func.now() / onupdate=func.now()
   - PhenomenonService pattern (smos/services/phenomenon_service.py):
       * from_str() for enum
       * create/get/list/update/delete
       * db.add + db.commit + db.refresh
   - Relation (models.py:212) is SPECIFIC (MemoryNode→MemoryNode) — do NOT reuse
       Create ContextRelation (specific Context→Context)

2. Context is NOT merely metadata:
   - Add ContextRelation table (Context → Context)
   - Reuse RelationType enum
   - Do NOT duplicate RelationType

3. Context must NOT automatically mean fact:
   - Default epistemic_status = EpistemicStatus.OBSERVED
   - Service methods do not auto-promote

4. Files to create:
   a) smos/models/context.py
      - class Context(Base):
          __tablename__ = "contexts"
          id = Column(Integer, primary_key=True, index=True)
          name = Column(String, index=True, nullable=False)
          description = Column(String, nullable=True)
          components = Column(JSON, default=list)
          source = Column(String, nullable=True)
          epistemic_status = Column(Enum(EpistemicStatus), default=EpistemicStatus.OBSERVED, nullable=False)
          temporal_scope = Column(String, nullable=True)
          spatial_scope = Column(String, nullable=True)
          created_at = Column(DateTime(timezone=True), server_default=func.now())
          updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
          provenance = Column(JSON, default=dict)
          def to_dict(self) -> dict: ...  (follow Phenomenon.to_dict)

      - class ContextRelation(Base):
          __tablename__ = "context_relations"
          id = Column(Integer, primary_key=True, index=True)
          source_context_id = Column(Integer, ForeignKey("contexts.id"), nullable=False)
          target_context_id = Column(Integer, ForeignKey("contexts.id"), nullable=False)
          relation_type = Column(Enum(RelationType), nullable=False)
          created_at = Column(DateTime(timezone=True), server_default=func.now())
          provenance = Column(JSON, default=dict)
          def to_dict(self) -> dict: ...

   b) smos/services/context_service.py
      - class ContextService:
          def __init__(self, db: Session): self.db = db
          def create(self, name, description=None, components=None, source=None,
                     epistemic_status=EpistemicStatus.OBSERVED,
                     temporal_scope=None, spatial_scope=None, provenance=None) -> Context
          def get(self, context_id) -> Optional[Context]
          def list(self, limit=50, offset=0) -> List[Context]
          def update(self, context_id, **fields) -> Optional[Context]
          def delete(self, context_id) -> bool
          def add_relation(self, source_id, target_id, relation_type, provenance=None) -> ContextRelation
          def list_relations(self, context_id) -> List[ContextRelation]

   c) smos/models/__init__.py — export Context, ContextRelation
   d) smos/services/__init__.py — export ContextService (if pattern)
   e) smos/core/init_db.py — import context

5. Tests (tests/test_context.py):
   - test_create_context
   - test_serialize_context (to_dict)
   - test_persist_context
   - test_retrieve_context
   - test_list_contexts
   - test_update_context
   - test_delete_context
   - test_context_components_json
   - test_context_temporal_scope
   - test_context_spatial_scope
   - test_context_provenance_preserved
   - test_invalid_epistemic_state_rejected
   - test_context_default_not_fact (default = OBSERVED)
   - test_context_relation_create
   - test_context_relation_list
   - test_context_relation_reuses_relationtype (PART_OF / EXTENDS / etc.)

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

TECHNICAL:
- Files:
    * smos/models/context.py (new)
    * smos/services/context_service.py (new)
    * smos/models/__init__.py (modify — export)
    * smos/services/__init__.py (modify — export, if pattern)
    * smos/core/init_db.py (modify — import)
    * tests/test_context.py (new)
- No shell=True
- Reuse EpistemicStatus (smos/models/models.py:69)
- Reuse RelationType (smos/models/models.py:142)
- Reuse Phenomenon pattern (to_dict, Enum, JSON)
- Reuse PhenomenonService pattern (from_str, db.add/commit/refresh)
- Do NOT reuse Relation (specific MemoryNode→MemoryNode)

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Context ≠ metadata (ContextRelation table)
- Context ≠ fact (default OBSERVED)
- Preserve backward compatibility with TASK 01 + TASK 02
- Do NOT duplicate EpistemicStatus enum
- Do NOT duplicate RelationType enum
- Do NOT reuse Relation (specific MemoryNode)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused (EpistemicStatus, RelationType, Phenomenon pattern, PhenomenonService pattern)
2. what was added (Context, ContextRelation, ContextService, tests)
3. what was deliberately not changed (EpistemicStatus, RelationType, Relation, Phenomenon)
4. tests/results
5. remaining gaps (esp. whether RelationType fits Context relations)

Commit message:
feat: add canonical context model

## Status

started

