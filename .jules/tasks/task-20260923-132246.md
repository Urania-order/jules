# Jules Task

## Task ID

task-20260923-132246

## Created

2026-09-23T13:22:47Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.4 — Fix DATABASE_URL default + init_db in lifespan (ERRATA-0041, 0042)

CONTEXT:
- Repo: Urania-order/jules
- Files: smos/core/database.py, smos/api/main.py, .env.example (new),
  README.md (update), tests/test_database_config.py (new)
- ERRATA-0041: DATABASE_URL defaults to PostgreSQL, no .env → connection refused
- ERRATA-0042: init_db() not called on uvicorn startup → "no such table"

CURRENT STATE:
- smos/core/database.py:6:
    DATABASE_URL = os.getenv("DATABASE_URL", "postgresql://postgres:postgres@localhost:5432/smos")
- smos/core/database.py:8:
    engine = create_engine(DATABASE_URL)
- smos/api/main.py:89-101 (lifespan):
    async def lifespan(app: FastAPI):
        log_default_token_warning_once()
        try:
            normalize_created_at_state(".co-smos/state.json")
        except Exception as e:
            logger.warning(f"normalize_created_at failed: {e}")
        scheduler_task = asyncio.create_task(background_scheduler_loop())
        yield
        ...
- smos/models/models.py:6 already detects SQLite:
    if os.getenv("DATABASE_URL", "").startswith("sqlite"):
        class Vector(TypeDecorator): ...
- smos/core/init_db.py: correct
- .env.example: NOT found
- README.md: exists (5282 bytes)
- AGENTS.md: exists (15757 bytes)

GOAL:

PART 1 — SQLite default (ERRATA-0041)
In smos/core/database.py:
BEFORE:
    DATABASE_URL = os.getenv("DATABASE_URL", "postgresql://postgres:postgres@localhost:5432/smos")
    engine = create_engine(DATABASE_URL)
AFTER:
    DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./smos.db")
    if DATABASE_URL.startswith("sqlite"):
        engine = create_engine(
            DATABASE_URL,
            connect_args={"check_same_thread": False},
        )
    else:
        engine = create_engine(DATABASE_URL)
Add docstring:
    """Database engine setup.
    Default: SQLite (file ./smos.db) for local development.
    For PostgreSQL (production): set DATABASE_URL env var.
    """

PART 2 — init_db() in lifespan (ERRATA-0042)
In smos/api/main.py lifespan, after log_default_token_warning_once() and
BEFORE normalize_created_at_state:
    try:
        from smos.core.init_db import init_db
        init_db()
        logger.info("Database tables initialized (idempotent).")
    except Exception as e:
        logger.warning(f"init_db failed: {e}")

Order in lifespan:
  a) log_default_token_warning_once()
  b) init_db()  (NEW)
  c) normalize_created_at_state(...)
  d) scheduler_task = ...
  e) yield

init_db() is idempotent (Base.metadata.create_all is idempotent).

PART 3 — .env.example (new, tracked in git)
Create .env.example with:
    # Co-SMOS environment configuration
    # Copy to .env: cp .env.example .env
    # Do NOT commit .env (may contain secrets).
    
    # Database (default SQLite local)
    DATABASE_URL=sqlite:///./smos.db
    # For PostgreSQL (production):
    # DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smos
    
    # Auth tokens (optional; dev defaults if empty)
    # JULES_CONSULTANT_TOKEN=
    # JULES_OPERATOR_TOKEN=
    # JULES_ADMIN_TOKEN=
    
    # Optional: author name for new tasks
    # COSMOS_AUTHOR=operator

PART 4 — README.md update
Add "Local Development Setup" section:
    ## Local Development Setup
    
    ### First time
    git clone <repo-url> jules
    cd jules
    python3 -m venv .venv
    source .venv/bin/activate
    pip install -r requirements.txt
    cp .env.example .env
    python3 -m smos.core.init_db
    uvicorn smos.api.main:app --host 0.0.0.0 --port 8080
    
    init_db is also called automatically on startup (idempotent).
    
    ### PostgreSQL (production)
    Edit .env: DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smos
    Ensure PostgreSQL is running; run: createdb smos

PART 5 — Tests (tests/test_database_config.py — new)
- test_database_url_default_sqlite
- test_database_url_respects_env
- test_sqlite_connect_args
- test_lifespan_calls_init_db (mock init_db)
- test_env_example_exists

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

TECHNICAL:
- Files: smos/core/database.py, smos/api/main.py, .env.example,
  README.md, tests/test_database_config.py
- No shell=True
- Preserve PostgreSQL path when DATABASE_URL set
- SQLite connect_args required for FastAPI async
- init_db() idempotent
- Reuse SQLite detection in smos/models/models.py:6

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Do NOT change behavior when DATABASE_URL explicitly set
- .env.example tracked; .env gitignored (already)
- Preserve normalize_created_at_state call in lifespan

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

Commit message:
fix: default DATABASE_URL to SQLite + init_db in lifespan (ERRATA-0041, 0042)

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 10683530139738271081

Completed at: 2026-09-23T13:38:17Z
