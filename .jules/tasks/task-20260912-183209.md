# Jules Task

## Task ID

task-20260912-183209

## Created

2026-09-12T18:32:09+00:00

## Repository

https://github.com/Urania-order/jules

## Branch

main

## Request

Розшир ObservatoryService у smos/services/observatory_service.py.

Контекст:
- smos/services/observatory_service.py має базову реалізацію
- smos/core/interfaces.py визначає Observable з методами get_health_metrics() і get_evolution_summary()
- Підсистеми (EcologyEngine, ValueEcologyService, CommonsService, DiscoverySystem) реалізують Observable

Завдання:
1. Додати метод get_health_report() -> Dict[str, Any]:
   - Агрегує get_health_metrics() з усіх підсистем
   - Додає timestamp (UTC ISO 8601)
   - Додає загальний health_score (0.0-1.0) на основі метрик
2. Розширити generate_quarterly_report() -> Dict[str, Any]:
   - health_report (з get_health_report)
   - evolution_summary (агрегація get_evolution_summary)
   - knowledge_impact_ranking (топ-10 за впливом)
   - forecasts (Cosmo-Initiate Forecasts)
   - recommendations (список рекомендацій на основі health)
3. Додати метод get_impact_ranking(limit: int = 10) -> List[Dict[str, Any]]:
   - Використовувати ValueAssessment для ранжування
   - Сортувати за knowledge_value * social_impact
   - Повертати [{rank, node_id, score, reason}]
4. Додати метод generate_recommendations() -> List[str]:
   - На основі health metrics (наприклад, якщо health < 0.5 — рекомендувати увагу)
   - На основі dormant knowledge
5. Написати tests/test_observatory_service.py з тестами:
   - get_health_report повертає health_score і timestamp
   - generate_quarterly_report містить всі 5 секцій
   - get_impact_ranking повертає правильну кількість
   - generate_recommendations повертає список
6. Запустити ./scripts/validate.sh і uv run pytest tests/ -v

НЕ чіпати .co-smos/ і .jules/
Звітувати за форматом AGENTS.md §14

## Status

started

