# Jules Task

## Task ID

task-20260914-163025

## Created

2026-09-14T13:30:26Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Розроби Co-SMOS Control Room v0.9 — Integration Review.

КОНТЕКСТ:
- Це working-репозиторій Urania-order/jules
- Існує CLI-оркестратор: 15 shell-скриптів (jules-task.sh, jules-queue-*.sh, jules-complete.sh, validate.sh)
- Існує FastAPI: smos/api/main.py (порт 8080)
- Існує черга: .jules/queue/{pending,running,completed,proposed,deferred}/
- Існує 90+ тестів, CI (GitHub Actions)
- AGENTS.md має 25 секцій

МЕТА v0.9:
Перетворити CLI-оркестратор на локальний WEB CONTROL ROOM.
Frontend НЕ повинен напряму виконувати shell-команди.
CLI scripts НЕ видаляти і НЕ ламати.

АРХІТЕКТУРА:
Browser → FastAPI → Co-SMOS Core → Queue/Orchestrator → CLI Adapter → existing scripts → Jules

ОБОВ'ЯЗКОВИЙ ПОРЯДОК:

PHASE 1 — INSPECT (без змін коду):
1. Проаналізуй структуру smos/ (api, core, models, services)
2. Знайди існуючі FastAPI modules
3. Знайди queue implementation
4. Знайди всі jules-*.sh
5. Знайди існуючий frontend (якщо є)
6. Визнач existing persistence (.co-smos/state.json, .jules/queue/*.json)
7. Визнач startup commands
8. Визнач existing tests
НЕ припускай структуру — перевір фактично.
Запиши результати в .jules/history/adr-0007-control-room-inspect.md

PHASE 2 — DESIGN:
Підготуй integration map: existing component → new Co-SMOS component → integration point.
НЕ створюй дублюючі компоненти.
Запиши в ADR.

PHASE 3 — CORE:
Реалізуй у smos/core/:
- task.py — Task model (id, description, priority, status, depends_on, created_at, updated_at)
- queue.py — Queue abstraction (читає .jules/queue/)
- proposals.py — Proposal abstraction
- events.py — Event log
- state.py — System state
Статуси: PENDING, READY, RUNNING, REVIEW, ACCEPTED, COMPLETED, BLOCKED, FAILED, CANCELLED, DEFERRED

PHASE 4 — CLI ADAPTER:
Створи smos/adapters/jules_cli.py:
- JulesCLIAdapter клас
- add_task(description, priority)
- run_queue()
- list_proposals()
- accept_proposal(id)
- defer_proposal(id, note)
- reject_proposal(id)
- get_queue_status()
Використовуй subprocess.run([...]) БЕЗ shell=True.

PHASE 5 — API:
Додай в smos/api/main.py endpoints:
- GET /api/system/status
- GET /api/queue
- POST /api/queue
- POST /api/queue/run
- GET /api/tasks
- GET /api/tasks/{id}
- PATCH /api/tasks/{id}
- POST /api/tasks/{id}/start
- POST /api/tasks/{id}/cancel
- GET /api/proposals
- POST /api/proposals/{id}/accept
- POST /api/proposals/{id}/modify
- POST /api/proposals/{id}/defer
- POST /api/proposals/{id}/reject
- GET /api/events
- GET /api/tasks/{id}/history
Errors: повертай {status: error, code, message, exit_code} з правильним HTTP status.

PHASE 6 — FRONTEND:
Створи frontend/index.html (vanilla HTML+CSS+JS):
- Control Room: system status, queue counts
- Queue: kanban (READY | RUNNING | REVIEW | BLOCKED | COMPLETED)
- Task Detail: modal з деталями
- Proposals: Accept/Modify/Defer/Reject кнопки
- Events: activity log
- History: timeline
- Auto-refresh кожні 5 сек (але архітектурно готовий до WebSocket)
- CSS variables для dark mode
- Responsive design
- Empty states, error states (LOADING, READY, EMPTY, ERROR, OFFLINE)

ВАЖЛИВО:
- Drag-and-drop ТІЛЬКИ для reorder у READY/PENDING
- Drag у RUNNING/COMPLETED НЕ дозволено — треба explicit команда
- Форма New Task з полями (description, priority slider, dependencies) — БЕЗ prompt()

PHASE 7 — INTEGRATION:
Перевір: UI → API → Core → Adapter → CLI
Кожен шар окремо.

PHASE 8 — REGRESSION:
Перевір, що старі CLI-команди працюють:
- ./scripts/jules-task.sh
- ./scripts/jules-queue-add.sh
- ./scripts/jules-queue-review.sh list
- ./scripts/jules-queue-runner.sh --once

PHASE 9 — DOCUMENTATION:
Онови README.md: How to start, How to create task, How to use Control Room, Architecture, API.
Додай scripts/co-smos.sh — запуск FastAPI + frontend.

ТЕСТИ:
- tests/test_control_room_api.py — API endpoints
- tests/test_cli_adapter.py — adapter
- tests/test_task_model.py — Task/Queue/Proposal/Event
- Онови tests/test_queue_scripts.py

ЗАБОРОНЕНО:
- Видаляти або ламати існуючі shell scripts
- Використовувати shell=True з user input
- Frontend напряму виконує shell
- Додавати React/Vue/Svelte без необхідності
- Дублювати існуючі компоненти
- Змінювати CLI contracts
- Змінювати storage без необхідності
- Переписувати Python framework

ACCEPTANCE CRITERIA:
- Existing CLI scripts працюють
- Control Room відкривається в browser
- Task створюється через форму
- Priority змінюється
- Reorder працює
- Task запускається
- Task скасовується
- Proposals відображаються (Accept/Modify/Defer/Reject)
- Events записуються
- History доступна
- API повертає реальні errors
- Source of truth на backend
- Всі 90+ існуючих тестів проходять
- Нові тести проходять

ЗАПУСК:
- uv run uvicorn smos.api.main:app --host 127.0.0.1 --port 8080
- python -m http.server 3000 --directory frontend

НЕ чіпати .co-smos/ і .jules/tasks/
Звітувати за форматом AGENTS.md §14
ПІСЛЯ ЗАВЕРШЕННЯ: запропонуй 3-5 наступних кроків через jules-queue-propose.sh

## Status

started

