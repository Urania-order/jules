# Jules Task

## Task ID

task-20260926-155439

## Created

2026-09-26T15:54:39Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.10 — TASK 20 — DOMAIN EVENTS API

CONTEXT:
- Repo: Urania-order/jules
- Existing DomainEvent model + DomainEventService (TASK 14):
    smos/models/domain_event.py
    smos/services/domain_event_service.py:
        record()
        get()
        list_for_entity(entity_type, entity_id, limit, offset)
        list_recent(limit)
        reconstruct_entity(entity_type, entity_id)
        reconstruct_lifecycle(entity_type, entity_id)
- DomainEvent is APPEND-ONLY (no update, no delete).

============================================================
STEP 0 — AUDIT (COMPLETED)
============================================================

Findings:
1. DomainEventService has all required read methods.
2. NO API endpoints yet.
3. Append-only contract.

DECISION:
   - Add THREE read-only endpoints:
       GET  /api/domain-events
       GET  /api/domain-events/{entity_type}/{entity_id}
       POST /api/domain-events/reconstruct/{entity_type}/{entity_id}
   - NO CRUD (no POST create, no PUT, no DELETE).
   - DomainEvent is HISTORY, not a mutable resource.
   - Reconstruct is a READ operation (POST because body may carry
     filters / parameters, not because it modifies state).

============================================================
OBJECTIVE
============================================================

Expose DomainEvent history via API:

GET /api/domain-events?limit=50
    → DomainEventService.list_recent(limit)
    → {"events": [...]}

GET /api/domain-events/{entity_type}/{entity_id}?limit=100&offset=0
    → DomainEventService.list_for_entity(...)
    → {"entity_type": ..., "entity_id": ..., "events": [...]}

POST /api/domain-events/reconstruct/{entity_type}/{entity_id}
    → DomainEventService.reconstruct_entity(...)
    → {"entity_type": ..., "entity_id": ..., "event_count": ...,
       "created_at": ..., "updated_at": ..., "events": [...],
       "reconstruction_note": "..."}

Auth:
   - GET endpoints: follow existing convention (public read like /api/tasks)
   - POST reconstruct: dependency=[Depends(require_operator)]

============================================================
FILES TO MODIFY
============================================================

smos/api/main.py:
   - Add 3 endpoints (following existing conventions)
   - Import DomainEventService

FILES TO CREATE
============================================================

tests/test_api_domain_events.py (new)

============================================================
TESTS
============================================================

- test_list_domain_events_endpoint
- test_list_domain_events_with_limit
- test_list_for_entity_endpoint
- test_list_for_entity_404_or_empty_for_unknown
- test_reconstruct_endpoint
- test_reconstruct_returns_events_in_order
- test_reconstruct_empty_for_unknown_entity
- test_no_create_domain_event_endpoint (POST /api/domain-events → 405)
- test_no_delete_domain_event_endpoint (DELETE → 405)
- test_domain_events_append_only_via_api
- test_existing_domain_event_service_unchanged

REQUIRED FIXTURE:
- Create DomainEvent records via DomainEventService (not API)
- GET /api/domain-events → see events
- POST /api/domain-events/reconstruct/... → see reconstruction

LIMIT: ≤1000 changed code lines.

TECHNICAL:
- Reuse DomainEventService (read-only)
- Do NOT modify DomainEvent model or DomainEventService
- NO CRUD (no create/update/delete endpoints)
- Follow existing /api/... prefix
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Append-only contract preserved
- Preserve backward compatibility
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused (DomainEventService methods)
2. what was added (3 endpoints + tests)
3. what was deliberately not changed (DomainEvent, DomainEventService)
4. tests/results
5. remaining gaps

Commit message:
feat: expose domain event history through api

## Status

started

