# Jules Task

## Task ID

task-20260925-122607

## Created

2026-09-25T12:26:07Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS — TASK 13 — PREDICTION INTEGRATION

CONTEXT:
- Repo: Urania-order/jules
- Existing ontology TASK 01-12 applied:
    * EpistemicStatus (TASK 01):
        OBSERVED, INFERRED, HYPOTHESIZED, PREDICTED,
        UNVALIDATED, CONTESTED, REFUTED
        → PREDICTED exists at models.py:74 ✅
    * RelationType (33)
    * Phenomenon (TASK 02)
    * Context (TASK 03)
    * Constraint (TASK 04)
    * PotentialPhenomenon (TASK 05):
        - id, phenomenon (String, no FK), status (PotentialStatus)
        - required_conditions / supporting_contexts /
          blocking_constraints / dependencies / expected_impacts (JSON)
        - Docstring: "Potential Phenomenon ≠ Prediction"
    * DomainRelation + DomainRelationService (TASK 07)
    * RoleProjectionService (TASK 08)
    * ContextExposure (TASK 09)
    * ConvergentResonanceService (TASK 10)
    * BlockageAnalysisService (TASK 11)
    * EmergenceAnalysisService (TASK 12)
- Ontology series TASK 01-12 applied
- TASK 13: Prediction Integration ← THIS

============================================================
STEP 0 — PREDICTION ARCHITECTURE AUDIT (COMPLETED)
============================================================

Findings (verified by direct inspection):

1. NO existing Prediction / Forecast / ExpectedState / ActualOutcome
   model in smos/models/.
2. NO existing prediction/forecast/evaluation/outcome fields in models.
3. NO existing evaluation/outcome/result/accuracy model.
4. NO prediction/forecast service in smos/services/
   (only observatory_service.generate_forecasts() — ecology health,
    unrelated to epistemic prediction).
5. Timeline EXISTS at smos/models/models.py:195
   - TimelineType at models.py:41
   - Purpose: alternative realities (REAL / COUNTERFACTUAL)
   - NOT a prediction lifecycle
   - Do NOT reuse Timeline for predictions.
6. EpistemicStatus.PREDICTED EXISTS at models.py:74 ✅
   - Use canonical status; do NOT invent a new one.
7. NO scheduled/queue_run/dispatch_at/fire_at fields.
8. PotentialPhenomenon (TASK 05) — source candidate.
9. EmergenceAnalysisService (TASK 12) — reusable for default conditions.

Decision (record in FINAL REPORT section 1):
   - Create NEW Prediction model (no existing equivalent).
   - Create NEW PredictionService.
   - Do NOT create a new forecast engine.
   - Reuse EpistemicStatus.PREDICTED.
   - Reuse PotentialPhenomenon as source target.
   - Reuse EmergenceAnalysisService for default conditions.
   - Do NOT reuse Timeline (different concept).
   - Do NOT modify EmergenceAnalysisService, BlockageAnalysisService,
     PotentialPhenomenon, Phenomenon, EpistemicStatus, Timeline.
   - Prediction is a first-class persistent model with a LIFECYCLE.
   - Document decision BEFORE implementation.

============================================================
PREDICTION ARCHITECTURE DECISION (FINAL — from operator)
============================================================

Prediction is a new first-class persistent model.

Do NOT reuse Timeline.

Do NOT create a new forecast engine.

Reuse:
- EpistemicStatus
- PotentialPhenomenon
- EmergenceAnalysisService
- existing Outcome infrastructure, if available

Prediction lifecycle:

    create
      ↓
    attach_outcome
      ↓
    evaluate

Rules (MANDATORY):

1. attach_outcome() MUST NOT change epistemic_status.

2. evaluate() MUST NOT change epistemic_status.

3. Epistemic status may change only through an explicit
   existing/user-authorized mechanism.

4. Prediction MUST remain persistent after evaluation
   (historical record — NOT deleted).

5. Prediction MUST NOT be automatically promoted to Fact,
   Observed, Refuted, or any other epistemic status.

6. source_hypothesis is polymorphic:
      source_hypothesis_type
      source_hypothesis_id

   Allowed source types:
      "potential_phenomenon"
      "phenomenon"
      None

7. source_hypothesis MUST NOT use a conventional FK,
   because it is polymorphic.

8. Document explicitly:

   PotentialPhenomenon ≠ Prediction

   PotentialPhenomenon:
       modal possibility
       "could happen"

   Prediction:
       declarative expectation
       "system expects it"

9. Timeline MUST remain conceptually separate from Prediction.

10. No separate forecast engine is permitted.

Historical reconstructability (MANDATORY):
   Prediction MUST store:
   - created_at       — when the record was created
   - prediction_time  — when the prediction was made
   - expected_at      — target time when the outcome is expected

   Rationale: after one year, the system must be able to reconstruct
   "what the system knew and expected at that moment" — as a
   historically reproducible point in the Space of States,
   not just as text of a future forecast.

============================================================
OBJECTIVE
============================================================

Integrate potential phenomenon with the existing prediction/forecast
architecture.

Do NOT create a new forecast system.

A prediction must contain:
    source_hypothesis (polymorphic — source_type + source_id)
    created_at
    prediction_time
    expected_at / target_time            ← MANDATORY (new)
    expected_state
    conditions
    confidence
    epistemic_status
    actual_outcome
    evaluation

Reuse existing fields where possible.

============================================================
DESIGN — Prediction model + PredictionService
============================================================

Create smos/models/prediction.py:

    from sqlalchemy import Column, Integer, String, Float, DateTime, Enum, JSON
    from sqlalchemy.sql import func
    from smos.core.database import Base
    from smos.models.models import EpistemicStatus


    class Prediction(Base):
        """Prediction — system expects something to happen under conditions.

        Distinction from PotentialPhenomenon (TASK 05):
        - PotentialPhenomenon: modal ("could happen")
        - Prediction:           declarative ("system expects it to happen
                                under the stated conditions")

        Distinction from Timeline (models.py:195):
        - Timeline: representation of realities / temporal states
          (REAL / COUNTERFACTUAL)
        - Prediction: assertion made BEFORE the expected outcome
        They are conceptually separate. Timeline MUST NOT be reused
        as a Prediction model.

        Lifecycle:
          1. Create prediction (epistemic_status=PREDICTED)
          2. (later) Attach actual_outcome
          3. (later) Evaluate (attach evaluation)

        Rules:
          - attach_outcome() MUST NOT change epistemic_status
          - evaluate() MUST NOT change epistemic_status
          - Prediction MUST remain persistent after evaluation
          - Prediction MUST NOT be auto-promoted to fact

        source_hypothesis is polymorphic:
          - source_hypothesis_type: "potential_phenomenon" | "phenomenon" | None
          - source_hypothesis_id:   int | None
          No FK — one FK cannot reference two tables.
        """

        __tablename__ = "predictions"

        id = Column(Integer, primary_key=True, index=True)

        # Source (polymorphic — no FK)
        source_hypothesis_type = Column(String, nullable=True)
        source_hypothesis_id = Column(Integer, nullable=True)

        # Timing (MANDATORY — historical reconstructability)
        created_at = Column(
            DateTime(timezone=True),
            server_default=func.now(),
        )
        prediction_time = Column(
            DateTime(timezone=True),
            server_default=func.now(),
            nullable=False,
        )
        expected_at = Column(
            DateTime(timezone=True),
            nullable=True,   # target time — may be null if unknown
        )

        # Content
        expected_state = Column(JSON, default=dict)
        conditions = Column(JSON, default=list)
        confidence = Column(Float, nullable=True)  # 0.0-1.0

        # Epistemic
        epistemic_status = Column(
            Enum(EpistemicStatus),
            default=EpistemicStatus.PREDICTED,
            nullable=False,
        )

        # Lifecycle (filled later, prediction remains persistent)
        actual_outcome = Column(JSON, default=dict)
        evaluation = Column(JSON, default=dict)

        # Provenance
        provenance = Column(JSON, default=dict)

        def to_dict(self) -> dict:
            return {
                "id": self.id,
                "source_hypothesis_type": self.source_hypothesis_type,
                "source_hypothesis_id": self.source_hypothesis_id,
                "created_at": self.created_at.isoformat() if self.created_at else None,
                "prediction_time": self.prediction_time.isoformat() if self.prediction_time else None,
                "expected_at": self.expected_at.isoformat() if self.expected_at else None,
                "expected_state": self.expected_state if self.expected_state is not None else {},
                "conditions": self.conditions if self.conditions is not None else [],
                "confidence": self.confidence,
                "epistemic_status": (
                    self.epistemic_status.value
                    if isinstance(self.epistemic_status, EpistemicStatus)
                    else self.epistemic_status
                ),
                "actual_outcome": self.actual_outcome if self.actual_outcome is not None else {},
                "evaluation": self.evaluation if self.evaluation is not None else {},
                "provenance": self.provenance if self.provenance is not None else {},
            }


Create smos/services/prediction_service.py:

    from datetime import datetime
    from typing import Any, Dict, List, Optional
    from sqlalchemy.orm import Session

    from smos.models.prediction import Prediction
    from smos.models.models import EpistemicStatus


    class PredictionService:
        """Service for managing Prediction lifecycle.

        Lifecycle (explicit, no auto-promotion):
          1. create_prediction -> epistemic_status=PREDICTED
          2. attach_outcome    -> does NOT change epistemic_status
          3. evaluate          -> does NOT auto-promote to fact
                                  does NOT change epistemic_status

        Prediction remains persistent after evaluation (historical record).
        The caller is responsible for explicitly updating epistemic_status
        through an authorized mechanism if desired.
        """

        def __init__(self, db: Session):
            self.db = db

        # --- Create ---
        def create_prediction(
            self,
            expected_state: Dict[str, Any],
            source_hypothesis_type: Optional[str] = None,
            source_hypothesis_id: Optional[int] = None,
            conditions: Optional[List[Any]] = None,
            confidence: Optional[float] = None,
            prediction_time: Optional[datetime] = None,
            expected_at: Optional[datetime] = None,
            provenance: Optional[Dict[str, Any]] = None,
        ) -> Prediction:
            prediction = Prediction(
                source_hypothesis_type=source_hypothesis_type,
                source_hypothesis_id=source_hypothesis_id,
                expected_state=dict(expected_state or {}),
                conditions=list(conditions or []),
                confidence=confidence,
                epistemic_status=EpistemicStatus.PREDICTED,
                expected_at=expected_at,
                provenance=dict(provenance or {}),
            )
            if prediction_time is not None:
                prediction.prediction_time = prediction_time
            self.db.add(prediction)
            self.db.commit()
            self.db.refresh(prediction)
            return prediction

        # --- Read ---
        def get(self, prediction_id: int) -> Optional[Prediction]: ...
        def list(self, limit: int = 50, offset: int = 0) -> List[Prediction]: ...
        def delete(self, prediction_id: int) -> bool: ...

        # --- Lifecycle ---
        def attach_outcome(
            self,
            prediction_id: int,
            actual_outcome: Dict[str, Any],
        ) -> Optional[Prediction]:
            """Attach actual outcome. Does NOT change epistemic_status."""
            ...

        def evaluate(
            self,
            prediction_id: int,
            evaluation: Dict[str, Any],
        ) -> Optional[Prediction]:
            """Attach evaluation. Does NOT auto-promote to fact.
            Does NOT change epistemic_status.
            """
            ...

        # --- Integration with TASK 05 / TASK 12 ---
        def from_potential_phenomenon(
            self,
            potential_id: int,
            expected_state: Dict[str, Any],
            conditions: Optional[List[Any]] = None,
            confidence: Optional[float] = None,
            expected_at: Optional[datetime] = None,
            provenance: Optional[Dict[str, Any]] = None,
        ) -> Optional[Prediction]:
            """Create a Prediction whose source_hypothesis is a
            PotentialPhenomenon (TASK 05).
            """
            ...

FILES TO CREATE:
1. smos/models/prediction.py (new)
2. smos/services/prediction_service.py (new)
3. smos/models/__init__.py — export Prediction
4. smos/services/__init__.py — export PredictionService
5. smos/core/init_db.py — import prediction  ← CRITICAL (ERRATA-0042)
6. tests/test_prediction.py (new)

============================================================
LIFECYCLE (from ТЗ + operator rules)
============================================================

1. Create prediction
   - source_hypothesis_type: "potential_phenomenon" | "phenomenon" | None
   - source_hypothesis_id:   int | None
   - prediction_time: now
   - expected_at: target time (nullable)
   - expected_state: {...}
   - conditions: [...]
   - confidence: 0.0-1.0
   - epistemic_status: PREDICTED

2. Attach outcome
   - actual_outcome: {...}
   - Does NOT auto-change epistemic_status

3. Evaluate
   - evaluation: {match: bool, accuracy: 0.0-1.0, notes: ...}
   - Does NOT auto-promote to fact
   - Does NOT auto-change epistemic_status
   - Prediction remains persistent (historical record)

============================================================
TESTS (tests/test_prediction.py)
============================================================

REQUIRED (from ТЗ):
- test_create_prediction
- test_later_attach_outcome
- test_evaluate_prediction

ADDITIONAL:
- test_create_prediction_sets_epistemic_status_predicted
- test_create_prediction_with_source_potential_phenomenon
- test_create_prediction_with_source_phenomenon
- test_create_prediction_with_source_hypothesis_nullable
- test_create_prediction_with_conditions
- test_create_prediction_with_confidence
- test_create_prediction_with_expected_at
- test_create_prediction_without_expected_at
- test_attach_outcome_does_not_auto_change_status
- test_evaluate_does_not_auto_promote_to_fact
- test_evaluate_does_not_auto_change_epistemic_status
- test_prediction_lifecycle_full
    (create -> attach_outcome -> evaluate -> verify all fields)
- test_prediction_remains_persistent_after_evaluation
- test_prediction_to_dict
- test_prediction_is_distinct_from_potential_phenomenon
    (documentation assertion)
- test_prediction_is_distinct_from_timeline
    (documentation assertion)
- test_prediction_service_list
- test_prediction_service_delete
- test_prediction_service_get
- test_prediction_confidence_range
- test_prediction_does_not_modify_potential_phenomenon
- test_prediction_does_not_modify_emergence_service
- test_prediction_does_not_modify_blockage_service
- test_prediction_does_not_modify_timeline
- test_prediction_reuses_epistemic_status_predicted
    (do NOT invent new status)
- test_from_potential_phenomenon_creates_prediction
- test_from_potential_phenomenon_returns_none_for_unknown_id
- test_prediction_source_hypothesis_polymorphic_no_fk
- test_prediction_has_three_timestamps
    (created_at, prediction_time, expected_at)

REQUIRED FIXTURE:
- Create Phenomenon + PotentialPhenomenon
- Create Prediction from potential phenomenon with expected_at
- Attach outcome
- Evaluate
- Assert all fields + historical record persistence

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

============================================================
TECHNICAL
============================================================

- Files:
    * smos/models/prediction.py (new)
    * smos/services/prediction_service.py (new)
    * smos/models/__init__.py (modify — export)
    * smos/services/__init__.py (modify — export)
    * smos/core/init_db.py (modify — import)
    * tests/test_prediction.py (new)
- No shell=True
- Reuse EpistemicStatus.PREDICTED (do NOT invent new status)
- Reuse PotentialPhenomenon (source_hypothesis)
- Do NOT modify EmergenceAnalysisService, BlockageAnalysisService
- Do NOT modify PotentialPhenomenon / Phenomenon / EpistemicStatus / Timeline
- Do NOT create new forecast engine
- Do NOT auto-promote prediction to fact
- Do NOT auto-change epistemic_status on evaluate
- Do NOT reuse Timeline (different concept: alternative realities)
- Do NOT use FK for source_hypothesis (polymorphic)
- MANDATORY: three timestamps (created_at, prediction_time, expected_at)
- ALL tests must pass locally BEFORE commit (ERRATA-0043)
- init_db.py MUST import prediction (ERRATA-0042)

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- STEP 0 audit recorded above
- Prediction lifecycle is explicit (no auto-promotion)
- Prediction remains persistent after evaluation
- Preserve backward compatibility with TASK 01-12
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT (expanded):
1. what already existed and was reused
   - STEP 0 audit:
     * NO existing Prediction/Forecast/ExpectedState/ActualOutcome model
     * NO existing evaluation/outcome/result/accuracy model
     * NO existing prediction service
     * EpistemicStatus.PREDICTED exists (models.py:74) — reused
     * Timeline exists (models.py:195) — NOT reused (different concept)
     * PotentialPhenomenon (TASK 05) — source candidate
     * EmergenceAnalysisService (TASK 12) — reusable for conditions
   - Decision: create new Prediction model + PredictionService
     (no existing equivalent; first-class model with lifecycle)
2. what was added (Prediction model with 3 timestamps, PredictionService
   with lifecycle + from_potential_phenomenon, tests)
3. what was deliberately not changed
   (EmergenceAnalysisService, BlockageAnalysisService, PotentialPhenomenon,
    Phenomenon, EpistemicStatus, Timeline)
4. tests/results (including 3 required scenarios from ТЗ)
5. remaining gaps

Commit message:
feat: integrate emergence with prediction lifecycle

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 15375166916788906698

Completed at: 2026-09-25T12:34:54Z
