# Jules Task

## Task ID

task-20260924-175201

## Created

2026-09-24T17:52:01Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.5 — Fix stale_lock race in jules-complete.sh + safe-git helpers

CONTEXT:
- Repo: Urania-order/jules
- v1.3.1 added safe_git_commit / safe_git_push in scripts/jules-complete.sh
  to handle stale .git/index.lock (ERRATA-0038).
- Two remaining issues:

ISSUE 1 — ERRATA-0044: flaky test in CI (stale_lock)
- Test: tests/test_jules_complete_flow.py::test_jules_complete_removes_stale_lock
- Fixture: setup_complete_env (uses tmp_path, isolated git repo) — CORRECT
- Symptom:
    * Locally: 465 passed (all green)
    * CI: intermittent failure (assert not lock_file.exists() fails)
    * Re-run alone: passes
- Root cause (hypothesis):
    * scripts/jules-complete.sh line 188:
        if [ -f .git/index.lock ] && ! pgrep -f "git" > /dev/null; then
            rm -f .git/index.lock
        fi
    * pgrep -f "git" is TOO BROAD — matches ANY git process
      (including unrelated background git in CI, other tests, github runner)
    * If any git process is running → lock NOT removed → test fails
    * Line 479 already uses narrower pattern: pgrep -f "git commit"
- Fix: narrow the pattern (see PART 1)

ISSUE 2 — ERRATA-0045: git checkout/pull/stash do not retry on stale index.lock
- Symptom:
    * gh pr merge succeeds
    * git checkout main → fatal: Unable to create '.git/index.lock'
    * git pull → "Already up to date"
- Observed:
    * 2026-09-23 — ERRATA-0042 merge (PR #68)
    * 2026-09-24 — ERRATA-0044 merge (PR #70)
- Cause: v1.3.1 fix covers only jules-complete.sh commit/push;
  git checkout / pull / stash in scripts (line 195, 221) and manual
  commands do NOT have retry
- Impact: manual rm -f .git/index.lock required

GOAL:

PART 1 — Fix stale_lock race in jules-complete.sh (ERRATA-0044)

1. Inspect scripts/jules-complete.sh lines 185-200 and 476-500.
   - Line 188 uses pgrep -f "git" (too broad).
   - Line 479 uses pgrep -f "git commit" (narrow).

2. Narrow the pattern at line 188:
   BEFORE:
       if [ -f .git/index.lock ] && ! pgrep -f "git" > /dev/null; then
           rm -f .git/index.lock
       fi
   AFTER:
       if [ -f .git/index.lock ] && ! pgrep -f "git (commit|push|rebase|merge|checkout|pull|stash)" > /dev/null; then
           rm -f .git/index.lock
       fi
   OR (simpler, more robust):
       if [ -f .git/index.lock ]; then
           # Check if lock is held by an active git process
           if command -v lsof >/dev/null 2>&1; then
               if ! lsof .git/index.lock >/dev/null 2>&1; then
                   rm -f .git/index.lock
               fi
           elif ! pgrep -f "git commit|git push|git checkout|git pull|git stash" >/dev/null 2>&1; then
               rm -f .git/index.lock
           fi
       fi

3. Extract the lock-removal logic into a shared helper function in
   scripts/jules-complete.sh (or scripts/safe-git.sh, see PART 2):
       remove_stale_git_lock() {
           [ -f .git/index.lock ] || return 0
           if command -v lsof >/dev/null 2>&1; then
               lsof .git/index.lock >/dev/null 2>&1 || rm -f .git/index.lock
           else
               pgrep -f "git (commit|push|rebase|merge|checkout|pull|stash)" >/dev/null 2>&1 \
                   || rm -f .git/index.lock
           fi
       }

4. Replace both line 188 and line 479 logic with remove_stale_git_lock.

5. Verify test passes locally 5 times in a row:
       for i in 1 2 3 4 5; do
           ./scripts/test.sh tests/test_jules_complete_flow.py::test_jules_complete_removes_stale_lock 2>&1 | tail -1
       done

PART 2 — Add safe-git helpers (ERRATA-0045)

1. Create scripts/safe-git.sh:

    #!/usr/bin/env bash
    # Safe git operations with stale index.lock handling (ERRATA-0045)
    # Source this file: source scripts/safe-git.sh

    remove_stale_git_lock() {
        [ -f .git/index.lock ] || return 0
        if command -v lsof >/dev/null 2>&1; then
            lsof .git/index.lock >/dev/null 2>&1 || rm -f .git/index.lock
        else
            pgrep -f "git (commit|push|rebase|merge|checkout|pull|stash)" >/dev/null 2>&1 \
                || rm -f .git/index.lock
        fi
    }

    safe_git_checkout() {
        local attempt
        for attempt in 1 2 3; do
            remove_stale_git_lock
            if git checkout "$@"; then return 0; fi
            echo "  [safe_git] checkout attempt $attempt failed; retrying..." >&2
            sleep 2
        done
        echo "  [safe_git] checkout failed after 3 attempts" >&2
        return 1
    }

    safe_git_pull() {
        local attempt
        for attempt in 1 2 3; do
            remove_stale_git_lock
            if git pull "$@"; then return 0; fi
            echo "  [safe_git] pull attempt $attempt failed; retrying..." >&2
            sleep 2
        done
        return 1
    }

    safe_git_fetch() {
        local attempt
        for attempt in 1 2 3; do
            remove_stale_git_lock
            if git fetch "$@"; then return 0; fi
            sleep 2
        done
        return 1
    }

    safe_git_stash_push() {
        remove_stale_git_lock
        git stash push "$@"
    }

    safe_git_stash_pop() {
        remove_stale_git_lock
        git stash pop "$@"
    }

2. In scripts/jules-complete.sh:
   - Source safe-git.sh near the top:
       SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
       # shellcheck source=scripts/safe-git.sh
       source "$SCRIPT_DIR/safe-git.sh"
   - Replace git stash push/pop (lines 195, 221) with safe_git_stash_push / safe_git_stash_pop.
   - Replace inline remove_stale_git_lock logic (lines 188, 479) with helper call.

3. Ensure jules-complete.sh still works standalone.

4. Optional: add scripts/safe-checkout.sh wrapper:
       #!/usr/bin/env bash
       source "$(dirname "$0")/safe-git.sh"
       safe_git_checkout "$@"

PART 3 — Tests

Python tests (extend tests/test_jules_complete_flow.py):
- test_jules_complete_removes_stale_lock_narrow_pattern
    (verify it does NOT remove lock when pgrep matches unrelated git)
- test_jules_complete_removes_stale_lock_with_lsof (if lsof available)

New Python test file (tests/test_safe_git.py):
- test_remove_stale_git_lock_removes_when_no_git_process
- test_remove_stale_git_lock_keeps_when_git_process_running
- test_safe_git_checkout_retries_on_lock
- test_safe_git_pull_retries_on_lock
- test_safe_git_checkout_fails_after_3_attempts
- test_safe_git_stash_push_pop_safe

Shell smoke test:
- Run scripts/safe-git.sh source + safe_git_checkout --help

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

TECHNICAL:
- Files:
    * scripts/safe-git.sh (new)
    * scripts/jules-complete.sh (modify — source safe-git.sh, narrow pgrep,
      replace stash calls)
    * tests/test_jules_complete_flow.py (modify — add narrow-pattern test)
    * tests/test_safe_git.py (new)
- No shell=True
- Reuse v1.3.1 pattern (safe_git_commit / safe_git_push)
- Do NOT break jules-complete.sh existing behavior
- Do NOT weaken existing tests
- ALL tests must pass locally BEFORE commit (ERRATA-0043)
- Run stale_lock test 5x locally to verify non-flakiness

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- safe_git helpers idempotent and safe to call multiple times
- Do NOT remove .git/index.lock if a git process is genuinely running
- Prefer lsof if available; fall back to narrow pgrep
- Preserve backward compatibility with v1.3.x / v1.4
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
   - v1.3.1 safe_git_commit / safe_git_push
   - Existing test structure (setup_complete_env uses tmp_path, isolated)
2. what was added
   - narrowed pgrep pattern / lsof-based lock detection
   - scripts/safe-git.sh (checkout, pull, fetch, stash)
   - new tests
3. what was deliberately not changed
4. tests/results (including 5x repeat of stale_lock test)
5. remaining gaps

Commit message:
fix: narrow stale-lock pgrep + add safe-git helpers (ERRATA-0044, 0045)

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 18028166012338821768

Completed at: 2026-09-24T18:12:22Z
