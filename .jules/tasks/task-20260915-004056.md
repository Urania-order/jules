# Jules Task

## Task ID

task-20260915-004056

## Created

2026-09-14T21:40:56Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

feat/seed-data

## Request

Co-SMOS v0.9 Integration Review — fix before merge.

Review the current Jules patch and fix ONLY the following issues.

1. In tests/test_control_room_api.py remove any acceptance of HTTP 500 as a valid result. In particular, replace assertions like:
assert res_defer.status_code in (200, 400, 500)
with a deterministic assertion based on the actual API contract. HTTP 500 must always fail the test.

2. Review proposal modify/defer tests. Make their fixtures deterministic and assert the actual expected status instead of allowing arbitrary 200/404/500 outcomes.

3. In tests/test_federation_service.py do not weaken the original assertion merely to make the test pass. Investigate database isolation. Preserve deterministic behavior and verify the two nodes created by the test.

4. In tests/test_observatory_service.py do not replace exact assertions with a weak >= 1 assertion without justification. Since the ranking request uses limit=2, test the actual contract or isolate the database so the expected result is deterministic.

5. Add a direct test for ValueEcologyService.assess_knowledge_value(). Verify:
- assessment is created;
- human_benefit is stored;
- social_impact is stored;
- knowledge_value equals benefit * impact;
- calling it again for the same node updates the existing assessment instead of creating a duplicate.

6. Verify the API changes in smos/api/main.py:
- CreateTwinRequest;
- CreateClusterRequest;
- start_session(workspace_id optional).
Search all repository call sites and tests to ensure the changed signatures/contracts do not break existing code.

7. Keep the DiscoverySystem / DiscoveryService import separation. Do not revert it to an alias workaround.

8. Do NOT fix unrelated historical test debt:
- tests/test_ecology_service.py
- tests/test_value_service.py
- tests/test_mcp.py
- missing ecology_service.py
- missing value_service.py
- missing pytest-asyncio configuration.
Do not create fake compatibility modules just to make the full suite green.

9. Do NOT modify runtime-generated .jules/queue/pending, completed, or deferred state as part of this patch.

10. Do not weaken tests, remove assertions, skip failures, or accept HTTP 500 to obtain green tests.

Run:
git diff --check
./scripts/validate.sh
uv run pytest tests/test_control_room_api.py tests/test_domain_api.py tests/test_federation_api.py -q
uv run pytest tests/test_federation_service.py tests/test_observatory_service.py -q
uv run pytest -q --ignore=tests/test_ecology_service.py --ignore=tests/test_value_service.py --ignore=tests/test_mcp.py

At the end report:
- exact files changed;
- exact test results;
- remaining pre-existing failures;
- confirmation that no runtime queue state was included;
- confirmation that no HTTP 500 is accepted as a successful test result.

Do not merge anything automatically.

## Status

started

