# Jules Task

## Task ID

task-20260920-064238

## Created

2026-09-20T03:42:38Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.2.7 — Navbar cleanup + fix task titles + no-op retry fix

CONTEXT:
- Repo: Urania-order/jules
- Frontend: frontend/index.html
- Backend: smos/api/main.py, smos/core/queue.py
- Scripts: scripts/jules-complete.sh
- v1.2.6 applied. Issues found:

ISSUE 1 — Navbar has redundant buttons
- SYNC button: duplicate of Refresh functionality
- Refresh button: duplicate of LED polling (LED updates every 10s)
- Clock: not needed
- Fix: REMOVE SYNC, REMOVE Refresh, REMOVE clock
- Keep only: ONLINE led + queue LED (clickable)

ISSUE 2 — Queue LED should be clickable
- LED updates every 10s (setInterval(pollQueueLed, 10000))
- On click: call refreshData() (refresh page content)
- Add cursor: pointer + hover effect
- Keep tooltip: "Pending: N | Running: N | Blocked: N | Deferred: N"

ISSUE 3 — Task titles broken for some entries
- task-20260920-055407: title="task-20260920-055407" (should be "Co-SMOS v1.2.6 — ...")
- Fallback in queue.py uses tid if request empty
- Fix: fallback order title → request → id
- Normalize stale history entries

ISSUE 4 — ERRATA-0036: no-op blocks retry
- jules-complete.sh treats no-op as applied → skip
- Fix: only `applied` blocks retry
- Add --force flag

GOAL:

PART 1 — Navbar cleanup

1. Remove SYNC button:
   - Remove <button id="btn-sync">SYNC</button>
   - Remove its event listener

2. Remove Refresh button (added in v1.2.6):
   - Remove <button id="btn-nav-refresh">⟳ Refresh</button>
   - Remove its onclick

3. Remove clock:
   - Remove clock element (06:31:47)
   - Remove its setInterval

4. Keep: ONLINE led, queue LED, MANUAL dropdown, RESET QUEUES

PART 2 — Queue LED clickable

1. Make navbar-queue-led clickable:
   - cursor: pointer
   - onclick="refreshData()"
   - title tooltip (already has counts)
   - hover: slight scale or glow

2. CSS:
   .queue-led { cursor: pointer; transition: transform 0.15s; }
   .queue-led:hover { transform: scale(1.3); }

PART 3 — Fix task titles

1. smos/core/queue.py _task_from_dict:
   - Fallback: title → request → id (prefer title)
   - If request empty but title present → use title
   - If both empty → use id

2. scripts/jules-complete.sh:
   - Copy title from active_task (already in v1.2.5)
   - Verify it's written

3. Normalize stale history entries in .co-smos/state.json (one-off script or at startup):
   - For entries with title=null: title = request[:80] or id
   - For entries with title=id but request present: use request[:80]
   - Run on jules-complete.sh startup (idempotent)

PART 4 — ERRATA-0036 fix

1. scripts/jules-complete.sh idempotency check (~line 100):
   - BEFORE: if result in ('applied', 'no-op'): skip
   - AFTER:  if result == 'applied': skip
   - no-op → allow retry

2. Add FORCE env/flag:
   - if [ "${FORCE:-0}" = "1" ]; then skip idempotency

TESTS:
- test_jules_complete_no_op_allows_retry
- test_jules_complete_force_flag
- test_task_from_dict_fallback_title_over_id
- frontend contract: NO btn-sync
- frontend contract: NO btn-nav-refresh
- frontend contract: NO navbar-clock
- frontend contract: navbar-queue-led has onclick="refreshData()"

REQUIREMENTS:
- Files: smos/core/queue.py, scripts/jules-complete.sh,
  frontend/index.html, tests/test_queue_reset.py,
  tests/test_frontend_contract.py
- No shell=True
- Preserve queue LED + Refresh functionality (LED click)

TESTS: pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 10209351041509055200

Completed at: 2026-09-20T03:59:34Z
