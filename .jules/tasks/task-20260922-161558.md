# Jules Task

## Task ID

task-20260922-161558

## Created

2026-09-22T16:15:58Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.3.1 — Fix git index.lock race + retry logic

CONTEXT:
- Repo: Urania-order/jules
- File: scripts/jules-complete.sh
- Issue: git index.lock race → commit skipped
- Observed: TASK 02 (task-20260922-153432)
  - tests passed
  - but [8/9] Committing failed: "Unable to create index.lock"
  - code applied but not committed

GOAL:

1. In scripts/jules-complete.sh, before git commit:
   - Check for .git/index.lock
   - If stale (no active git process) → remove
   - If active → wait + retry

2. Retry git commit (3 attempts):
   for attempt in 1 2 3; do
       if git commit -m "$MSG"; then break; fi
       echo "⚠ commit attempt $attempt failed — retry in 3s"
       sleep 3
       rm -f .git/index.lock
   done

3. Same for git push:
   - Retry 3 attempts
   - Handle transient network errors

4. Add helper function:
   safe_git_commit() {
       local msg="$1"
       for attempt in 1 2 3; do
           if [ -f .git/index.lock ] && ! pgrep -f "git commit" > /dev/null; then
               rm -f .git/index.lock
           fi
           if git commit -m "$msg"; then return 0; fi
           sleep 3
       done
       return 1
   }

   safe_git_push() {
       for attempt in 1 2 3; do
           if git push origin main; then return 0; fi
           sleep 5
       done
       return 1
   }

TESTS:
- test_jules_complete_removes_stale_lock
- test_jules_complete_retries_on_lock

REQUIREMENTS:
- Files: scripts/jules-complete.sh, tests/test_jules_complete_flow.py
- No shell=True

TESTS: pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

## Status

started

