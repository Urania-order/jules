# Jules Task

## Task ID

task-20260922-200334

## Created

2026-09-22T20:03:34Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.3.2 — Fix run-task.sh session matching (ERRATA-0039)

CONTEXT:
- Repo: Urania-order/jules
- File: scripts/run-task.sh
- ERRATA-0039: run-task.sh reports "unknown" for valid session
- Two bugs:
  1. TASK_MATCH (date part) not in Description column
  2. Fallback DESC from wrong task file section (# Jules Task, not ## Request)

GOAL:

1. Fix session matching (multi-level fallback):
   a) First: extract session_id from task log (.jules/results/<TASK_ID>.log)
      SESSION_ID=$(grep -oE 'ID: [0-9]+' .jules/results/$TASK_ID.log | head -1 | awk '{print $2}')
      jules remote list --session | grep -F "$SESSION_ID"
   b) Second: TASK_MATCH (date part) — current
   c) Third: DESC from ## Request section of task file:
      DESC=$(awk '/^## Request/{flag=1;next}/^## /{flag=0}flag' "$TASK_FILE" | head -1 | head -c 40)
   d) Fourth: DESC from anywhere in task file (last resort)

2. Never report "unknown" without trying all fallbacks.
3. If unknown after 60s, print warning.

4. Tests:
   - test_run_task_matches_session_by_log_id
   - test_run_task_matches_session_by_request_desc
   - test_run_task_never_reports_unknown_after_60s

TESTS: pytest tests/ + git diff --check

REQUIREMENTS:
- Files: scripts/run-task.sh, tests/test_run_task_errata39.py (new)
- No shell=True

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 17983204355982451054

Completed at: 2026-09-22T20:15:27Z
