# Jules Task

## Task ID

task-20260919-010101

## Created

2026-09-18T22:01:01Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.2.3 — Stages sort + Admin async + "Done" + fix queue cleanup

CONTEXT:
- Repo: Urania-order/jules
- v1.2.1 Queue UI, v1.2.2 EN + run-task whitelist + upload (applied)
- Found issues:

ISSUE 1 — Stages view not sorted (oldest first)

ISSUE 2 — Admin Run Next blocks UI (ERRATA-0032, sync dispatch)

ISSUE 3 — jules-complete.sh cleanup moves admin-*.txt to deferred/
  - scripts/jules-complete.sh lines 188-214:
      FORBIDDEN_UNTRACKED="$(git ls-files --others --exclude-standard \
        | grep -E '^\.(co-smos|jules/(tasks|results|queue))/' \
        | grep -v "^\.jules/tasks/${TASK_ID}\.md$" \
        | grep -v "^\.jules/results/${TASK_ID}\.log$" \
        | grep -v "^\.jules/results/post-complete-.*\.log$" || true)"
      ... mv "$f" ".jules/queue/deferred/${base}"
  - This moves .jules/queue/running/admin-*.txt (and pending/admin-*.txt)
    into deferred/ because it matches the regex and is not excluded.
  - deferred/ is ALSO used by smos/core/proposals.py for PROPOSAL files.
  - Result: admin queue files get lost in deferred/.
  - FIX: jules-complete.sh cleanup must EXCLUDE .jules/queue/{pending,running,completed}/admin-*.txt

GOAL:

PART 1 — Stages sort

1. frontend/index.html: find renderStages() (or similar).
   Apply sortTasksByCreatedAt(tasks, true). Reuse helper (line 1875).
2. Test: test_frontend_contract_stages_sorted

PART 2 — Async run-next + job status (ERRATA-0032)

1. POST /api/admin/queue/run-next — ASYNC
   - Start thread (daemon=True) running subprocess.run(run-task.sh, running_file, ...)
   - Return {started: true, job_id: <uuid8>, filename} immediately
   - RUN_JOBS dict: {status, filename, started_at, finished_at, task_id, exit_code, stdout, stderr}
   - On completion:
       * exit_code == 0: move running/ -> completed/
       * exit_code != 0: keep in running/ + append runner.log
       * NEVER move to deferred/
   - Write admin_audit entry
2. GET /api/admin/queue/jobs/{job_id}
3. GET /api/admin/queue/jobs (last 20)

PART 3 — Frontend polling + "Done" indicator

1. adminRunNext():
   - POST -> {started, job_id}
   - Save job_id in localStorage
   - Poll GET /jobs/{job_id} every 5s
   - Output:
       running:   "Running... (Xs elapsed)"
       completed: "DONE (Xs) - task_id: ..." + stdout tail
       failed:    "FAILED - exit_code: N" + stderr tail
   - Stop polling on done/failed
2. On page reload: if last job_id in localStorage, check status + resume

PART 4 — Fix jules-complete.sh cleanup

1. scripts/jules-complete.sh (line 188):
   Extend the exclusion regex to preserve queue admin files:
   BEFORE:
     FORBIDDEN_UNTRACKED="$(git ls-files --others --exclude-standard 2>/dev/null \
       | grep -E '^\.(co-smos|jules/(tasks|results|queue))/' \
       | grep -v "^\.jules/tasks/${TASK_ID}\.md$" \
       | grep -v "^\.jules/results/${TASK_ID}\.log$" \
       | grep -v "^\.jules/results/post-complete-.*\.log$" \
       || true)"
   AFTER (add):
       | grep -v "^\.jules/queue/pending/admin-.*\.txt$" \
       | grep -v "^\.jules/queue/running/admin-.*\.txt$" \
       | grep -v "^\.jules/queue/completed/admin-.*\.txt$" \
       | grep -v "^\.jules/queue/pending/.*\.meta\.json$" \
       | grep -v "^\.jules/queue/running/.*\.meta\.json$" \
       | grep -v "^\.jules/queue/completed/.*\.meta\.json$" \
2. Add a comment explaining: admin-*.txt are legitimate queue files
3. Add test: test_jules_complete_does_not_move_admin_queue_files

REQUIREMENTS:
- Files: smos/api/main.py, frontend/index.html, scripts/jules-complete.sh,
  tests/test_admin_api.py, tests/test_frontend_contract.py
- No shell=True
- Reuse run-task.sh + sortTasksByCreatedAt
- Do NOT modify smos/core/proposals.py

TESTS:
- test_stages_sorted
- test_run_next_returns_job_id
- test_run_next_async_completes
- test_run_next_moves_to_completed_on_success
- test_jules_complete_does_not_move_admin_queue_files
- test_queue_jobs_endpoint
- frontend contract: "Done" indicator, polling, stages sorted

TESTS:
- pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not modify .jules/queue/pending/*.gitkeep
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started

