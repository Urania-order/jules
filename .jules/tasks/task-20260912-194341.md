# Jules Task

## Task ID

task-20260912-194341

## Created

2026-09-12T19:43:41+00:00

## Repository

https://github.com/Urania-order/jules

## Branch

main

## Request

Комплексне розширення Co-SMOS: метрики, колаборація, embeddings.

Контекст:
- smos/core/interfaces.py визначає Observable з get_health_metrics() і get_evolution_summary()
- ObservatoryService.get_health_report() агрегує метрики, але не всі підсистеми мають осмислені метрики
- CommonsService реалізує Observable, але має базові методи
- MemoryService.process_event() генерує embeddings, але api/main.py::create_event може не викликати його

Завдання:

ЧАСТИНА 1 — get_health_metrics() для всіх Observable:
1. Перевірити, які сервіси реалізують Observable (EcologyEngine, ValueEcologyService, CommonsService, DiscoverySystem)
2. Для кожного, хто не реалізує або має mock — додати реальні метрики з БД:
   - EcologyEngine: health, active_clusters, total_resonance (вже є — перевірити)
   - ValueEcologyService: avg_value, total_social_impact (вже є — перевірити)
   - CommonsService: active_collaborations, cosmonaut_population, interaction_density
   - DiscoverySystem: active_hypotheses, analogies_found
3. Всі метрики мають бути реальними (query до БД), не mock

ЧАСТИНА 2 — Розширити CommonsService:
1. Додати find_collaborators(cosmonaut_id, limit=5) — пошук партнерів за профілем
2. Додати get_active_collaborations() — список активних колаборацій з БД
3. Додати record_collaboration_outcome(collab_id, outcome: str, metrics: dict)
4. Додати get_collaboration_history(cosmonaut_id) — історія
5. Оновити get_health_metrics() — реальні дані з БД (кількість cosmonaut, collaborations)

ЧАСТИНА 3 — Embeddings у create_event:
1. Перевірити, чи AgentOrchestrator.on_event() викликає MemoryService.process_event()
2. Якщо ні — додати виклик
3. Перевірити, що при POST /event — MemoryNode створюється з embeddings
4. Додати тест у tests/test_api_basic.py: після POST /event перевірити, що MemoryNode має 1536-вимірний embeddings

ТЕСТИ:
- tests/test_health_metrics.py — перевірити, що кожна Observable підсистема повертає dict з метриками
- tests/test_commons_service.py — тести для нових методів
- Оновити tests/test_api_basic.py — перевірка embeddings після POST /event

ВИМОГИ:
- Всі тести мають проходити: uv run pytest tests/ -q --ignore=tests/test_ecology_service.py --ignore=tests/test_value_service.py --ignore=tests/test_mcp.py
- ./scripts/validate.sh має проходити
- НЕ чіпати .co-smos/ і .jules/
- Звітувати за форматом AGENTS.md §14 (STATUS, CHANGES, TESTS, RISKS, NEXT STEP)

## Status

started

