# Jules Task

## Task ID

task-20260918-055754

## Created

2026-09-18T02:57:55Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.1.4 — Fix state.json history + Remove navbar RESET button

CONTEXT:
- Repo: Urania-order/jules
- Files:
    * smos/core/queue_reset.py  — backend reset logic
    * frontend/index.html       — navbar + views
- Two issues to fix:

  ### Issue 1 (HIGH, ERRATA-0027): state.json history overwritten by Reset
  - Location: smos/core/queue_reset.py:306, 318-320
    "Update state.json by removing cleared tasks"
    if isinstance(state_data.get("history"), list):
        state_data["history"] = [
            t for t in state_data["history"]
            if not (isinstance(t, dict) and t.get("id") in removed_task_ids)
        ]
  - Impact: after RESET, all history entries whose task ids were in scope
    are removed. Even completed/applied tasks vanish from audit.
  - Correct pattern (compare): scripts/jules-complete.sh:265
        state["history"].append(completed)   ← APPEND-only

  ### Issue 2 (MEDIUM, safety): RESET QUEUES button in navbar
  - Location: frontend/index.html navbar (top row)
  - The ⚠ RESET QUEUES button sits next to SYNC — easy to click accidentally.
  - It triggers reset scope=all without confirmation.
  - Users have accidentally wiped queues.
  - Remove this button from the navbar.
  - Keep the RESET ALL QUEUES button in Queue Management view (with its
    existing confirmation/behavior).
  - Keep Reset Center → Columns tab (btn-reset-ready etc.).

GOAL:
1. Make state.json["history"] APPEND-ONLY.
   - Remove filter at queue_reset.py:318-320.
   - Same for state_data["tasks"] at lines 325-329.
   - Reset MAY append a reset event:
         state_data["history"].append({
           "action": "reset",
           "timestamp": <ISO8601>,
           "scope": <scope>,
           "moved": <total_moved>,
         })
   - Keep active_task / last_task cleanup ONLY if they refer to cleared task
     (those are pointers, not audit log).

2. Remove RESET QUEUES button from navbar in frontend/index.html.
   - Search for the navbar button with text "RESET QUEUES" and id like
     "btn-reset-all-topbar" or similar.
   - Remove the button element.
   - Remove its onclick handler if it's unique.
   - Do NOT remove RESET ALL QUEUES in Queue Management view.
   - Do NOT remove Reset Center → Columns buttons.

REQUIREMENTS:
- Do not break Reset functionality (still clears queue, still archives)
- Do not break Undo (still creates new tasks)
- Add backend tests:
    * test_reset_preserves_history
        - Given state.json with N completed tasks
        - After reset-column (scope=all)
        - history length >= N
        - Those task ids still present in history
    * test_reset_appends_event (if event added)
    * test_reset_does_not_crash_on_empty_history
    * test_reset_does_not_crash_on_missing_history
    * test_reset_clears_active_task_if_matching
    * test_reset_clears_last_task_if_matching
- Add frontend contract test:
    * navbar does NOT contain a RESET QUEUES button
    * Queue Management view still has RESET ALL QUEUES button
    * Reset Center Columns tab still has btn-reset-* buttons

TESTS:
- pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/state.json (runtime file)
- Do not weaken existing tests
- Keep .jules/queue/reset-*.jsonl archive format unchanged
- Do not touch .jules/tasks/, .jules/results/

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 14708426532954196345

Completed at: 2026-09-18T13:33:10Z
