# Jules Task

## Task ID

task-20260920-155505

## Created

2026-09-20T15:55:05Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.3.0 — LED running count + sort fallback + auto created_at normalize

CONTEXT:
- Repo: Urania-order/jules
- Files: smos/api/main.py, frontend/index.html, scripts/jules-complete.sh
- Three issues + one automation:

ISSUE 1 — LED shows "Running: 0" while task is running
- /api/queue/status counts only .jules/queue/running/*.txt
- Tasks started via run-task.sh have status=RUNNING in state.json
- Fix: also count state.json tasks with status=RUNNING

ISSUE 2 — sortTasksByCreatedAt fallback uses '' (not id)
- If created_at null → sorts LAST
- Fix: fallback to parse id

ISSUE 3 — created_at is inconsistent (needs manual fix repeatedly)
- Many history entries have created_at from datetime.now() (not id timestamp)
- Or null → manual fix every time
- Fix: AUTO-normalize in jules-complete.sh + on uvicorn startup

GOAL:

PART 1 — LED running count (smos/api/main.py)

In GET /api/queue/status:
    from smos.core.queue import QueueManager
    from smos.core.task import TaskStatus
    qm = QueueManager()
    tasks = qm.list_all_tasks()
    running_tasks = sum(1 for t in tasks if t.status == TaskStatus.RUNNING)
    return {
        "pending": count("pending"),
        "running": count("running") + running_tasks,
        "running_files": count("running"),
        "running_tasks": running_tasks,
        "blocked": count("blocked"),
        "deferred": count("deferred"),
    }

PART 2 — sortTasksByCreatedAt fallback (frontend/index.html)

In sortTasksByCreatedAt:
    const key = (t) => {
        if (t.created_at) return t.created_at;
        const id = t.id || '';
        const m = id.match(/^task-(\d{4})(\d{2})(\d{2})-(\d{2})(\d{2})(\d{2})/);
        if (m) return `${m[1]}-${m[2]}-${m[3]}T${m[4]}:${m[5]}:${m[6]}+00:00`;
        return '';
    };

PART 3 — AUTO-normalize created_at

3a. In scripts/jules-complete.sh (after state.json update [5/9]):
    Add python3 block:
    - Parse .co-smos/state.json
    - For each history/tasks/last_task entry:
        * If id matches task-YYYYMMDD-HHMMSS:
            * set created_at = ISO8601 (UTC from id)
    - Write back if changed
    - Print "normalized created_at for N task(s)"

3b. In smos/api/main.py lifespan (startup):
    - Before starting scheduler_task:
        try:
            normalize_created_at_state(".co-smos/state.json")
        except Exception as e:
            logger.warning(f"normalize_created_at failed: {e}")
    - Implement normalize_created_at_state(path) in smos/core/state.py or similar
    - Idempotent

3c. Add scripts/normalize-state.sh (standalone):
    - Same logic, callable manually or from cron

TESTS:
- test_queue_status_includes_running_tasks
- test_frontend_contract_sort_uses_id_fallback
- test_normalize_created_at_idempotent
- test_normalize_created_at_from_id

REQUIREMENTS:
- Files: smos/api/main.py, frontend/index.html, scripts/jules-complete.sh,
  scripts/normalize-state.sh (new), tests/test_admin_api.py,
  tests/test_frontend_contract.py, tests/test_queue_reset.py
- No shell=True

TESTS: pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/ (except read state.json)
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 3099323807965562247

Completed at: 2026-09-20T16:08:06Z
