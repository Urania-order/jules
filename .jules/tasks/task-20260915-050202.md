# Jules Task

## Task ID

task-20260915-050202

## Created

2026-09-15T02:02:02Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.9 Frontend Patch A — UI consistency patch

CONTEXT:
- Repo: Urania-order/jules
- Frontend: frontend/index.html
- Backend: smos/api/main.py
- Current baseline: commit b52cc154d5b6065b74a126d2c954fd6081fe8467
- This repository ALREADY contains part of the v0.9 implementation.
- DO NOT reimplement or rewrite functionality that already exists.
- First inspect the current frontend/index.html and existing tests.
- Then implement ONLY missing or incorrect parts listed below.

IMPORTANT:
This is an incremental patch, not a rewrite.

BEFORE CODING:
1. Inspect frontend/index.html.
2. Search for all requested IDs/classes/functions.
3. Inspect tests/test_control_room_api.py and any existing frontend tests.
4. Determine which requirements are already implemented.
5. Preserve all working existing functionality.
6. Do not modify backend endpoints unless absolutely required by the frontend contract.
7. Do not touch .co-smos/ or .jules/tasks/.

REQUIRED FIXES:

1. PRIORITY NORMALIZATION
Current v0.9 canonical priority is 1-5.

Required:
- task-priority input:
  id="task-priority"
  min="1"
  max="5"
  value="3"
- label exactly:
  Priority (1-5)

Also inspect JS validation/defaults so no frontend path still assumes 1-10.

2. SYNC
Add only if missing:
- <button id="btn-sync">⟳ SYNC</button>

Handler:
- show "⟳ SYNCING..."
- call refreshData()
- on success show "✓ SYNCED"
- restore normal "⟳ SYNC"
- on failure show error/offline state
- do not create a second refresh implementation if refreshData already exists.

3. QUEUE VIEW

Transform the queue presentation to five status columns:

READY
RUNNING
REVIEW
BLOCKED
COMPLETED

Required:
- CSS grid class .queue-columns
- individual .queue-column
- PENDING tasks must appear in READY
- READY is the only draggable column
- RUNNING, REVIEW, BLOCKED, COMPLETED are read-only
- Drag/drop must NOT imply execution or completion
- Do not add backend state transitions merely for drag/drop
- preserve existing task click/detail behavior

If a queue structure already exists, modify it minimally rather than replacing the view.

4. PROPOSAL MODIFY UI

Backend endpoint already exists in the current baseline.

Add only missing UI:
- Accept
- Modify
- Defer
- Reject

Modify:
- opens modal
- textarea for description
- priority input constrained to 1-5
- submit POST /api/proposals/{id}/modify
- payload:
  {description, priority}

Reuse existing modal infrastructure if present.

5. ERROR / OFFLINE

Add only if missing:
- window.addEventListener('offline', showOffline)
- banner:
  "⚠ Co-SMOS backend unavailable [RETRY]"

RETRY must invoke the existing refreshData() path.

Do not create duplicate error banners or duplicate retry handlers.

6. CSS

Add only missing classes:
- .queue-columns
- .queue-column
- .error-state
- .loading-state
- .modal-overlay
- .modal
- .modal-actions
- .btn-sm

Preserve existing CSS variables and visual language.

TESTS:

Create tests/test_frontend_contract.py if it does not already exist.

Contract checks must verify the FINAL source contains:
- id="task-priority"
- min="1"
- max="5"
- id="btn-sync"
- queue READY/RUNNING/REVIEW/BLOCKED/COMPLETED
- .queue-columns
- openModifyModal or equivalent existing modify function
- offline handler
- retry mechanism

Do not write tests that merely check that a string exists if a stronger structural assertion is practical.

Run:
- git diff --check
- targeted frontend contract tests
- existing Control Room API tests
- relevant regression tests

Do not weaken, skip, or delete existing tests.

REPORT FORMAT:
Use AGENTS.md §14.

FINAL REPORT MUST STATE:
- what was already present and therefore preserved
- what was actually changed
- tests executed and results
- any remaining v0.9 gaps

AFTER COMPLETION:
Suggest 3-5 next steps, but do not implement them.

## Status

started

