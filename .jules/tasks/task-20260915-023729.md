# Jules Task

## Task ID

task-20260915-023729

## Created

2026-09-14T23:37:29Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

feat/seed-data

## Request

Continue the current Co-SMOS v0.9 Integration Review corrective task. Do NOT start a new task and do NOT merge anything.

Fix ONLY the following issues in the current Jules patch:

1. PROPOSAL API CONTRACT
- Revert the current production implementation change that replaced JulesCLIAdapter with ProposalManager in:
  /api/proposals/{id}/accept
  /api/proposals/{id}/defer
  /api/proposals/{id}/reject
- Restore the existing JulesCLIAdapter-based runtime behavior and existing response contract.
- Do NOT redesign ProposalManager.
- Do NOT introduce unrelated proposal runtime behavior.
- Keep the deterministic proposal tests required below.

2. CONTROL ROOM PROPOSAL TESTS
- In tests/test_control_room_api.py use deterministic tmp_path/monkeypatch fixtures.
- Verify BOTH MODIFY and DEFER proposal behavior.
- Do not accept HTTP 500 anywhere.
- Assert exact expected HTTP status and relevant response fields.
- Add/fix the deterministic MODIFY test if missing.
- Do not weaken assertions to make tests pass.

3. OBSERVATORY SERVICE
- Inspect tests/test_observatory_service.py.
- Preserve the original exact assertions.
- Do NOT use >= instead of an exact contract.
- Isolate the DB deterministically if necessary.
- Test the actual limit=2 contract.
- With two valid assessments, assert exactly two results are returned.

4. FEDERATION SERVICE
- Preserve exact two-node behavior.
- Keep deterministic DB isolation.
- Register exactly two nodes.
- Assert total_nodes == 2.
- Assert health == 1.0.
- Assert exactly those two registered node IDs are returned.
- Do NOT weaken assertions.

5. VALUE ECOLOGY
- Keep ValueEcologyService.assess_knowledge_value().
- Add/keep a direct test in tests/test_value_ecology_service.py verifying:
  create;
  human benefit;
  social impact;
  calculated knowledge value;
  update of the same assessment;
  no duplicate assessment.
- Assert the assessment ID remains unchanged after update.
- Assert the count for that node remains exactly 1.

6. API CONTRACTS
Inspect smos/api/main.py and every relevant call site/test for:
- CreateTwinRequest
- CreateClusterRequest
- start_session(workspace_id optional)
Ensure signatures, request models, call sites and tests are consistent.
Do not make unrelated API changes.
workspace_id must remain optional.

7. DISCOVERY IMPORT SEPARATION
Keep:
from smos.services.discovery_system import DiscoverySystem
from smos.services.discovery_service import DiscoveryService
Do not reintroduce an alias import.
Verify both are used correctly.

8. STRICT SCOPE
Do NOT fix:
- tests/test_ecology_service.py
- tests/test_value_service.py
- tests/test_mcp.py
- missing unrelated service modules
- missing pytest-asyncio
- unrelated historical debt
- unrelated failing tests.

9. RUNTIME QUEUE SAFETY
Do NOT modify:
.jules/queue/pending
.jules/queue/completed
.jules/queue/deferred
Do not create, delete, reorder, or rewrite runtime queue state.
Use isolated temporary fixtures/databases for tests.

10. TEST INTEGRITY
- No skipped tests.
- No xfail.
- No acceptance of HTTP 500.
- No weakened exact assertions.
- Do not change tests merely to accommodate incorrect production behavior.
- Investigate failures instead.

11. RUN ALL REQUIRED CHECKS

git diff --check

./scripts/validate.sh

uv run pytest tests/test_control_room_api.py tests/test_domain_api.py tests/test_federation_api.py -q

uv run pytest tests/test_federation_service.py tests/test_observatory_service.py -q

uv run pytest tests/test_value_ecology_service.py -q

uv run pytest -q --ignore=tests/test_ecology_service.py --ignore=tests/test_value_service.py --ignore=tests/test_mcp.py

12. FINAL REVIEW
Inspect the final git diff and verify:
- only task-related files changed;
- no runtime .jules/queue state changed;
- no HTTP 500 is accepted;
- no exact assertion was weakened;
- MODIFY and DEFER proposal tests are deterministic;
- observatory limit=2 is exact;
- federation verifies exactly two nodes;
- ValueAssessment updates without duplication;
- API request models/signatures/call sites are consistent;
- DiscoverySystem/DiscoveryService imports remain separated.

Required workflow:
ANALYZE → PLAN → IMPLEMENT → TEST → REVIEW → REPORT.

Do NOT merge.

Final report must state:
1. exact files changed;
2. exact tests added/modified;
3. exact commands and results;
4. remaining failures and which are pre-existing/out-of-scope;
5. explicit confirmation that .jules/queue runtime state was not modified;
6. explicit confirmation that no HTTP 500 is accepted by corrected tests;
7. concise explanation that the existing Proposal API runtime contract was preserved.

## Status

started

