# Jules Task

## Task ID

task-20260915-030752

## Created

2026-09-15T00:07:52Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

feat/seed-data

## Request

FINAL CORRECTION — DO NOT MERGE.

Inspect the current patch from the preceding task and fix ONLY the remaining Value Ecology contract issue plus perform the requested final verification.

1. VALUE ECOLOGY — CRITICAL
The required contract for ValueEcologyService.assess_knowledge_value() is:
    knowledge_value = human_benefit * social_impact

Do NOT introduce calculate_metrics(), base_value, weighted 0.4/0.4/0.2 logic, or any other formula into this method.

The method must:
- find the existing ValueAssessment by node_id;
- create it if absent;
- set human_benefit;
- set social_impact;
- set knowledge_value exactly to human_benefit * social_impact;
- commit and refresh;
- return the same assessment on subsequent calls;
- never create a duplicate for the same node.

2. VALUE ECOLOGY TEST
Strengthen tests/test_value_ecology_service.py so the exact calculated value is verified:
- first call: human_benefit=0.9, social_impact=0.8, assert knowledge_value == 0.72
- second call: human_benefit=0.95, social_impact=0.85, assert knowledge_value == 0.8075
- assert assessment ID is unchanged;
- assert count for that node is exactly 1.

Do not weaken these assertions.

3. FINAL PROPOSAL CONTRACT REVIEW
Verify smos/api/main.py:
- /api/proposals/{id}/accept
- /api/proposals/{id}/defer
- /api/proposals/{id}/reject

The existing JulesCLIAdapter runtime behavior and response contract must be preserved.
Do not use ProposalManager for these runtime endpoints unless it was already present before this task.

4. FINAL API CONTRACT REVIEW
Verify:
- CreateTwinRequest exists and is used consistently;
- CreateClusterRequest exists and is used consistently;
- start_session(workspace_id is Optional) remains backward compatible;
- all relevant call sites/tests are consistent;
- DiscoverySystem and DiscoveryService remain separate imports and usages.

5. TEST INTEGRITY
Do not:
- accept HTTP 500;
- add skip;
- add xfail;
- weaken exact assertions;
- modify unrelated historical debt;
- modify .jules/queue/pending;
- modify .jules/queue/completed;
- modify .jules/queue/deferred;
- modify runtime queue state in any way.

6. RUN:
git diff --check
./scripts/validate.sh
uv run pytest tests/test_control_room_api.py tests/test_domain_api.py tests/test_federation_api.py -q
uv run pytest tests/test_federation_service.py tests/test_observatory_service.py -q
uv run pytest tests/test_value_ecology_service.py -q
uv run pytest -q --ignore=tests/test_ecology_service.py --ignore=tests/test_value_service.py --ignore=tests/test_mcp.py

7. FINAL REPORT
Report:
- exact files changed;
- exact tests changed;
- exact test commands/results;
- remaining failures and whether pre-existing/out-of-scope;
- confirmation runtime .jules/queue state was untouched;
- confirmation no test accepts HTTP 500;
- confirmation Proposal API runtime behavior uses the existing JulesCLIAdapter contract;
- confirmation assess_knowledge_value uses exactly human_benefit * social_impact.

Required workflow:
ANALYZE → PLAN → IMPLEMENT → TEST → REVIEW → REPORT.

Do NOT merge.

## Status

started

