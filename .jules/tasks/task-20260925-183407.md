# Jules Task

## Task ID

task-20260925-183407

## Created

2026-09-25T18:34:07Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.10 — TASK 16 — OBSERVATORY INTEGRATION

CONTEXT:
- Repo: Urania-order/jules
- Existing ontology TASK 01-15 applied.
- Existing ObservatoryService at smos/services/observatory_service.py:
    - 284 lines
    - class ObservatoryService:
        __init__(db, subsystems: List[Observable])
        measure_ecosystem_health() -> Dict
        get_proposal_metrics() -> Dict
        get_health_report() -> Dict
        generate_quarterly_report() -> Dict
        get_evolution_history() -> List
        get_impact_ranking(limit) -> List
        generate_recommendations() -> List
        generate_forecasts() -> List
        export_report_json() / export_report_markdown()
    - Aggregates subsystems via Observable interface.
- Existing Observable interface at smos/core/interfaces.py:15:
    class Observable(ABC):
        def get_health_metrics(self) -> Dict
        def get_evolution_summary(self) -> List
- Existing Observatory API endpoints (smos/api/main.py):
    2416: GET /observatory/health
    2421: GET /observatory/proposals
    2426: GET /observatory/report
    2431: GET /observatory/ranking
    2436: GET /observatory/recommendations
    2441: GET /observatory/export/json
    Prefix: /observatory/... (historical, no /api/)
- Existing TASK 10-14 services (for domain aggregation):
    ConvergentResonanceService, BlockageAnalysisService,
    EmergenceAnalysisService, PredictionService, DomainRelationService
- Existing TASK 15 API:
    /api/phenomena, /api/contexts, /api/potential-phenomena,
    /api/constraints, /api/domain-relations, /api/predictions
- Existing tests:
    tests/test_observatory_api.py, tests/test_observatory_service.py

============================================================
STEP 0 — OBSERVATORY AUDIT (COMPLETED)
============================================================

Findings:
1. ObservatoryService exists at smos/services/observatory_service.py:284 lines
2. Uses Observable interface (smos/core/interfaces.py:15)
3. Existing subsystems: EcologyEngine, ValueEcologyService, CommonsService,
   DiscoverySystem, EvolutionTimelineService, etc.
4. Existing endpoints at /observatory/* (no /api/ prefix)
5. NO existing domain-state aggregation (Phenomenon/Context/Constraint/
   Potential/Prediction/Resonance)
6. Existing pattern: aggregator builds dict from subsystem.get_health_metrics()

DECISION:
   - Extend existing ObservatoryService.
   - Add new method get_domain_state() -> Dict.
   - Add new endpoint /observatory/domain-state (or /observatory/domain).
   - Reuse existing TASK 10-14 services (read-only).
   - Do NOT create a second dashboard system.
   - Do NOT create a second aggregation engine.
   - Do NOT modify existing Observatory methods.

============================================================
OBJECTIVE
============================================================

Extend existing Observatory.

Show:
    WHAT EXISTS
    WHAT IS EMERGING
    WHAT IS BLOCKED
    WHAT COULD EMERGE
    WHAT CHANGES THE CONTEXT
    WHAT RESONATES
    WHAT IS PREDICTED

Reuse existing Observatory aggregation.
No second dashboard system.

============================================================
DESIGN — Extend ObservatoryService
============================================================

Add to smos/services/observatory_service.py:

    def get_domain_state(self) -> Dict[str, Any]:
        """Aggregate domain entity state for Observatory.

        Reuses existing TASK 01-14 services (read-only).
        Does NOT modify existing Observatory methods.
        """
        ...

Mapping (реалізувати як окремі секції):

1. WHAT EXISTS
   - phenomena with epistemic_status == OBSERVED
   → list of {id, name, epistemic_status, ...}

2. WHAT IS EMERGING
   - phenomena with epistemic_status in (INFERRED, HYPOTHESIZED)
   → list

3. WHAT IS BLOCKED
   - for each phenomenon, call BlockageAnalysisService.analyze_blockage(id)
   - collect phenomena with direct_constraints OR indirect_constraints non-empty
   → list of {phenomenon_id, direct_count, indirect_count, unknowns}

4. WHAT COULD EMERGE
   - PotentialPhenomenon with status == POSSIBLE
   → list of {id, phenomenon, status, required_conditions}

5. WHAT CHANGES THE CONTEXT
   - DomainRelation where relation_type in (CHANGES_CONTEXT, CREATES_CONTEXT)
   → list of {source_type, source_id, target_type, target_id, relation_type}

6. WHAT RESONATES
   - ConvergentResonanceService.detect() → candidates
   → list of {kind, confidence, supporting_trajectories}

7. WHAT IS PREDICTED
   - predictions with epistemic_status == PREDICTED
   → list of {id, expected_state, prediction_time, expected_at, confidence}

Add endpoint in smos/api/main.py (following existing convention):

    @app.get("/observatory/domain-state")
    def get_observatory_domain_state(db: Session = Depends(get_db)):
        obs = _get_observatory(db)
        return obs.get_domain_state()

Alternative: extend /observatory/health response with domain_state section.
Decision: separate endpoint /observatory/domain-state (clearer).

FILES TO MODIFY:
1. smos/services/observatory_service.py (add get_domain_state + helpers)
2. smos/api/main.py (add endpoint)

FILES TO CREATE:
3. tests/test_observatory_domain.py (new)

============================================================
TESTS (tests/test_observatory_domain.py)
============================================================

Use: TestClient(app), pattern from tests/test_observatory_api.py

- test_domain_state_exists_endpoint
- test_domain_state_response_shape
- test_domain_state_what_exists
- test_domain_state_what_emerging
- test_domain_state_what_blocked
- test_domain_state_what_could_emerge
- test_domain_state_what_changes_context
- test_domain_state_what_resonates
- test_domain_state_what_predicted
- test_domain_state_empty_db
- test_domain_state_does_not_modify_entities (read-only)
- test_observatory_existing_endpoints_unchanged
- test_observatory_health_still_works

LIMIT: ≤1000 changed code lines.

TECHNICAL:
- Reuse ObservatoryService, Observable interface
- Reuse TASK 10-14 services (read-only)
- Do NOT modify existing Observatory methods
- Do NOT create second dashboard system
- Follow existing /observatory/... prefix
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Existing Observatory endpoints unchanged
- Read-only aggregation
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added (get_domain_state, endpoint, tests)
3. what was deliberately not changed (existing Observatory methods)
4. tests/results
5. remaining gaps

Commit message:
feat: integrate phenomenon context state into observatory

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 18095432188304261731

Completed at: 2026-09-25T18:49:12Z
