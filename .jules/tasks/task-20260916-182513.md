# Jules Task

## Task ID

task-20260916-182513

## Created

2026-09-16T15:25:13Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.9 — Scheduler Backend for Batch (night / window)

CONTEXT:
- Repo: Urania-order/jules
- Backend: smos/api/main.py (FastAPI)
- Existing BatchManager: smos/core/batch.py
- Existing POST /api/batch/run with schedule: now | now-sequential | night | window
- Currently "night" queues but does not run, "window" returns 400 SCHEDULER_NOT_IMPLEMENTED
- Frontend: view-batch with batch-schedule selector
- Spec: Co-SMOS v0.9 §14, §25

GOAL:
Implement a background scheduler that runs queued batches
when their scheduled time window is reached.

REQUIREMENTS:

1. NEW MODULE smos/core/scheduler.py
- class Scheduler
- register_schedule(name, config) with config:
    {"start": "HH:MM", "end": "HH:MM", "days": ["mon",...]}
- is_due(name, now=None) -> bool
- check_due_batches() -> list of batch_ids started
- default schedules:
    night:  02:00-06:00, all days
    window: 09:00-18:00, mon-fri
- read overrides from env:
    JULES_SCHEDULE_NIGHT_START, JULES_SCHEDULE_NIGHT_END
    JULES_SCHEDULE_WINDOW_START, JULES_SCHEDULE_WINDOW_END

2. INTEGRATE INTO BatchManager (smos/core/batch.py)
- run_due_batches() -> list of batch_ids started
- only touch batches with status == "queued"
- respect concurrency limit
- update batch.status to "running" then "completed"
- must NOT touch "now" or "now-sequential" batches

3. BACKGROUND LOOP in FastAPI
- use lifespan context manager
- asyncio.create_task(background_scheduler_loop())
- loop: try bm.run_due_batches(); except log; await asyncio.sleep(60)
- cancel task on shutdown

4. NEW ENDPOINTS
GET  /api/scheduler/schedules    -> list of registered schedules
POST /api/scheduler/schedules    -> register/override a schedule
GET  /api/scheduler/status       -> scheduler health + last run
POST /api/scheduler/trigger      -> force run due batches (for tests)

5. UPDATE POST /api/batch/run
- remove 400 SCHEDULER_NOT_IMPLEMENTED for "window"
- "window" now queues the batch (like "night")

6. TESTS tests/test_scheduler.py (new)
- test_scheduler_registers_default_schedules
- test_scheduler_is_due_night_in_window
- test_scheduler_is_due_night_outside_window
- test_scheduler_is_due_window_in_window
- test_scheduler_is_due_window_outside_window
- test_scheduler_runs_due_batches
- test_scheduler_does_not_run_early_batches
- test_scheduler_respects_concurrency
- test_scheduler_updates_batch_status
- test_scheduler_does_not_touch_now_batches
- test_batch_run_window_now_works
- test_scheduler_endpoint_status
- test_scheduler_endpoint_register
- test_scheduler_trigger_for_testing

Use POST /api/scheduler/trigger to force runs in tests.
Use mock time or pass now= to is_due.

7. FRONTEND view-batch
- show scheduled_for for each queued batch
- show scheduler status LED (green = active, yellow = waiting)
- add "Trigger now" button (only effective if role allows)
- do NOT auto-trigger

8. DOCS docs/COSMOS-JULES-WORKFLOW.md §19 (Scheduler)
- default schedules
- how to override via env
- how the background loop works
- how to test with /api/scheduler/trigger
- timezone note (UTC by default, override with TZ)

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/, .jules/queue/.
- Do not change API_BASE.
- Do not weaken existing tests.
- Do not add heavy dependencies (use stdlib + asyncio).
- Do not run "night" schedules during tests (use trigger or mock time).
- Do not break "now" or "now-sequential" behavior.

TESTS:
- new scheduler tests
- existing batch tests still pass
- existing contract tests still pass
- git diff --check

REPORT FORMAT: AGENTS.md §14.

FINAL REPORT:
1. what already existed and was reused
2. what was added (scheduler.py, endpoints, tests, docs)
3. what was deliberately not changed
4. tests/results
5. remaining v0.9 gaps

AFTER COMPLETION: Suggest 3-5 next steps.

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 8171516816474149820

Completed at: 2026-09-16T16:05:50Z
