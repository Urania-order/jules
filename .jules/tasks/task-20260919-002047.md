# Jules Task

## Task ID

task-20260919-002047

## Created

2026-09-18T21:20:47Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

> Co-SMOS v1.2.2 — Admin UI fixes: EN error + run-task in Custom + .txt upload
> 
> CONTEXT:
> - Repo: Urania-order/jules
> - v1.2.1 added Queue UI. Verified: /api/admin/queue/run-next (line ~751)
>   already invokes ./scripts/run-task.sh <running-file> [--verify N].
>   Do NOT modify run-next.
> 
> GOAL (3 fixes):
> 
> 1. Frontend error message — English
>    File: frontend/index.html, line 3741
>    BEFORE:
>      showError("Наступне завдання не сформовано");
>    AFTER:
>      showError("No pending task. Please create one first.");
> 
> 2. Add "run-task" to admin whitelist + dropdown
>    a) Backend: smos/api/main.py, ADMIN_COMMAND_WHITELIST (line ~439)
>       Add:
>           "run-task": {
>               "exec": "./scripts/run-task.sh",
>               "fixed_args": [],
>               "timeout": 1200,
>           },
>    b) Frontend: frontend/index.html, admin-command dropdown (line ~1431)
>       Add option:
>           <option value="run-task">run-task (./scripts/run-task.sh &lt;args&gt;)</option>
>    c) Placeholder for admin-args (line ~1445) — update hint:
>           "args (optional)" -> "args: /path/to/file.txt [--verify N]"
> 
> 3. NEW: .txt upload in Custom Command section
>    File: frontend/index.html, Custom Command card (after admin-run button,
>    around line 1448).
>    Add:
>        <div class="form-group" style="margin-top: 0.75rem;">
>          <label for="admin-upload-txt">Or upload .txt file</label>
>          <div style="display: flex; gap: 0.75rem;">
>            <input type="file" id="admin-upload-txt" accept=".txt">
>            <button class="btn btn-secondary" id="btn-admin-upload-run"
>                    onclick="adminUploadTxtRun()">📤 Upload &amp; Run</button>
>          </div>
>        </div>
> 
>    Behavior (new JS function adminUploadTxtRun):
>      - Get file from #admin-upload-txt
>      - Read via FileReader.readAsText
>      - POST /api/admin/queue/add {text: <content>, verify_task: null}
>      - On success: POST /api/admin/queue/run-next
>      - Show output in #admin-output
>      - Refresh queue status via renderAdminQueueStatus()
>      - Clear file input
> 
> REQUIREMENTS:
> - Files: smos/api/main.py, frontend/index.html, tests/test_admin_api.py
> - No shell=True
> - Reuse run-task.sh (do NOT modify)
> - Reuse /api/admin/queue/add + /run-next (do NOT modify)
> - Do NOT modify /api/admin/queue/run-next logic
> - English error messages in UI
> 
> TESTS:
> - test_admin_whitelist_has_run_task
> - frontend contract: admin-upload-txt, btn-admin-upload-run, run-task option
> 
> TESTS:
> - pytest tests/ + git diff --check
> 
> CONSTRAINTS:
> - Do not touch .co-smos/, .jules/tasks/, .jules/results/
> - Do not modify .jules/queue/pending/*.gitkeep
> - Do not weaken existing tests
> 
> REPORT FORMAT: AGENTS.md §14
> 
> FINAL REPORT:
> 1. what already existed and was reused
> 2. what was added
> 3. what was deliberately not changed
> 4. tests/results
> 5. remaining gaps
> TASK_EOF
(smos) (base) Mac-mini:jules vagra$ 
(smos) (base) Mac-mini:jules vagra$ wc -l /tmp/task-ui-fixes.txt
      81 /tmp/task-ui-fixes.txt
(smos) (base) Mac-mini:jules vagra$ head -3 /tmp/task-ui-fixes.txt
Co-SMOS v1.2.2 — Admin UI fixes: EN error + run-task in Custom + .txt upload

CONTEXT:
(smos) (base) Mac-mini:jules vagra$ tail -3 /tmp/task-ui-fixes.txt
3. what was deliberately not changed
4. tests/results
5. remaining gaps
(smos) (base) Mac-mini:jules vagra$ cat /tmp/task-ui-fixes.txt
Co-SMOS v1.2.2 — Admin UI fixes: EN error + run-task in Custom + .txt upload

CONTEXT:
- Repo: Urania-order/jules
- v1.2.1 added Queue UI. Verified: /api/admin/queue/run-next (line ~751)
  already invokes ./scripts/run-task.sh <running-file> [--verify N].
  Do NOT modify run-next.

GOAL (3 fixes):

1. Frontend error message — English
   File: frontend/index.html, line 3741
   BEFORE:
     showError("Наступне завдання не сформовано");
   AFTER:
     showError("No pending task. Please create one first.");

2. Add "run-task" to admin whitelist + dropdown
   a) Backend: smos/api/main.py, ADMIN_COMMAND_WHITELIST (line ~439)
      Add:
          "run-task": {
              "exec": "./scripts/run-task.sh",
              "fixed_args": [],
              "timeout": 1200,
          },
   b) Frontend: frontend/index.html, admin-command dropdown (line ~1431)
      Add option:
          <option value="run-task">run-task (./scripts/run-task.sh &lt;args&gt;)</option>
   c) Placeholder for admin-args (line ~1445) — update hint:
          "args (optional)" -> "args: /path/to/file.txt [--verify N]"

3. NEW: .txt upload in Custom Command section
   File: frontend/index.html, Custom Command card (after admin-run button,
   around line 1448).
   Add:
       <div class="form-group" style="margin-top: 0.75rem;">
         <label for="admin-upload-txt">Or upload .txt file</label>
         <div style="display: flex; gap: 0.75rem;">
           <input type="file" id="admin-upload-txt" accept=".txt">
           <button class="btn btn-secondary" id="btn-admin-upload-run"
                   onclick="adminUploadTxtRun()">📤 Upload &amp; Run</button>
         </div>
       </div>

   Behavior (new JS function adminUploadTxtRun):
     - Get file from #admin-upload-txt
     - Read via FileReader.readAsText
     - POST /api/admin/queue/add {text: <content>, verify_task: null}
     - On success: POST /api/admin/queue/run-next
     - Show output in #admin-output
     - Refresh queue status via renderAdminQueueStatus()
     - Clear file input

REQUIREMENTS:
- Files: smos/api/main.py, frontend/index.html, tests/test_admin_api.py
- No shell=True
- Reuse run-task.sh (do NOT modify)
- Reuse /api/admin/queue/add + /run-next (do NOT modify)
- Do NOT modify /api/admin/queue/run-next logic
- English error messages in UI

TESTS:
- test_admin_whitelist_has_run_task
- frontend contract: admin-upload-txt, btn-admin-upload-run, run-task option

TESTS:
- pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not modify .jules/queue/pending/*.gitkeep
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 11474560666448050738

Completed at: 2026-09-18T21:36:09Z
