# Jules Task

## Task ID

task-20260922-153432

## Created

2026-09-22T15:34:33Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS — TASK 02 — PHENOMENON MODEL

CONTEXT:
- Repo: Urania-order/jules
- TASK 01 (EPISTEMIC FOUNDATION) already applied:
    * smos/models/models.py:69 — EpistemicStatus (enum) with 7 canonical + legacy
    * smos/models/models.py:98 — EpistemicStatus.from_str() (case-insensitive)
    * smos/models/models.py:107 — EpistemicStatus.to_canonical()
    * smos/models/epistemic.py:6 — EpistemicLayer (SQLAlchemy)
    * smos/services/epistemic_service.py — EpistemicService
    * tests/test_epistemic_foundation.py
- Existing primitives (reuse these patterns):
    * smos/models/models.py:181 — Timeline(Base)
    * smos/models/models.py:190 — MemoryNode(Base)
    * smos/models/models.py:231 — Event(Base)
    * smos/models/epistemic.py:16-17 — name/description Column pattern
    * smos/models/ecology.py:54 — "provenance_records" table
    * smos/api/main.py:2269 — GET /provenance/{node_id}
- Ontology task series:
    * TASK 01: Epistemic foundation ✅
    * TASK 02: Phenomenon model ← THIS
    * TASK 03: Context model (deferred)

OBJECTIVE:
Introduce Phenomenon as a first-class domain concept.
Phenomenon must NOT automatically mean fact.

MINIMUM FIELDS:
- id: Integer (primary key)
- name: String (required, index=True)
- description: String (nullable=True)
- epistemic_status: EpistemicStatus (required)
- source: String (nullable=True)
- created_at: DateTime(timezone=True), server_default=func.now()
- updated_at: DateTime(timezone=True), onupdate=func.now()
- provenance: JSON (nullable=True) — where it came from (source refs, evidence ids)

REQUIREMENTS:

1. Reuse existing primitives (do NOT duplicate):
   - EpistemicStatus (smos/models/models.py:69) for epistemic_status
   - Column patterns from smos/models/epistemic.py
   - server_default=func.now() / onupdate=func.now() from existing models
   - Service pattern from smos/services/epistemic_service.py
   - Provenance: JSON column (like domains/members in intellectual_clusters)

2. Phenomenon must NOT automatically mean fact:
   - Default epistemic_status = EpistemicStatus.OBSERVED
   - Service methods do not auto-promote to VERIFIED
   - Documentation: phenomenon ≠ fact
   - Test: test_phenomenon_default_not_fact

3. Files to create:
   a) smos/models/phenomenon.py
      - class Phenomenon(Base):
          __tablename__ = "phenomena"
          id = Column(Integer, primary_key=True, index=True)
          name = Column(String, index=True, nullable=False)
          description = Column(String, nullable=True)
          epistemic_status = Column(Enum(EpistemicStatus), default=EpistemicStatus.OBSERVED, nullable=False)
          source = Column(String, nullable=True)
          created_at = Column(DateTime(timezone=True), server_default=func.now())
          updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
          provenance = Column(JSON, default=dict)

   b) smos/services/phenomenon_service.py
      - class PhenomenonService:
          def create(self, name, description=None, epistemic_status=EpistemicStatus.OBSERVED, source=None, provenance=None) -> Phenomenon
          def get(self, id) -> Optional[Phenomenon]
          def list(self, limit=50, offset=0) -> List[Phenomenon]
          def update(self, id, **fields) -> Optional[Phenomenon]
          def delete(self, id) -> bool
      - Follow pattern from smos/services/epistemic_service.py

   c) smos/models/__init__.py — export Phenomenon
   d) smos/services/__init__.py — export PhenomenonService (if pattern)
   e) smos/core/init_db.py — import phenomenon (for create_all)

4. Tests (tests/test_phenomenon.py):
   - test_create_phenomenon
   - test_serialize_phenomenon (to dict)
   - test_persist_phenomenon (SQLAlchemy insert)
   - test_retrieve_phenomenon (get by id)
   - test_list_phenomena
   - test_update_phenomenon
   - test_delete_phenomenon
   - test_invalid_epistemic_state_rejected (invalid string → ValueError)
   - test_phenomenon_default_not_fact (default = OBSERVED, not VERIFIED)
   - test_phenomenon_provenance_preserved (JSON)
   - test_phenomenon_source_optional (source nullable)

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

TECHNICAL:
- Files:
    * smos/models/phenomenon.py (new)
    * smos/services/phenomenon_service.py (new)
    * smos/models/__init__.py (modify — export)
    * smos/core/init_db.py (modify — import)
    * tests/test_phenomenon.py (new)
- No shell=True
- Reuse EpistemicStatus from smos/models/models.py:69
- Reuse SQLAlchemy Base, Column from smos/core/database.py
- Reuse service pattern from smos/services/epistemic_service.py
- Use Enum(EpistemicStatus) for column type
- created_at/updated_at: server_default=func.now() / onupdate=func.now()

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Phenomenon ≠ fact (default epistemic_status = OBSERVED)
- Preserve backward compatibility with TASK 01
- Do NOT duplicate EpistemicStatus enum
- Reuse Column patterns from existing models

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused (EpistemicStatus, Column patterns, service pattern, provenance)
2. what was added (Phenomenon model, service, tests)
3. what was deliberately not changed (EpistemicStatus, existing models)
4. tests/results
5. remaining gaps

Commit message:
feat: add canonical phenomenon model

## Status

started

