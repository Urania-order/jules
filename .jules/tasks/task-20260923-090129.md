# Jules Task

## Task ID

task-20260923-090129

## Created

2026-09-23T09:01:29Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS — TASK 04 — CONSTRAINT MODEL

CONTEXT:
- Repo: Urania-order/jules
- TASK 01 (EPISTEMIC FOUNDATION) applied:
    * smos/models/models.py:69 — EpistemicStatus (enum)
    * smos/models/models.py:142 — RelationType (enum)
- TASK 02 (PHENOMENON MODEL) applied:
    * smos/models/phenomenon.py — Phenomenon(Base) with to_dict()
    * Pattern: Enum columns, JSON provenance, server_default=func.now()
- TASK 03 (CONTEXT MODEL) applied:
    * smos/models/context.py — Context(Base) + ContextRelation(Base)
    * smos/services/context_service.py — ContextService
- Existing enum patterns:
    * smos/models/cognitive.py:6 — SessionStatus(str, enum.Enum)
    * smos/models/cognitive.py:19 — status = Column(Enum(SessionStatus), default=SessionStatus.ACTIVE)
    * smos/models/goals.py:6 — GoalStatus
    * smos/models/goals.py:12 — TaskStatus
- Existing confidence pattern:
    * smos/models/discovery.py:11,19,29 — confidence = Column(Float, default=0.0)
    * smos/models/experience.py:21,46 — confidence = Column(Float, default=1.0)
- Constraint — NOT yet defined
- Ontology task series:
    * TASK 01: Epistemic foundation ✅
    * TASK 02: Phenomenon model ✅
    * TASK 03: Context model ✅
    * TASK 04: Constraint model ← THIS

OBJECTIVE:
Introduce Constraint as a first-class domain concept.
Constraint must NOT automatically imply causality.
(i.e., a constraint is a limiting condition — not a causal agent.)

TYPES (ConstraintType, str enum):
- PHYSICAL, ECONOMIC, LEGAL, SOCIAL, TECHNICAL, ECOLOGICAL,
  INFORMATIONAL, ORGANIZATIONAL, SECURITY, INFRASTRUCTURAL,
  TEMPORAL, UNKNOWN

STATUS (ConstraintStatus, str enum):
- ACTIVE, RESOLVED, VIOLATED, UNKNOWN

MINIMUM FIELDS:
- id: Integer (primary key)
- name: String (required, index=True)
- description: String (nullable=True)
- type: ConstraintType (required, default UNKNOWN)
- strength: Float (nullable=True, 0.0-1.0)
- status: ConstraintStatus (required, default UNKNOWN)
- evidence: JSON (default list)
- context: String (nullable=True) — free-form context ref (or "context:<id>")
- confidence: Float (default 0.5, 0.0-1.0)
- provenance: JSON (default dict)
- created_at: DateTime(timezone=True), server_default=func.now()
- updated_at: DateTime(timezone=True), server_default=func.now(), onupdate=func.now()

REQUIREMENTS:

1. Reuse existing patterns:
   - Phenomenon pattern (smos/models/phenomenon.py):
       * to_dict() method
       * JSON provenance, server_default=func.now()
   - Context pattern (smos/models/context.py):
       * Enum column pattern
   - SessionStatus pattern (smos/models/cognitive.py:6,19):
       * class ConstraintStatus(str, enum.Enum)
       * status = Column(Enum(ConstraintStatus), default=ConstraintStatus.UNKNOWN)
   - Confidence pattern (smos/models/discovery.py:11):
       * confidence = Column(Float, default=0.5)
   - Service pattern (PhenomenonService, ContextService):
       * from_str() for enums
       * create/get/list/update/delete
       * db.add + db.commit + db.refresh
   - Do NOT duplicate existing enums (EpistemicStatus, RelationType)

2. New enums (in smos/models/constraint.py):
   - class ConstraintType(str, enum.Enum): PHYSICAL, ECONOMIC, LEGAL,
     SOCIAL, TECHNICAL, ECOLOGICAL, INFORMATIONAL, ORGANIZATIONAL,
     SECURITY, INFRASTRUCTURAL, TEMPORAL, UNKNOWN
   - class ConstraintStatus(str, enum.Enum): ACTIVE, RESOLVED, VIOLATED, UNKNOWN

3. Constraint must NOT automatically imply causality:
   - Documentation: constraint ≠ cause
   - Constraint is a limiting condition, not a causal agent
   - Test: test_constraint_not_causality (documentation assertion)
   - Service methods do not create causal relations

4. Files to create:
   a) smos/models/constraint.py
      - class ConstraintType(str, enum.Enum): ... (12 types)
      - class ConstraintStatus(str, enum.Enum): ... (4 statuses)
      - class Constraint(Base):
          __tablename__ = "constraints"
          id = Column(Integer, primary_key=True, index=True)
          name = Column(String, index=True, nullable=False)
          description = Column(String, nullable=True)
          type = Column(Enum(ConstraintType), default=ConstraintType.UNKNOWN, nullable=False)
          strength = Column(Float, nullable=True)  # 0.0-1.0
          status = Column(Enum(ConstraintStatus), default=ConstraintStatus.UNKNOWN, nullable=False)
          evidence = Column(JSON, default=list)
          context = Column(String, nullable=True)  # free-form context ref
          confidence = Column(Float, default=0.5)  # 0.0-1.0
          provenance = Column(JSON, default=dict)
          created_at = Column(DateTime(timezone=True), server_default=func.now())
          updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
          def to_dict(self) -> dict: ...  (follow Phenomenon.to_dict)

   b) smos/services/constraint_service.py
      - class ConstraintService:
          def __init__(self, db: Session): self.db = db
          def create(self, name, description=None, type=ConstraintType.UNKNOWN,
                     strength=None, status=ConstraintStatus.UNKNOWN,
                     evidence=None, context=None, confidence=0.5,
                     provenance=None) -> Constraint
          def get(self, constraint_id) -> Optional[Constraint]
          def list(self, limit=50, offset=0, type=None, status=None) -> List[Constraint]
          def update(self, constraint_id, **fields) -> Optional[Constraint]
          def delete(self, constraint_id) -> bool

   c) smos/models/__init__.py — export Constraint, ConstraintType, ConstraintStatus
   d) smos/services/__init__.py — export ConstraintService (if pattern)
   e) smos/core/init_db.py — import constraint

5. Tests (tests/test_constraint.py):
   - test_create_constraint
   - test_serialize_constraint (to_dict)
   - test_persist_constraint
   - test_retrieve_constraint
   - test_list_constraints
   - test_update_constraint
   - test_delete_constraint
   - test_constraint_type_enum (all 12 types accessible)
   - test_constraint_status_enum (ACTIVE/RESOLVED/VIOLATED/UNKNOWN)
   - test_constraint_type_unknown_default
   - test_constraint_status_unknown_default
   - test_constraint_strength_optional (float, can be null)
   - test_constraint_confidence_range (0.0-1.0)
   - test_constraint_evidence_json (list preserved)
   - test_constraint_provenance_preserved
   - test_constraint_invalid_type_rejected
   - test_constraint_invalid_status_rejected
   - test_constraint_not_causality (documentation assertion)

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

TECHNICAL:
- Files:
    * smos/models/constraint.py (new)
    * smos/services/constraint_service.py (new)
    * smos/models/__init__.py (modify — export)
    * smos/services/__init__.py (modify — export, if pattern)
    * smos/core/init_db.py (modify — import)
    * tests/test_constraint.py (new)
- No shell=True
- Reuse Phenomenon pattern (to_dict, JSON, server_default)
- Reuse SessionStatus pattern (cognitive.py:6,19) for ConstraintStatus
- Reuse confidence pattern (discovery.py:11)
- Reuse service pattern (from_str, db.add/commit/refresh)
- Do NOT duplicate existing enums

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Constraint ≠ cause (documentation)
- Preserve backward compatibility with TASK 01-03
- Do NOT duplicate EpistemicStatus, RelationType

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused (Phenomenon, Context, SessionStatus, confidence patterns)
2. what was added (Constraint, ConstraintType, ConstraintStatus, ConstraintService, tests)
3. what was deliberately not changed
4. tests/results
5. remaining gaps

Commit message:
feat: add constraint domain model

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 6085796840002210955

Completed at: 2026-09-23T09:11:11Z
