# Jules Task

## Task ID

task-20260923-192923

## Created

2026-09-23T19:29:23Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS — TASK 07 — DOMAIN RELATION INTEGRATION

CONTEXT:
- Repo: Urania-order/jules
- Existing ontology TASK 01-06 applied:
    * EpistemicStatus, RelationType (33 types)
    * Phenomenon (phenomenon.py:15)
    * Context (context.py:16) + ContextRelation (context.py:66)
    * Constraint (constraint.py:78)
    * PotentialPhenomenon (potential.py:45)
    * Relation (models.py:228) — MemoryNode-specific
- Existing relation models:
    * Relation (models.py:228): from_node_id FK, to_node_id FK → memory_nodes
    * ContextRelation (context.py:66): Context → Context
    * CausalRelation (experience.py:74): cause/effect MemoryNode
    * ClusterRelation (epistemic.py:32): cluster → cluster
- Services:
    * memory_service.py:92 create_relation (Relation)
    * context_service.py:98 add_relation (ContextRelation)
    * discovery_service.py:17 ClusterRelation
- Ontology series TASK 01-06 applied
- TASK 07: Domain Relation Integration ← THIS

============================================================
STEP 0 — RELATION ARCHITECTURE AUDIT (MANDATORY, DO FIRST)
============================================================

Before creating anything, Jules MUST inspect and document:

1. Where Relation is defined: smos/models/models.py:228
   - FK: from_node_id → memory_nodes.id
   - FK: to_node_id → memory_nodes.id
   - Fields: id, from_node_id, to_node_id, type, provenance, epistemic_status,
             evidence, confidence, observer, context

2. Where ContextRelation is defined: smos/models/context.py:66
   - FK: source_context_id, target_context_id → contexts.id
   - Fields: id, source_context_id, target_context_id, relation_type,
             created_at, provenance

3. Where CausalRelation is defined: smos/models/experience.py:74
   - FK: cause_node_id, effect_node_id → memory_nodes.id
   - Fields: id, cause_node_id, effect_node_id, confidence, evidence

4. Where ClusterRelation is defined: smos/models/epistemic.py:32
   - FK: source_cluster_id, target_cluster_id → intellectual_clusters.id

5. Who uses them:
   - memory_service.py:92 — Relation (MemoryNode)
   - context_service.py:98 — ContextRelation (Context)
   - discovery_service.py:17 — ClusterRelation
   - API endpoints (grep for /relations, /provenance, etc.)

6. Is there a canonical generic relation abstraction?
   - Answer: NO. Each model is type-specific.

7. Can existing Relation be safely generalized?
   - Answer: NO. Relation is coupled to memory_nodes via FK.
   - Changing from_node_id/to_node_id to polymorphic (source_type+source_id)
     would require migration and break existing memory_service usage.

8. Decision (record in FINAL REPORT section 1):
   - Relation remains MemoryNode-specific, UNCHANGED.
   - ContextRelation remains Context-specific, UNCHANGED.
   - CausalRelation remains, UNCHANGED.
   - ClusterRelation remains, UNCHANGED.
   - Create NEW DomainRelation (canonical domain relation layer)
     for cross-domain edges between Phenomenon / Context / Constraint /
     PotentialPhenomenon.
   - Do NOT create parallel relation systems.
   - Do NOT make ContextRelation generic.
   - Do NOT store relations independently as JSON on domain objects.

9. Document this decision BEFORE implementation.

============================================================
DESIGN — DomainRelation (canonical domain relation layer)
============================================================

Create smos/models/domain_relation.py:

    class DomainRelation(Base):
        __tablename__ = "domain_relations"
        id = Column(Integer, primary_key=True, index=True)
        source_type = Column(String, nullable=False)   # "phenomenon"|"context"|"constraint"|"potential"
        source_id = Column(Integer, nullable=False)
        target_type = Column(String, nullable=False)
        target_id = Column(Integer, nullable=False)
        relation_type = Column(Enum(RelationType), nullable=False)
        epistemic_status = Column(Enum(EpistemicStatus), default=EpistemicStatus.OBSERVED, nullable=True)
        provenance = Column(JSON, default=dict)
        evidence = Column(JSON, default=list)
        confidence = Column(Float, nullable=True)
        created_at = Column(DateTime(timezone=True), server_default=func.now())
        updated_at = Column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())

        def to_dict(self) -> dict: ...  (follow Phenomenon.to_dict)

NOTES:
- Polymorphic source/target: source_type + source_id.
- Reuses RelationType (33 types) — single vocabulary.
- Reuses EpistemicStatus — single epistemic vocabulary.
- provenance = JSON (consistent with domain models).
- evidence = JSON (list of evidence refs).
- confidence = Float (0.0-1.0).
- epistemic_status ≠ confidence ≠ provenance ≠ evidence — separate fields
  (consistent with Relation in models.py:228).

PROVENANCE DECISION (from TASK 06):
- DomainRelation.provenance = JSON — canonical domain relation provenance.
- ProvenanceRecord (ecology.py:53) remains unchanged (memory_nodes/ecology).
- No separate provenance engine for domain relations.

============================================================
REQUIRED SCENARIOS (build a single network, not 4 graphs)
============================================================

Create these scenarios (relations between domain objects):

1. Context ENABLES Phenomenon
       source=context, target=phenomenon, type=ENABLES

2. Context BLOCKS Phenomenon
       source=context, target=phenomenon, type=BLOCKS

3. Constraint BLOCKS Potential Phenomenon
       source=constraint, target=potential, type=BLOCKS

4. Phenomenon CHANGES_CONTEXT Context
       source=phenomenon, target=context, type=CHANGES_CONTEXT

5. Phenomenon CREATES_CONTEXT Context
       source=phenomenon, target=context, type=CREATES_CONTEXT

6. Potential Phenomenon DEPENDS_ON Context
       source=potential, target=context, type=DEPENDS_ON

IMPORTANT:
- Do NOT calculate causal truth.
- Only represent documented / inferred / hypothesized relations
  with epistemic status.
- Each relation has provenance, epistemic_status, evidence, confidence.

============================================================
FILES TO CREATE
============================================================

1. smos/models/domain_relation.py
   - class DomainRelation(Base)
   - to_dict()

2. smos/services/domain_relation_service.py
   - class DomainRelationService:
       def __init__(self, db: Session): self.db = db
       def create(self, source_type, source_id, target_type, target_id,
                  relation_type, provenance=None, epistemic_status=None,
                  confidence=None, evidence=None) -> DomainRelation
       def get(self, relation_id) -> Optional[DomainRelation]
       def list(self, source_type=None, target_type=None,
                relation_type=None, limit=50, offset=0) -> List[DomainRelation]
       def update(self, relation_id, **fields) -> Optional[DomainRelation]
       def delete(self, relation_id) -> bool
       def list_for(self, source_type, source_id) -> List[DomainRelation]
       def list_into(self, target_type, target_id) -> List[DomainRelation]
       # Scenario helpers (documented, not causal):
       def context_enables_phenomenon(self, context_id, phenomenon_id, **kw)
       def context_blocks_phenomenon(self, context_id, phenomenon_id, **kw)
       def constraint_blocks_potential(self, constraint_id, potential_id, **kw)
       def phenomenon_changes_context(self, phenomenon_id, context_id, **kw)
       def phenomenon_creates_context(self, phenomenon_id, context_id, **kw)
       def potential_depends_on_context(self, potential_id, context_id, **kw)

3. smos/models/__init__.py — export DomainRelation
4. smos/services/__init__.py — export DomainRelationService (if pattern)
5. smos/core/init_db.py — import domain_relation  ← CRITICAL (ERRATA-0042)

============================================================
TESTS (tests/test_domain_relation_integration.py)
============================================================

- test_create_domain_relation
- test_serialize_domain_relation (to_dict)
- test_persist_domain_relation
- test_retrieve_domain_relation
- test_list_domain_relations
- test_update_domain_relation
- test_delete_domain_relation
- test_list_for_source
- test_list_into_target
- test_scenario_context_enables_phenomenon
- test_scenario_context_blocks_phenomenon
- test_scenario_constraint_blocks_potential
- test_scenario_phenomenon_changes_context
- test_scenario_phenomenon_creates_context
- test_scenario_potential_depends_on_context
- test_full_graph_integration (build all 6 scenarios, verify count)
- test_domain_relation_provenance_json
- test_domain_relation_epistemic_status_default
- test_domain_relation_does_not_calculate_causality (documentation assertion)
- test_domain_relation_reuses_relationtype
- test_domain_relation_reuses_epistemicstatus
- test_relation_unchanged (Relation MemoryNode still works)
- test_context_relation_unchanged (ContextRelation still works)
- test_causal_relation_unchanged (CausalRelation still works)
- test_step0_documented (FINAL REPORT contains audit)

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

============================================================
TECHNICAL
============================================================

- Files:
    * smos/models/domain_relation.py (new)
    * smos/services/domain_relation_service.py (new)
    * smos/models/__init__.py (modify — export)
    * smos/services/__init__.py (modify — export, if pattern)
    * smos/core/init_db.py (modify — import)
    * tests/test_domain_relation_integration.py (new)
- No shell=True
- Reuse RelationType (33 types)
- Reuse EpistemicStatus
- Reuse Phenomenon pattern (to_dict, JSON, server_default)
- provenance = JSON (consistent with domain models)
- epistemic_status ≠ confidence ≠ provenance ≠ evidence (separate fields)
- Do NOT modify Relation, ContextRelation, CausalRelation, ClusterRelation
- Do NOT create new graph engine
- ALL tests must pass locally BEFORE commit (ERRATA-0043)
- init_db.py MUST import domain_relation (ERRATA-0042)

============================================================
CONSTRAINTS
============================================================

- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Do NOT calculate causal truth
- Only represent documented/inferred/hypothesized relations with epistemic status
- Preserve backward compatibility with TASK 01-06
- PROVENANCE RULE: DomainRelation.provenance = JSON (single point)
- Do NOT modify Relation (MemoryNode), ContextRelation (Context),
  CausalRelation (cause), ClusterRelation (cluster) — they remain unchanged
- Do NOT store relations independently as JSON on domain objects
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

============================================================
REPORT FORMAT: AGENTS.md §14
============================================================

FINAL REPORT (expanded):
1. what already existed and was reused
   - MUST include STEP 0 audit results:
     * Relation location + FK (MemoryNode-specific)
     * ContextRelation location + FK (Context-specific)
     * CausalRelation location + FK (MemoryNode cause/effect)
     * ClusterRelation location + FK (cluster-specific)
     * Decision: cannot safely generalize Relation; create DomainRelation
2. what was added (DomainRelation, DomainRelationService, scenario helpers, tests)
3. what was deliberately not changed (Relation, ContextRelation, CausalRelation,
   ClusterRelation, ProvenanceRecord)
4. tests/results
5. remaining gaps

Commit message:
feat: integrate phenomenon context constraint relations

## Status

started

