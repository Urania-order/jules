# Jules Task

## Task ID

task-20260914-165322

## Created

2026-09-14T13:53:22Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Розроби Co-SMOS Control Room v0.9 — Integration Review.

КОНТЕКСТ:
- Working-репозиторій Urania-order/jules
- 15 shell-скриптів
- FastAPI: smos/api/main.py (порт 8080)
- Черга: .jules/queue/{pending,running,completed,proposed,deferred}/
- 90+ тестів, CI
- AGENTS.md має 25 секцій

МЕТА:
Перетворити CLI-оркестратор на WEB CONTROL ROOM.
Frontend НЕ виконує shell напряму.
CLI scripts НЕ ламати.

PHASE 1 — INSPECT:
Проаналізуй smos/, scripts/, .jules/queue/, .co-smos/
Запиши в .jules/history/adr-0007-control-room-inspect.md

PHASE 2 — DESIGN:
Integration map: existing → new → integration point.

PHASE 3 — CORE (smos/core/):
task.py, queue.py, proposals.py, events.py, state.py
Статуси: PENDING, READY, RUNNING, REVIEW, ACCEPTED, COMPLETED, BLOCKED, FAILED, CANCELLED, DEFERRED

PHASE 4 — ADAPTER (smos/adapters/jules_cli.py):
JulesCLIAdapter: add_task, run_queue, list_proposals, accept_proposal, defer_proposal, reject_proposal
subprocess.run([...]) БЕЗ shell=True

PHASE 5 — API (smos/api/main.py):
GET /api/system/status, /api/queue, POST /api/queue, POST /api/queue/run
GET /api/tasks, GET /api/tasks/{id}, PATCH /api/tasks/{id}
POST /api/tasks/{id}/start, /cancel
GET /api/proposals
POST /api/proposals/{id}/accept, /modify, /defer, /reject
GET /api/events, GET /api/tasks/{id}/history
Error: {status: error, code, message, exit_code}

PHASE 6 — FRONTEND:
frontend/index.html (vanilla HTML+CSS+JS)
Views: Control Room, Queue, Task Detail, Proposals, Events, History
Auto-refresh, CSS variables, responsive, empty/error states
Форма New Task без prompt()
Drag-and-drop для reorder (тільки READY/PENDING)

PHASE 7 — INTEGRATION:
UI → API → Core → Adapter → CLI

PHASE 8 — REGRESSION:
Старі CLI працюють.

PHASE 9 — DOCUMENTATION:
README.md. scripts/co-smos.sh — запуск.

ТЕСТИ:
tests/test_control_room_api.py, tests/test_cli_adapter.py, tests/test_task_model.py

ЗАБОРОНЕНО:
- Видаляти shell scripts
- shell=True з user input
- Frontend напряму shell
- React/Vue/Svelte
- Дублювати компоненти

ACCEPTANCE CRITERIA:
- Existing CLI працюють
- Control Room відкривається
- Task створюється, запускається, скасовується
- Proposals (Accept/Modify/Defer/Reject)
- Events, History
- 90+ існуючих тестів проходять
- Нові тести проходять

НЕ чіпати .co-smos/ і .jules/tasks/
Звітувати за форматом AGENTS.md §14
ПІСЛЯ ЗАВЕРШЕННЯ: запропонуй 3-5 наступних кроків

## Status

started

