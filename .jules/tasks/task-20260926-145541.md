# Jules Task

## Task ID

task-20260926-145541

## Created

2026-09-26T14:55:41Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.10 — TASK 19 — PREDICTION LIFECYCLE API

CONTEXT:
- Repo: Urania-order/jules
- Existing ontology TASK 01-18 applied.
- Existing Prediction model + PredictionService (TASK 13):
    smos/models/prediction.py
    smos/services/prediction_service.py:
        28: create_prediction()
        81: attach_outcome(prediction_id, actual_outcome)
        95: evaluate(prediction_id, evaluation)
- Existing API (TASK 15):
    GET    /api/predictions
    POST   /api/predictions
    GET    /api/predictions/{id}
    PUT    /api/predictions/{id}
    — NO DELETE (historical record) —
- TASK 17: graph API

============================================================
STEP 0 — AUDIT (COMPLETED)
============================================================

Findings:
1. PredictionService.attach_outcome(prediction_id, actual_outcome) exists
   (smos/services/prediction_service.py:81)
2. PredictionService.evaluate(prediction_id, evaluation) exists
   (smos/services/prediction_service.py:95)
3. Existing API /api/predictions/* has CRUD only (TASK 15).
4. NO lifecycle endpoints exist yet.
5. Model Prediction lifecycle rules (TASK 13):
    - attach_outcome MUST NOT change epistemic_status
    - evaluate MUST NOT change epistemic_status
    - Prediction MUST remain persistent after evaluation
    - Prediction MUST NOT be auto-promoted to fact

DECISION:
   - Add TWO lifecycle endpoints:
       POST /api/predictions/{id}/outcome
       POST /api/predictions/{id}/evaluate
   - Do NOT add DELETE (TASK 15 rule preserved)
   - Do NOT add PUT that changes outcome/evaluation (PUT = metadata only)
   - Do NOT auto-promote epistemic_status
   - Follow existing conventions (@app.<method>, inline Pydantic,
     dict response, _error_response, require_operator for writes)

============================================================
OBJECTIVE
============================================================

Expose the Prediction lifecycle via API.

POST /api/predictions/{id}/outcome
    body: { actual_outcome: {...} }
    calls PredictionService.attach_outcome()
    returns: updated Prediction.to_dict()
    CRITICAL: MUST NOT change epistemic_status

POST /api/predictions/{id}/evaluate
    body: { evaluation: {...} }
    calls PredictionService.evaluate()
    returns: updated Prediction.to_dict()
    CRITICAL: MUST NOT change epistemic_status

Both endpoints:
   - require_operator (write)
   - 404 if prediction not found
   - 422 on invalid body (Pydantic validation)
   - use existing _error_response for errors

============================================================
FILES TO MODIFY
============================================================

smos/api/main.py:
   - Add Pydantic schemas:
       class PredictionOutcomeRequest(BaseModel):
           actual_outcome: Dict[str, Any]
       class PredictionEvaluateRequest(BaseModel):
           evaluation: Dict[str, Any]
   - Add 2 endpoints:
       @app.post("/api/predictions/{id}/outcome",
                 dependencies=[Depends(require_operator)])
       @app.post("/api/predictions/{id}/evaluate",
                 dependencies=[Depends(require_operator)])

FILES TO CREATE
============================================================

tests/test_api_prediction_lifecycle.py (new)

============================================================
TESTS (tests/test_api_prediction_lifecycle.py)
============================================================

Use: TestClient(app) pattern with auth headers.

- test_attach_outcome_endpoint_returns_200
- test_attach_outcome_persists
- test_attach_outcome_does_not_change_epistemic_status   ← CRITICAL
- test_attach_outcome_404_for_unknown_prediction
- test_attach_outcome_422_on_invalid_body
- test_attach_outcome_requires_operator
- test_evaluate_endpoint_returns_200
- test_evaluate_persists
- test_evaluate_does_not_change_epistemic_status         ← CRITICAL
- test_evaluate_404_for_unknown_prediction
- test_evaluate_422_on_invalid_body
- test_evaluate_requires_operator
- test_prediction_no_auto_promotion_full_flow            ← CRITICAL
    (create → attach_outcome → evaluate → status still PREDICTED)
- test_existing_crud_endpoints_unchanged
- test_delete_still_not_available_for_predictions

REQUIRED FIXTURE:
- Create a Prediction via API
- POST /outcome, verify persisted + status PREDICTED
- POST /evaluate, verify persisted + status PREDICTED
- Re-fetch via GET, verify status PREDICTED

LIMIT: ≤1000 changed code lines.

TECHNICAL:
- Reuse existing FastAPI app (smos/api/main.py)
- Follow existing conventions (@app.<method>, inline Pydantic)
- Reuse PredictionService.attach_outcome / evaluate
- CRITICAL: endpoints MUST NOT modify epistemic_status
- Do NOT modify Prediction model or PredictionService
- Do NOT add DELETE
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- NO auto-promotion of epistemic_status
- Preserve backward compatibility with TASK 01-18
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused (PredictionService methods)
2. what was added (2 endpoints + 2 schemas + tests)
3. what was deliberately not changed (Prediction model, PredictionService)
4. tests/results (including no-auto-promotion assertions)
5. remaining gaps

Commit message:
feat: expose prediction lifecycle endpoints

## Status

started

