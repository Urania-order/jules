# Jules Task

## Task ID

task-20260912-165918

## Created

2026-09-12T16:59:18+00:00

## Repository

https://github.com/Urania-order/jules

## Branch

main

## Request

Реалізуй EmbeddingService у smos/services/embedding_service.py.

Зараз це заглушка — використовує numpy.random з seed від хешу тексту.

Завдання:
1. Реалізувати get_embedding(text: str) -> List[float] (1536 dims)
2. Якщо OPENAI_API_KEY встановлено — використати OpenAI embeddings (text-embedding-3-small)
3. Якщо ні — детермінований fallback на основі хешу тексту (як зараз, але з коментарями та edge cases)
4. Додати get_embeddings_batch(texts: List[str]) -> List[List[float]]
5. Написати tests/test_embedding_service.py з тестами:
   - get_embedding повертає 1536 dims
   - get_embedding детермінований для однакового тексту
   - різні тексти дають різні вектори
   - batch повертає правильну кількість векторів
   - fallback працює без OPENAI_API_KEY
6. Запустити ./scripts/validate.sh і uv run pytest tests/ -v

НЕ чіпати .co-smos/ і .jules/
Звітувати за форматом AGENTS.md §14 (STATUS, CHANGES, TESTS, RISKS, NEXT STEP)

## Status

started

