# Jules Task

## Task ID

task-20260923-184214

## Created

2026-09-23T18:42:14Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS — TASK 06 — RELATION TYPES (extend existing + PROVENANCE DECISION)

CONTEXT:
- Repo: Urania-order/jules
- Existing relation infrastructure:
    * smos/models/models.py:142 — RelationType (str, enum.Enum), 20 types
    * smos/models/models.py:212 — Relation(Base): {id, from_node_id, to_node_id, type}
    * smos/models/context.py:66 — ContextRelation(Base): has provenance JSON
    * smos/models/experience.py:74 — CausalRelation(Base): has confidence, evidence
    * smos/models/epistemic.py:32 — ClusterRelation(Base)
- Existing provenance:
    * smos/models/ecology.py:53 — ProvenanceRecord(Base) — canonical for memory_nodes
        Fields: id, node_id (FK memory_nodes), created_by_id, contributors,
                evidence_links, revision_history, created_at
    * phenomenon.py:42, context.py:47, constraint.py:105, potential.py:68 —
        JSON provenance column (consistent domain-model pattern)
- Ontology task series TASK 01-05 applied
- TASK 06: Relation Types ← THIS

============================================================
STEP 0 — PROVENANCE ARCHITECTURE REPORT (MANDATORY, DO FIRST)
============================================================

Before ANY modification, Jules MUST inspect and document:

1. Existing canonical ProvenanceRecord:
   - Location: smos/models/ecology.py:53
   - Confirm: it is coupled to memory_nodes (node_id FK).
   - Confirm: it is NOT polymorphic (no subject_id/subject_type).

2. Existing domain-model provenance patterns:
   - smos/models/phenomenon.py:42 — provenance = Column(JSON, default=dict)
   - smos/models/context.py:47,79 — provenance = Column(JSON, default=dict)
   - smos/models/constraint.py:105 — provenance = Column(JSON, default=dict)
   - smos/models/potential.py:68 — provenance = Column(JSON, default=dict)

3. Confirm that these four models use JSON provenance with a consistent
   principle: provenance is data about origin/basis of the domain entity.
   Keys may differ, but the PRINCIPLE must be the same.

4. Confirm: Relation currently has NO provenance.

5. Decision (record in FINAL REPORT section 1):
   - ProvenanceRecord remains UNCHANGED.
   - It remains canonical for its existing memory_nodes/ecology scope.
   - Relation receives: provenance = Column(JSON, default=dict)
     — following the SAME domain-model pattern already used by
     Phenomenon / Context / Constraint / Potential.
   - ContextRelation and CausalRelation MUST NOT introduce separate
     provenance fields or mechanisms.

6. Do NOT:
   - migrate ProvenanceRecord to polymorphic subject_id/subject_type;
   - add Relation → ProvenanceRecord FK;
   - create a second provenance engine;
   - duplicate provenance in Relation subclasses.

7. Document this decision in the FINAL REPORT BEFORE implementation.

============================================================
PROVENANCE DECISION (OPTION B — FINAL)
============================================================

- ProvenanceRecord remains unchanged, canonical for memory_nodes/ecology.
- Relation.provenance = JSON, following the same pattern as
  Phenomenon/Context/Constraint/Potential.
- ContextRelation / CausalRelation do NOT get their own provenance.
- NO Relation → ProvenanceRecord FK.
- NO polymorphic ProvenanceRecord.
- NO second provenance engine.

Rationale (architectural consistency):
- Phenomenon.provenance = JSON
- Context.provenance = JSON
- Constraint.provenance = JSON
- Potential.provenance = JSON
- Relation.provenance = JSON  ← extends existing pattern, not new

Exact rule:
    Relation.provenance is the canonical provenance representation
    for domain relations.
(ProvenanceRecord remains canonical for memory_nodes/ecology;
 it has a different area of responsibility, not replaced.)

============================================================
RELATIONS TO ADD
============================================================

Already present (do NOT duplicate):
- DEPENDS_ON ✅
- CONTRADICTS ✅

New (13 to add):
- SUPPORTS
- BLOCKS
- ENABLES
- AMPLIFIES
- SUPPRESSES
- TRANSFORMS
- CREATES_CONTEXT
- CHANGES_CONTEXT
- EMERGES_FROM
- RESONATES_WITH
- CORRELATES_WITH
- REQUIRES
- PREVENTS

Total after: 20 + 13 = 33 relation types.

============================================================
PROVENANCE FIELDS ON Relation (single point for domain relations)
============================================================

Add to Relation model (smos/models/models.py:212):
- provenance = Column(JSON, default=dict)   # {source_case, observer, context_ids, timestamp}
- epistemic_status = Column(Enum(EpistemicStatus), nullable=True)
- evidence = Column(JSON, default=list)      # evidence refs
- confidence = Column(Float, nullable=True)  # 0.0-1.0

Optional (if backward-compatible):
- observer = Column(String, nullable=True)
- context = Column(String, nullable=True)

All new fields MUST be nullable or have default → backward compatibility.

ContextRelation / CausalRelation:
- Do NOT add own provenance field.
- Inherit provenance contract from Relation (documentation only).
- If they currently have provenance JSON (ContextRelation:79) — keep it
  (backward compat) but document that it should reference Relation.provenance
  semantics; do NOT add new provenance mechanisms.

Do NOT touch:
- ProvenanceRecord (ecology.py:53)
- phenomenon/context/constraint/potential provenance JSON

============================================================
TESTS
============================================================

- test_relation_type_supports
- test_relation_type_blocks
- test_relation_type_enables
- test_relation_type_correlates_with
- test_relation_type_contradicts
- test_relation_type_requires
- test_relation_type_prevents
- test_new_relations_count (33 total)
- test_no_duplicate_relations
- test_relation_provenance_field_exists (JSON)
- test_relation_provenance_default_empty_dict
- test_relation_epistemic_status_nullable
- test_relation_evidence_field
- test_relation_confidence_field
- test_context_relation_has_no_new_provenance_field
- test_causal_relation_has_no_new_provenance_field
- test_existing_relation_usage_still_works (backward compat)
- test_provenance_record_unchanged (verify ProvenanceRecord fields same)
- test_step0_documented (FINAL REPORT section 1 contains inspection)

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

============================================================
TECHNICAL
============================================================

- Files:
    * smos/models/models.py (extend RelationType + Relation)
    * tests/test_relation_types.py (new)
- No shell=True
- Reuse existing RelationType enum pattern
- Do NOT duplicate existing relation types
- Do NOT create new graph
- Do NOT implement provenance separately in ContextRelation/CausalRelation
- Do NOT touch ProvenanceRecord (ecology.py)
- Relation.provenance = JSON (single domain-relation provenance point)
- Backward compatibility: existing Relation usage must still work
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

============================================================
CONSTRAINTS
============================================================

- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Extend existing infrastructure — NO new graph
- Preserve backward compatibility with TASK 01-05
- PROVENANCE DECISION: Relation.provenance = JSON (Option B)
- ProvenanceRecord remains unchanged
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

============================================================
REPORT FORMAT: AGENTS.md §14
============================================================

FINAL REPORT (expanded):
1. what already existed and was reused
   - MUST include STEP 0 inspection results
   - ProvenanceRecord location + scope (memory_nodes-specific)
   - Domain-model provenance pattern (JSON)
   - Decision recorded (Option B)
2. what was added (13 new relation types + provenance fields)
3. what was deliberately not changed (ProvenanceRecord, domain model provenance)
4. tests/results
5. remaining gaps

Commit message:
feat: extend canonical relation vocabulary

## Status

started

