# Jules Task

## Task ID

task-20260919-025227

## Created

2026-09-18T23:52:27Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.2.4 — System Overview (Recent Tasks) sort + Stages comment fix

CONTEXT:
- Repo: Urania-order/jules
- frontend/index.html
- v1.1.7 added sortTasksByCreatedAt (line ~1972)
- v1.2.3 applied it to Stages (line ~2260)
- Missing: System Overview — function renderRecentTasks() (line ~1925)

ISSUE 1 — Recent Tasks not sorted
- Function: renderRecentTasks() at line ~1925
- Container: id="recent-tasks-list" (line 948)
- Code (line ~1933):
    currentTasks.slice(0, 5).forEach(task => {
      container.appendChild(createTaskCard(task));
    });
- Takes FIRST 5 in raw array order (not newest 5)
- Fix: sort DESC by created_at, then slice top 5

ISSUE 2 — Stages comment misleading
- Line ~2259: "// Sort tasks oldest first"
- Code: sortTasksByCreatedAt(currentTasks, true) → DESC (newest first)
- Fix comment: "// Sort tasks newest first"

GOAL:

1. Fix renderRecentTasks() (line ~1925):
   BEFORE:
     currentTasks.slice(0, 5).forEach(task => {
       container.appendChild(createTaskCard(task));
     });
   AFTER:
     const sortedTasks = sortTasksByCreatedAt(currentTasks, true);
     sortedTasks.slice(0, 5).forEach(task => {
       container.appendChild(createTaskCard(task));
     });
   Reuse existing helper (line ~1972) — do NOT duplicate.

2. Fix Stages comment (line ~2259):
   BEFORE: // Sort tasks oldest first
   AFTER:  // Sort tasks newest first

3. Add frontend contract test (tests/test_frontend_contract.py):
   - test_frontend_contract_recent_tasks_sorted
     * assert that renderRecentTasks calls sortTasksByCreatedAt
     * (regex: /renderRecentTasks[\s\S]{0,500}sortTasksByCreatedAt/)

REQUIREMENTS:
- Only frontend/index.html + tests/test_frontend_contract.py
- Reuse sortTasksByCreatedAt (do NOT duplicate)
- Do not break other views

TESTS:
- pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started

