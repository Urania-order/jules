# Jules Task

## Task ID

task-20260925-190114

## Created

2026-09-25T19:01:14Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.10 — TASK 17 — GRAPH / UI PROJECTION

CONTEXT:
- Repo: Urania-order/jules
- Existing ontology TASK 01-16 applied.
- Existing graph endpoint at smos/api/main.py:2039:
    @app.get("/api/graph")
    def get_task_graph():
        # task nodes only; edges = []
        return {"nodes": [...], "edges": []}
- Existing frontend at frontend/index.html (4000 lines, monolithic).
- Frontend nav views (data-view):
    dashboard, command-intake, reset-center, stages, sequence, cycles,
    queue, batch, sequences, templates, proposals, events, history,
    consult, admin
- NO frontend graph rendering currently exists (no D3/Cytoscape/SVG).
- NO view for ontology.
- Existing TASK 15 API:
    /api/phenomena, /api/contexts, /api/potential-phenomena,
    /api/constraints, /api/domain-relations, /api/predictions

============================================================
STEP 0 — GRAPH / UI AUDIT (COMPLETED)
============================================================

Findings:
1. /api/graph exists but only returns task nodes with empty edges.
2. Frontend is monolithic index.html with a simple nav pattern
   (data-view + <section id="view-...">).
3. No graph visualization library currently included.
4. No ontology view.

DECISION:
   - Extend /api/graph to return domain entities + relations (backward compatible).
   - Add a new frontend view "Ontology" (view-ontology) using simple
     SVG or Canvas (NO new external library unless already available).
   - Do NOT create a second frontend app.
   - Do NOT create a second graph engine.

============================================================
OBJECTIVE
============================================================

Extend existing graph UI.

Visually distinguish:
    Phenomenon
    Context
    Potential
    Blocked
    Constraint
    Prediction
    Resonance

Use existing frontend architecture.
Do not create a new frontend application.

Required navigation (UI flow):
    Phenomenon -> Context -> Constraints -> Potential Phenomenon ->
    Prediction -> Outcome

============================================================
DESIGN — Extend /api/graph + add view-ontology
============================================================

PART A — Backend: extend /api/graph

Modify smos/api/main.py:2039 get_task_graph():

BEFORE:
    return {"nodes": [...task nodes...], "edges": []}

AFTER (backward compatible):
    return {
        "nodes": [
            # task nodes (existing, type="task")
            {"id": "task:<id>", "type": "task", "status": ..., ...},
            # domain nodes (new)
            {"id": "phenomenon:<id>", "type": "phenomenon",
             "label": name, "status": epistemic_status},
            {"id": "context:<id>", "type": "context", "label": name},
            {"id": "constraint:<id>", "type": "constraint",
             "label": name, "status": ...},
            {"id": "potential:<id>", "type": "potential",
             "label": phenomenon, "status": ...},
            {"id": "prediction:<id>", "type": "prediction",
             "label": expected_state summary, "status": epistemic_status},
        ],
        "edges": [
            # from DomainRelation
            {"source": "constraint:3", "target": "phenomenon:1",
             "type": "BLOCKS", "confidence": 0.8},
            {"source": "phenomenon:1", "target": "context:5",
             "type": "CHANGES_CONTEXT"},
            # from Prediction source_hypothesis
            {"source": "prediction:7", "target": "phenomenon:1",
             "type": "PREDICTS"},
            # from PotentialPhenomenon (if linked by name)
            # ... (optional, only if trivially derivable)
        ]
    }

Node ID format: "<entity_type>:<entity_id>" — required for uniqueness.

Backward compat:
   - Existing task nodes keep id = task id (or convert to "task:<id>"
     — document choice).
   - New frontend should handle both.

Note: Prediction source_hypothesis is polymorphic (type + id);
      edge source is "prediction:<id>", target is "<type>:<id>".

PART B — Frontend: view-ontology

Add to frontend/index.html:

1. New nav button:
    <button class="nav-btn" data-view="ontology">🧠 Ontology</button>

2. New view section:
    <section id="view-ontology" class="view">
        <div id="ontology-graph-container">
            <svg id="ontology-graph-svg"></svg>
        </div>
        <div id="ontology-detail-panel"></div>
        <div id="ontology-filters">
            <!-- checkboxes for: phenomenon, context, constraint,
                 potential, prediction, resonance -->
        </div>
    </section>

3. Simple SVG renderer (NO external library):
    - Load /api/graph on view activate
    - Filter nodes by type
    - Simple force-less layout: circle packing by type, or
      grid by type, or radial layout
    - Render nodes as circles with type-based color
    - Render edges as lines
    - Click node → show details in side panel
    - Hover node → tooltip

Color scheme (suggested):
    phenomenon  — blue
    context     — green
    constraint  — red
    potential   — yellow
    prediction  — purple
    blocked     — dark red (overlay)
    resonance   — orange (overlay)

Navigation flow in UI:
   Click on phenomenon → side panel shows:
     - incoming/outgoing relations
     - contexts
     - constraints
     - potentials referencing it
     - predictions with source_hypothesis == this phenomenon
   This gives the Phenomenon → Context → Constraints → Potential →
   Prediction navigation.

FILES TO MODIFY:
1. smos/api/main.py (extend /api/graph)
2. frontend/index.html (add view-ontology, nav button, renderer)

FILES TO CREATE:
3. tests/test_graph_api.py (new — backend only)
4. (optional) tests/test_frontend_contract.py — extend with
   view-ontology / ontology-graph-svg / data-view="ontology"

============================================================
TESTS
============================================================

Backend (tests/test_graph_api.py):
- test_graph_returns_nodes_and_edges
- test_graph_includes_task_nodes (backward compat)
- test_graph_includes_phenomenon_nodes
- test_graph_includes_context_nodes
- test_graph_includes_constraint_nodes
- test_graph_includes_potential_nodes
- test_graph_includes_prediction_nodes
- test_graph_edges_from_domain_relations
- test_graph_edges_from_prediction_source
- test_graph_node_id_format (entity_type:entity_id)
- test_graph_empty_db
- test_graph_read_only

Frontend contract (tests/test_frontend_contract.py — extend):
- test_nav_has_ontology_button
- test_view_ontology_exists
- test_ontology_graph_svg_exists
- test_ontology_filters_exist

LIMIT: ≤1000 changed code lines.

TECHNICAL:
- Reuse existing frontend (index.html)
- Reuse existing nav pattern (data-view + section#view-*)
- Reuse existing /api/graph endpoint (extend)
- NO external graph library (use SVG / Canvas directly)
- Do NOT create second frontend app
- Do NOT create second graph engine
- Backward compat: task nodes preserved
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- Existing /api/graph consumers must still work (backward compat)
- Read-only graph
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added (graph extension, view-ontology, renderer, tests)
3. what was deliberately not changed (existing views, task graph)
4. tests/results
5. remaining gaps

Commit message:
feat: visualize context phenomenon emergence graph

## Status

started

