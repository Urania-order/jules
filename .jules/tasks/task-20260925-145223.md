# Jules Task

## Task ID

task-20260925-145223

## Created

2026-09-25T14:52:23Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.10 — TASK 15 — API INTEGRATION

PURPOSE:
Expose the already implemented Co-SMOS v0.9/v0.10 domain capabilities
through the existing API. This task is API integration only.

DO NOT redesign ontology.
DO NOT modify TASK 01-14 domain infrastructure.
DO NOT create parallel architecture.

The API is an integration boundary:
    HTTP
    -> request validation
    -> existing service
    -> canonical model
    -> existing serialization

No business logic belongs in routes.

============================================================
STEP 0 — VERIFIED API ARCHITECTURE (COMPLETED)
============================================================

Repository inspection has established:

1. Existing FastAPI application:
   - smos/api/main.py (2351 lines)
   - app = FastAPI(title="Co-SMOS Control Room API", version="0.9",
                   lifespan=lifespan) at line 109

2. main.py is monolithic.
   - NO APIRouter
   - NO include_router
   - NO second FastAPI() instance anywhere

3. Existing prefix convention: "/api/..."
   - /api/tasks, /api/proposals, /api/sequences, /api/batch, /api/events
   - NO versioning (/api/v1 absent)

4. Pydantic request models are inline in main.py (line 328+):
   EventCreate, CreateTaskRequest, CreateProposalRequest,
   RunQueueRequest, QueueResetRequest, ...
   - NO separate schemas.py

5. Existing responses are dict-based / to_dict().
   - NO response_model= usage in existing endpoints.

6. Existing errors use _error_response(code, message, status_code):
   {"error": {"code": ..., "message": ...}}

7. Existing write routes use Depends(require_operator).
   Read routes sometimes public (GET /api/tasks, GET /api/proposals).

8. Existing API tests use:
   - TestClient(app)
   - tests/test_api_basic.py, tests/test_admin_api.py

9. Existing domain endpoints for TASK 01-14 resources:
   - ZERO endpoints for: Phenomena, Contexts, Constraints,
     PotentialPhenomena, DomainRelations, Predictions.
   - ONE legacy endpoint @app.post("/resonance") at main.py:2191
     for the legacy knowledge-level ResonanceService.
     This legacy endpoint MUST remain unchanged.

DECISION:
   - USE existing FastAPI app in smos/api/main.py.
   - Follow existing conventions (@app.<method>, inline Pydantic,
     dict responses, _error_response, require_operator for writes).
   - Do NOT create: second FastAPI app, APIRouter, second API package,
     second schema layer, second service layer.
   - Do NOT refactor unrelated API code.

============================================================
STEP 1 — CANONICAL API RESOURCES
============================================================

Expose ONLY the following missing canonical persistent resources:

    /api/phenomena
    /api/contexts
    /api/potential-phenomena
    /api/constraints
    /api/domain-relations
    /api/predictions

CRITICAL: Do NOT create both /api/relations and /api/domain-relations.
DomainRelation is the canonical domain relation layer.
The only endpoint prefix is /api/domain-relations.

============================================================
STEP 2 — CRUD CONTRACT
============================================================

For: Phenomenon, Context, PotentialPhenomenon, Constraint,
     DomainRelation

Provide:

    GET    /api/<resource>
    POST   /api/<resource>
    GET    /api/<resource>/{id}
    PUT    /api/<resource>/{id}
    DELETE /api/<resource>/{id}

For every endpoint:
   - validate input (Pydantic request model)
   - call existing canonical service/model layer
   - return serialized canonical data via to_dict()
   - use _error_response for errors
   - do NOT implement domain logic inside route handlers

Use actual canonical model/service APIs.
Do NOT invent fields that are not supported by the models.

============================================================
STEP 3 — PREDICTION API (SPECIAL CASE)
============================================================

Prediction is a first-class persistent model (TASK 13).

Expose ONLY:

    POST /api/predictions          (create; PredictionService.create_prediction)
    GET  /api/predictions          (list)
    GET  /api/predictions/{id}     (get)
    PUT  /api/predictions/{id}     (update permitted metadata only)

CRITICAL RULES:

1. Prediction creation MUST use existing PredictionService.create_prediction.

2. The API MUST NOT create a second forecast engine.

3. Reuse: EpistemicStatus, PotentialPhenomenon, existing PredictionService.

4. Prediction status rule:
   - Creation sets epistemic_status = PREDICTED (via model default).
   - Do NOT invent a new status.

5. PUT rule:
   PUT MAY modify ONLY fields that the existing PredictionService/model
   defines as mutable metadata.
   PUT MUST NOT:
     - evaluate the prediction
     - attach an outcome unless an explicit existing service method
       supports it
     - automatically change epistemic_status
     - promote Prediction to Fact
     - promote Prediction to OBSERVED
     - promote Prediction to REFUTED

6. Lifecycle separation:
   Prediction lifecycle is:
       create
         |
         v
       attach_outcome
         |
         v
       evaluate
   This TASK exposes ONLY operations that already exist in the
   canonical PredictionService.

   DO NOT create:
       /api/predictions/{id}/evaluate
       /api/predictions/{id}/outcome
   unless an explicit canonical service method already exists AND is
   explicitly requested in this task.

   This task DOES NOT request those endpoints. So: do NOT add them.

7. DELETE for Prediction:
   DO NOT add DELETE /api/predictions/{id}.
   Prediction is a persistent historical record of an expectation.
   Its lifecycle MUST NOT be implemented as generic CRUD deletion.
   If deletion is required later, it must be a separate governance
   decision.

============================================================
STEP 4 — PYDANTIC REQUEST SCHEMAS (INLINE)
============================================================

For NEW endpoints, use inline Pydantic REQUEST models in
smos/api/main.py (following existing convention):

    PhenomenonCreate
    PhenomenonUpdate

    ContextCreate
    ContextUpdate

    ConstraintCreate
    ConstraintUpdate

    PotentialPhenomenonCreate
    PotentialPhenomenonUpdate

    DomainRelationCreate
    DomainRelationUpdate

    PredictionCreate
    PredictionUpdate

Follow existing repository conventions (inline, same style).

Do NOT:
   - introduce a new schemas.py
   - migrate existing endpoints to Pydantic
   - introduce response_model for the new API merely for style

============================================================
STEP 5 — RESPONSE SERIALIZATION
============================================================

Follow existing response convention: dict / model.to_dict().

Do NOT introduce response_model=.
Do NOT rewrite existing response serialization.

If an existing canonical service provides a response object
(e.g., BlockageAnalysis, EmergenceAnalysis, ResonanceCandidate),
serialize via to_dict() consistently with the repository.

============================================================
STEP 6 — AUTHORIZATION
============================================================

Write operations (POST, PUT, DELETE) MUST use:
    dependencies=[Depends(require_operator)]

Read operations follow existing convention.

CRITICAL — hidden_context_ids:
   `hidden_context_ids` MUST NOT be exposed merely because the caller
   can read the parent ContextExposure-like object.

   hidden_context_ids is a VISIBILITY STATE per projection/run
   (see TASK 09 audit), NOT a property of Context.

   If hidden-context data is ever exposed by a future endpoint, it
   MUST pass through the existing authorization/governance model.

   This TASK does NOT expose /api/exposures and therefore does NOT
   expose hidden_context_ids at all.

============================================================
STEP 7 — ANALYSIS ENDPOINTS
============================================================

Expose only analysis services that ALREADY exist.

## Resonance

    POST /api/resonance/detect

Request body (optional):
    min_agents: int
    max_context_overlap: float
    min_confidence: float
    limit: int

Must call:
    ConvergentResonanceService.detect(...)

CRITICAL: This endpoint MUST NOT modify the existing legacy:
    POST /resonance  (main.py:2191)
The legacy endpoint belongs to the existing knowledge-level
ResonanceService (stub) and remains UNCHANGED.

Do NOT rename, replace, or wrap the legacy /resonance endpoint.

## Blockage

    POST /api/blockage/analyze/{phenomenon_id}

Optional request:
    indirect_max_hops: int

Must call:
    BlockageAnalysisService.analyze_blockage(...)

## Emergence

    POST /api/emergence/analyze/{phenomenon_id}

Must call:
    EmergenceAnalysisService.analyze_emergence(...)

Analysis routes MUST contain no duplicated analysis logic.

============================================================
STEP 8 — EXCLUDED FROM THIS TASK
============================================================

Do NOT implement:

    /api/domain-events
    /api/events/reconstruct/...
    /api/perspectives/phenomenon/{id}
    /api/exposures
    /api/recipes
    /api/analysis/four-positions

These belong to later dedicated integration tasks.

Do NOT create placeholder endpoints for future functionality.

============================================================
STEP 9 — TESTS (tests/test_api_domain.py)
============================================================

Use:
    from fastapi.testclient import TestClient
    from smos.api.main import app
    client = TestClient(app)

For each new persistent resource test:
   - successful creation
   - successful retrieval
   - list
   - update
   - delete (only where DELETE is part of the contract)
   - validation failure
   - not found
   - persistence
   - authorization for writes
   - response serialization

For DomainRelation additionally test:
   - Phenomenon -> Context
   - Constraint -> Phenomenon
   - backward compatibility with existing relation infrastructure
   - provenance survives serialization

For Prediction test:
   - create persists Prediction
   - get returns Prediction
   - update changes only permitted metadata
   - epistemic_status is NOT changed implicitly
   - Prediction is NOT automatically promoted to fact
   - DELETE is NOT exposed (expect 405 or 404)
   - source_hypothesis polymorphic fields are preserved
   - prediction timestamps are preserved
     (created_at, prediction_time, expected_at)

For analysis endpoints test:
   - resonance endpoint delegates to ConvergentResonanceService
   - blockage endpoint delegates to BlockageAnalysisService
   - emergence endpoint delegates to EmergenceAnalysisService
   - errors are mapped through existing API error handling

============================================================
STEP 10 — VERTICAL SLICE
============================================================

The minimum end-to-end API slice is:

    POST /api/phenomena
        |
        v
    POST /api/contexts
        |
        v
    POST /api/domain-relations
        |
        v
    POST /api/constraints
        |
        v
    POST /api/domain-relations
        |
        v
    POST /api/blockage/analyze/{phenomenon_id}
        |
        v
    POST /api/predictions
        |
        v
    PUT /api/predictions/{id}
        |
        v
    GET /api/predictions/{id}

This verifies:
    Phenomenon
    -> Context
    -> DomainRelation
    -> Constraint
    -> Analysis
    -> Prediction
    -> persistence

Do NOT pretend this is the complete Prediction lifecycle.
Outcome attachment and evaluation belong to a dedicated lifecycle
implementation task when those canonical services exist.
This TASK does NOT implement them.

============================================================
STEP 11 — FILES
============================================================

Modify ONLY:
    smos/api/main.py

Create:
    tests/test_api_domain.py

Do NOT create:
    - second FastAPI app
    - APIRouter
    - schemas.py
    - new service layer
    - new domain models
    - new ontology layer

If STEP 0 discovers an explicitly required existing abstraction
is missing AND the change is unavoidable:
    STOP and report before expanding scope.

============================================================
STEP 12 — HARD CONSTRAINTS
============================================================

Max: 1000 changed CODE lines.
Preferred: 300-700 changed CODE lines.

Documentation does not justify exceeding the code limit.

If implementation exceeds 1000 changed code lines:
    STOP
    report the reason
    propose an exact split
    do NOT continue automatically

Do NOT:
    - weaken tests
    - modify TASK 01-14 services
    - refactor unrelated API code
    - create parallel architecture
    - create duplicate relation systems
    - create a second forecast system
    - create a second event system
    - expose hidden context without authorization
    - automatically change epistemic status
    - implement future tasks
    - create /api/relations (only /api/domain-relations)

============================================================
STEP 13 — REQUIRED VERIFICATION
============================================================

Before commit:
    pytest tests/
    git diff --check

Also verify by structural inspection:
    - only one FastAPI() instance in smos/api/
    - existing API tests still pass
    - existing legacy /resonance remains unchanged
    - existing v0.9 endpoints remain functional
    - no duplicate resource endpoints were introduced
    - new endpoints use /api/... prefix
    - no new domain business logic inside route handlers

============================================================
FINAL REPORT
============================================================

Jules must report:

1. Existing API architecture reused.
2. Existing services/models reused.
3. New endpoints added.
4. New request schemas added.
5. Tests added.
6. Tests passed.
7. Vertical slice result.
8. Existing endpoints deliberately unchanged.
9. Future endpoints deliberately excluded.
10. Files changed.
11. Lines changed.
12. Any unresolved architectural gap.

Do NOT automatically execute the next TASK.

============================================================
COMMIT
============================================================

feat: expose cosmos v0.9 domain through canonical API

============================================================
FINAL EXECUTION RULE
============================================================

Jules executes ONLY the explicitly requested TASK.

Example: "Execute TASK 15"

Jules must:
   1. inspect the current repository;
   2. read previous task results;
   3. execute only TASK 15;
   4. run tests;
   5. run git diff --check;
   6. leave the repository working;
   7. report the result;
   8. stop.

Jules MUST NOT execute TASK 16 or any future task automatically.

If scope exceeds 1000 changed code lines:
    STOP.
Do not sacrifice architectural quality to meet the line limit.
Propose a concrete split and wait.

============================================================
AGENTS.md §14 REPORT FORMAT
============================================================

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 3768617962636155431

Completed at: 2026-09-25T15:03:07Z
