# Jules Task

## Task ID

task-20260912-202630

## Created

2026-09-12T20:26:30+00:00

## Repository

https://github.com/Urania-order/jules

## Branch

main

## Request

Додай API endpoint для ObservatoryService в api/main.py.

Контекст:
- smos/services/observatory_service.py має get_health_report(), generate_quarterly_report(), get_impact_ranking(), generate_recommendations()
- api/main.py має FastAPI endpoints
- ObservatoryService потребує список Observable підсистем

Завдання:
1. Імпортувати ObservatoryService і Observable підсистеми в api/main.py
2. Створити helper _get_observatory(db: Session) -> ObservatoryService:
   - Ініціалізувати EcologyEngine, ValueEcologyService, CommonsService, DiscoverySystem
   - Передати їх у ObservatoryService
3. Додати endpoint GET /observatory/health → get_health_report()
4. Додати endpoint GET /observatory/report → generate_quarterly_report()
5. Додати endpoint GET /observatory/ranking?limit=10 → get_impact_ranking(limit)
6. Додати endpoint GET /observatory/recommendations → generate_recommendations()
7. Написати tests/test_observatory_api.py з тестами для всіх 4 endpoints
8. Запустити ./scripts/validate.sh і uv run pytest tests/ -q --ignore=tests/test_ecology_service.py --ignore=tests/test_value_service.py --ignore=tests/test_mcp.py

НЕ чіпати .co-smos/ і .jules/
Звітувати за форматом AGENTS.md §14

## Status

started

