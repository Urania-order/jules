# Jules Task

## Task ID

task-20260912-181506

## Created

2026-09-12T18:15:06+00:00

## Repository

https://github.com/Urania-order/jules

## Branch

main

## Request

Онови api/main.py::search_memory, щоб використовувати реальні embeddings замість текстового фільтру.

Контекст:
- smos/services/embedding_service.py реалізовано (get_embedding, get_embeddings_batch)
- smos/services/memory_service.py тепер генерує embeddings для MemoryNode
- api/main.py::search_memory зараз має fallback на текстовий пошук для SQLite

Завдання:
1. У api/main.py::search_memory — використовувати embedding_service.get_embedding(q) для запиту
2. На PostgreSQL (pgvector) — використовувати MemoryNode.embeddings.l2_distance(embedding) (як зараз)
3. На SQLite — реалізувати cosine similarity через numpy:
   - Завантажити всі MemoryNode з embeddings
   - Обчислити cosine similarity між запитом і кожним node
   - Відсортувати за спаданням і взяти limit
4. Додати helper _cosine_similarity(a: List[float], b: List[float]) -> float
5. Оновити tests/test_search.py — перевірити, що пошук повертає релевантні результати на SQLite
6. Запустити ./scripts/validate.sh і uv run pytest tests/ -v

НЕ чіпати .co-smos/ і .jules/
Звітувати за форматом AGENTS.md §14 (STATUS, CHANGES, TESTS, RISKS, NEXT STEP)

## Status

started

