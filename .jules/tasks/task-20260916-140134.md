# Jules Task

## Task ID

task-20260916-140134

## Created

2026-09-16T11:01:34Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v0.9 — Control Panel + Task Card UX + Parallel Batch + Consult Access

CONTEXT:
- Repo: Urania-order/jules
- Frontend: frontend/index.html (vanilla HTML/CSS/JS)
- Backend: smos/api/main.py (FastAPI)
- Scripts: scripts/jules-task.sh, jules-complete.sh, jules-status.sh, jules-review.sh
- Existing UI (Patches A+B): Control Room, Events, Task Detail, Queue, Proposals,
  HISTORY view, AUTONOMY select, WHY section, REPLAY modal
- Spec: Co-SMOS v0.9 §14, §16, §17, §25

IMPORTANT:
This is an incremental frontend + backend patch.
Do NOT rewrite frontend/index.html wholesale.
Do NOT remove existing views.
Preserve all existing contract elements from Patch A and Patch B
(see tests/test_frontend_contract.py).

GOAL:
Add:
1. Control Panel (intake, stages, sequence, cycles, LED)
2. Task Card UX (left-drag reorder, right-click edit, Ctrl+hover WHY preview)
3. Parallel Batch button (run multiple tasks at once, per schedule)
4. Read-only Consult Access (for external AI consultants like DeepSeek)

FIRST:
1. Inspect frontend/index.html — find existing views, navigation, modal infra.
2. Inspect tests/test_frontend_contract.py — extend, do not weaken.
3. Inspect smos/api/main.py — find existing endpoints for tasks, events, queue,
   priority, auth.
4. Identify reusable pieces: modal, error banner, fetchJSON, refreshData,
   openModifyModal, showOffline.
5. Implement ONLY missing pieces.

REQUIREMENTS:

1. COMMAND INTAKE PANEL
Section: id="view-command-intake"
Navigation: <button data-view="command-intake">Command Intake</button>
Form:
- id="command-input" (textarea, required)
- id="command-priority" (number, min=1, max=5, value=3)
- id="command-submit" (button) "▶ SUBMIT COMMAND"
On submit: POST to existing task creation endpoint. Clear, refresh, append.

2. STAGE TRACKING PANEL
Section: id="view-stages"
  <div class="stage-card" data-task-id="...">
    <ol class="stage-list">
      <li class="stage" data-stage="intake">INTAKE</li>
      <li class="stage" data-stage="analyze">ANALYZE</li>
      <li class="stage" data-stage="plan">PLAN</li>
      <li class="stage" data-stage="implement">IMPLEMENT</li>
      <li class="stage" data-stage="test">TEST</li>
      <li class="stage" data-stage="review">REVIEW</li>
      <li class="stage" data-stage="report">REPORT</li>
    </ol>
  </div>
Classes: stage-active, stage-done, stage-failed, stage-unknown.

3. COMMAND SEQUENCE VIEW
Section: id="view-sequence"
  <ol id="sequence-list">
    <li class="sequence-item" data-task-id="...">
      <span class="seq-time">[HH:MM:SS]</span>
      <span class="seq-id">task-...</span>
      <span class="seq-state">submitted|running|done|failed</span>
      <span class="seq-priority">P1..P5</span>
    </li>
  </ol>

4. CYCLIC LEVERS OF INFLUENCE
Section: id="view-cycles"
  <div class="lever" data-lever="intake|priority|queue|review|acceptance">
    <div class="lever-name">...</div>
    <div class="lever-state">active|idle|blocked</div>
    <div class="lever-cycle">cycle N</div>
  </div>
UI-only. MUST NOT trigger execution.

5. LED STATUS INDICATORS
  <div class="led" data-state="green|yellow|red|off"></div>
Place: next to each stage, each lever, ONLINE badge.
CSS: .led-green, .led-yellow, .led-red, .led-off. No emoji.

6. TASK CARD UX
6a. Left-drag to reorder
- draggable="true" on .task-card in READY column.
- On drop, POST /api/queue/reorder with {task_ids:[...]}.
- MUST NOT execute task. MUST NOT change priority.

6b. Right-click to edit
- contextmenu on .task-card opens modal:
  - id="task-edit-input" (textarea)
  - id="task-edit-priority" (number, min=1, max=5)
  - id="task-edit-save", id="task-edit-cancel"
- On save, PATCH existing route (or POST if backend uses POST).

6c. Hover + Ctrl to preview WHY
- Task card default: title + priority badge + stage LED + hover zone.
- Hover + Ctrl key held → floating .why-preview near cursor.
- Content: source_task, proposal origin, proposed_by, history transitions.
- Hide on mouseleave or Ctrl release.
- Do NOT fabricate WHY data.

6d. Default collapsed view
- Only: short title, priority badge, LED, hover zone.

7. PRIORITY END-TO-END
- id="task-priority" (existing, 1..5)
- id="command-priority" (1..5)
- id="task-edit-priority" (1..5)
- task-priority-badge with text P1..P5
- Priority independent from order (add code comment).

8. PARALLEL BATCH BUTTON (NEW)
Section: id="view-batch"
Navigation: <button data-view="batch">Batch</button>

UI:
- Checkbox list of READY tasks:
  <input type="checkbox" class="batch-select" data-task-id="...">
- Schedule selector:
  <select id="batch-schedule">
    <option value="now">Now (parallel)</option>
    <option value="now-sequential">Now (sequential)</option>
    <option value="night">Night window</option>
    <option value="window">Custom window</option>
  </select>
- Concurrency limit:
  <input type="number" id="batch-concurrency" min="1" max="10" value="3">
- Button: <button id="batch-run">▶ RUN BATCH</button>
- Status area: <div id="batch-status"></div>

Semantics:
- "Now (parallel)": start up to N tasks simultaneously.
- "Now (sequential)": start one at a time.
- "Night window": queue for off-hours (UI-only; no scheduler backend).
- "Custom window": uses existing schedule if present; else report missing.

Backend:
- Use existing endpoints for starting tasks.
- If a batch endpoint is missing, report explicitly; do NOT invent.
- Concurrency limit MUST be respected.
- Batch MUST NOT bypass autonomy setting.
- If autonomy = MANUAL, batch requires explicit confirm dialog.
- If autonomy = ASSISTED, batch shows a proposal; user confirms.
- If autonomy = AUTO, batch runs immediately (within limit).

9. CONSULT ACCESS (NEW) — READ-ONLY
Add a read-only consult surface for external AI consultants
(e.g., DeepSeek) that MUST NOT modify anything.

Backend:
- Add if missing:
  GET /api/consult/state         → read-only snapshot of state.json
  GET /api/consult/tasks         → read-only list of tasks
  GET /api/consult/task/{id}     → read-only task record
  GET /api/consult/events        → read-only events
  GET /api/consult/errata        → read-only errata INDEX + files
  GET /api/consult/health        → service health, no side effects
- All consult endpoints MUST be:
  - read-only (no POST/PATCH/PUT/DELETE)
  - rate-limited (simple counter is enough; report if missing)
  - safe (no shell, no subprocess, no fs writes)
- If an auth mechanism exists, reuse it; otherwise:
  - mark consult endpoints as "no auth" in the final report
  - do NOT invent a new auth system

Frontend:
- Section: id="view-consult"
- Navigation: <button data-view="consult">Consult</button>
- Display:
  - consult endpoint list
  - last response time
  - health LED
  - short usage note: "Read-only. External consultants (e.g., DeepSeek)
    may read but MUST NOT write."
- No write actions in this view.

10. NAVIGATION
Add: command-intake, stages, sequence, cycles, batch, consult.

11. STYLES
Add: .stage-card, .stage-list, .stage, .stage-active, .stage-done,
.stage-failed, .stage-unknown, .sequence-item, .seq-time, .seq-id,
.seq-state, .seq-priority, .lever, .lever-name, .lever-state, .lever-cycle,
.led, .led-green, .led-yellow, .led-red, .led-off,
.task-card, .task-card-hover, .task-priority-badge,
.why-preview, .why-preview-visible,
.task-edit-modal, .task-edit-input,
.batch-select, .batch-status, .consult-endpoint, .consult-led.
Vanilla HTML/CSS/JS only.

12. TESTS
Extend tests/test_frontend_contract.py:
- view-command-intake, data-view="command-intake"
- id="command-input", id="command-priority", id="command-submit"
- view-stages, stage-card, stage-list, stage-active, stage-done, stage-failed
- view-sequence, sequence-list, sequence-item
- view-cycles, lever
- led, led-green, led-yellow, led-red, led-off
- task-card, task-priority-badge
- why-preview, why-preview-visible
- task-edit-modal, id="task-edit-input", id="task-edit-priority",
  id="task-edit-save", id="task-edit-cancel"
- draggable="true"
- contextmenu handler presence
- Ctrl+hover handler presence
- view-batch, id="batch-schedule", id="batch-concurrency",
  id="batch-run", id="batch-status", .batch-select
- view-consult, consult-endpoint, consult-led
- Backend: consult endpoints exist and are read-only
  (add a separate test that asserts no POST/PATCH/DELETE on /api/consult/*)

13. API COMPATIBILITY
Inspect smos/api/main.py. Use existing routes.
Do NOT invent. If PATCH /api/tasks/{id}, /api/queue/reorder,
or batch endpoints are missing — report explicitly.

14. OFFLINE / ERROR
Reuse Patch A mechanism. Do not duplicate.

15. AUTONOMY INTEGRATION
- Batch button behavior depends on existing autonomy setting
  (MANUAL/ASSISTED/AUTO) from Patch B.
- Do NOT auto-execute under MANUAL.
- Do NOT bypass ASSISTED confirmation.
- Respect concurrency limit in all modes.

CONSTRAINTS:
- No wholesale rewrite of frontend/index.html.
- Minimal coherent changes.
- Do not remove working functionality.
- Do not touch .co-smos/, .jules/tasks/, .jules/results/, .jules/queue/.
- Do not change API_BASE.
- Do not weaken tests.
- Do not execute tasks from UI (drag/drop only reorders).
- Do not change priority silently on drag/drop.
- Do not fabricate WHY data.
- Consult endpoints MUST be read-only.
- Consult endpoints MUST NOT write files or spawn processes.

ACCEPTANCE:
- Command Intake panel
- Stage Tracking panel
- Sequence view
- Cycles view
- LED indicators
- Task card UX: left-drag reorder, right-click edit, Ctrl+hover WHY
- Priority 1..5 end-to-end
- Parallel Batch button with schedule + concurrency limit
- Consult view (read-only) with backend endpoints
- All existing views preserved

TESTS: frontend contract, Control Room API, regression, git diff --check,
       consult read-only assertions.

REPORT FORMAT: AGENTS.md §14.

FINAL REPORT:
1. what already existed and was reused
2. what was added (sections, classes, endpoints, tests)
3. what was deliberately not changed
4. tests/results
5. remaining v0.9 gaps:
   - missing endpoints (PATCH /api/tasks/{id}, /api/queue/reorder,
     batch endpoints, consult endpoints)
   - missing priority field in task schema
   - missing scheduler backend for batch
   - missing auth for consult
   - any UX that could not be implemented

AFTER COMPLETION: Suggest 3-5 next steps.

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 5390457372810171581

Completed at: 2026-09-16T12:12:56Z
