# Jules Task

## Task ID

task-20260920-055407

## Created

2026-09-20T02:54:08Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS v1.2.6 — Remove auto-refresh + Add Refresh button + Navbar LED

CONTEXT:
- Repo: Urania-order/jules
- Frontend: frontend/index.html
- Backend: smos/api/main.py
- v1.2.5 added setInterval(refreshData, 10000) — caused UI flicker
- This was NOT requested by user (mistake in ТЗ)
- Fix: remove auto-refresh, add manual Refresh button, add navbar LED

GOAL:

PART 1 — Remove auto-refresh

In frontend/index.html (~line 3956):
  BEFORE:
    setInterval(refreshData, 10000);
  AFTER:
    // (removed — not requested, caused UI flicker)
    // Manual refresh via SYNC / Refresh button only

PART 2 — Add Refresh button near ONLINE led

1. In navbar, next to ONLINE led (and SYNC button):
   - Add button id="btn-nav-refresh"
   - Label: "⟳ Refresh" (or icon)
   - Position: near ONLINE / SYNC
   - onclick: refreshData()
   - Visual: same style as SYNC button

2. Optional: show last refresh time:
   - Small text next to button: "12:34:56"
   - Updated after refreshData()

PART 3 — Navbar queue status LED

1. Backend: add GET /api/queue/status
   Returns: {pending: N, running: N, blocked: N, deferred: N}
   Reads .jules/queue/{pending,running,blocked,deferred}/*.txt counts
   Auth: operator/admin

2. Frontend navbar:
   - Add LED indicator id="navbar-queue-led"
   - Position: next to ONLINE led
   - CSS classes: queue-led queue-led-{green|blue|yellow|red}
   - Colors:
     * green:  pending=0, running=0, deferred=0
     * blue:   pending>0, running=0
     * yellow: running>0 (pulsing)
     * red:    deferred>0
   - Tooltip: "Pending: N | Running: N | Blocked: N | Deferred: N"
   - Click: navigate to Queue Management

3. LED polling (LED ONLY — no full page refresh):
   - setInterval(async () => {
       try {
         const s = await fetchJSON(API_BASE + '/queue/status');
         updateQueueLed(s);
       } catch (e) {}
     }, 10000);
   - updateQueueLed(s): update LED class + title ONLY
   - Do NOT call refreshData() from this interval

4. CSS:
   .queue-led { width: 10px; height: 10px; border-radius: 50%; display: inline-block; margin-right: 6px; vertical-align: middle; }
   .queue-led-green { background-color: #22c55e; }
   .queue-led-blue { background-color: #3b82f6; }
   .queue-led-yellow { background-color: #eab308; animation: pulse 1.5s infinite; }
   .queue-led-red { background-color: #ef4444; }
   @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.5; } }

TESTS:
- test_queue_status_endpoint (backend)
- frontend contract: btn-nav-refresh, navbar-queue-led, queue-led classes
- frontend contract: NO setInterval(refreshData, ...) in code

REQUIREMENTS:
- Files: smos/api/main.py, frontend/index.html,
  tests/test_admin_api.py, tests/test_frontend_contract.py
- No shell=True
- Preserve existing navbar (ONLINE led, SYNC)

TESTS:
- pytest tests/ + git diff --check

CONSTRAINTS:
- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests

REPORT FORMAT: AGENTS.md §14

FINAL REPORT:
1. what already existed and was reused
2. what was added
3. what was deliberately not changed
4. tests/results
5. remaining gaps

## Status

started


## Final Status

completed

## Result

pull result: no-op

Session: 9411005948909953281

Completed at: 2026-09-20T02:57:19Z

## Final Status

completed

## Result

pull result: applied

Session: 9411005948909953281

Completed at: 2026-09-20T03:14:50Z
