# Jules Task

## Task ID

task-20260924-164611

## Created

2026-09-24T16:46:11Z

## Repository

https://github.com/Urania-order/jules.git

## Branch

main

## Request

Co-SMOS — TASK 09 — PROVENANCE / CONTEXT EXPOSURE

CONTEXT:
- Repo: Urania-order/jules
- Existing ontology TASK 01-08 applied:
    * EpistemicStatus, RelationType (33)
    * Phenomenon, Context, Constraint, PotentialPhenomenon
    * DomainRelation (TASK 07) — polymorphic cross-domain edges
    * RoleProjectionService (TASK 08) — 3 perspectives
- Existing actor/agent models (entities.py:5-50):
    * DigitalTwin — id, owner_id, goals, preferences, knowledge_base
    * Cosmonaut (line 15) — id, type (HUMAN/LLM/AGENT/...), name, reputation_score,
      expertise, behavior_profile_id, llm_profile_id
    * BehaviorProfile — id, name, cognitive_speed, etc.
    * LLMProfile (line 37) — id, role (Analytical/Empathic/Creative), reasoning_style,
      emotional_bias, creativity_level
- Existing User (models.py:185) — id, role (UserRole), ...
- Existing Event (models.py:259) — user_id, workspace_id, type, content, timestamp
- Existing ProvenanceRecord (ecology.py:53) — node_id FK memory_nodes.id (memory-specific)
- NO existing Conclusion / Verdict / Output model (confirmed by STEP 0 audit)
- NO existing context exposure / visibility record (confirmed by STEP 0 audit)
- NO conclusion service (confirmed by STEP 0 audit)
- Ontology series TASK 01-08 applied
- TASK 09: Context Exposure Provenance ← THIS

============================================================
STEP 0 — ARCHITECTURE AUDIT RESULTS (DOCUMENTED)
============================================================

1. Actor/agent models:
   - Cosmonaut (entities.py:15) — type: HUMAN, LLM, AGENT, DIGITAL_TWIN,
     SIMULATION, HISTORIAN, RECONSTRUCTOR
   - LLMProfile (entities.py:37) — role: Analytical, Empathic, Creative
   - User (models.py:185) — role: UserRole enum
   - DigitalTwin (entities.py:5) — owner_id, goals, preferences
   - Decision: agent_id is polymorphic (agent_type + agent_id).
     Do NOT add FK to specific model — agent_id points to whichever
     model the agent_type indicates (cosmonauts.id, users.id, etc.).

2. Conclusion model:
   - NO existing Conclusion / Verdict / Output model.
   - Decision: conclusion is JSON inside ContextExposure.
     Do NOT create separate Conclusion model in TASK 09.

3. Context / Knowledge:
   - context_ids → JSON list of contexts.id (Context model exists, TASK 03)
   - knowledge_ids → JSON list of memory_nodes.id.
     NOTE: semantic of memory_nodes vs knowledge is NOT assumed.
     Field name preserves original intent; no new Knowledge model.

4. hidden_context_ids:
   - Represents context that existed in the system but was NOT visible
     to a specific agent during a specific analysis/projection.
   - Visibility belongs to the projection/run, NOT to Context itself.
   - Same Context may be visible to Agent A, hidden from Agent B.
   - No existing exposure/visibility record → create new ContextExposure.

5. Decision (recorded):
   - Create ContextExposure (NEW model).
   - Reuse EpistemicStatus from TASK 01.
   - Reuse Phenomenon pattern (to_dict, JSON, server_default).
   - Reuse Constraint/Potential enum pattern (_missing_, from_str) for AgentType.
   - Do NOT modify Cosmonaut, LLMProfile, User, Context, Event, ProvenanceRecord.
   - ProvenanceRecord remains canonical for memory_nodes/ecology.
   - ContextExposure is orthogonal to ProvenanceRecord.

============================================================
OBJECTIVE
============================================================

Record which context was visible to an agent when it generated a conclusion.

MINIMUM INFORMATION:
- agent_id          (polymorphic — see AgentType)
- role              (String — analytical role, e.g. skeptic, expert)
- context_ids       (JSON list — visible context ids)
- knowledge_ids     (JSON list — knowledge/memory node ids visible)
- timestamp         (created_at DateTime)
- conclusion        (JSON — the conclusion text/structure)

ADDITIONAL:
- hidden_context_ids (JSON list — context that existed but was hidden)

PURPOSE:
Detect possible context-induced convergence.
Example: 73 agents agree; 61 received the same context fragment.
This is a SIGNAL, NOT evidence of truth.
Document explicitly.

============================================================
FILES TO CREATE
============================================================

1. smos/models/context_exposure.py

    import enum
    from sqlalchemy import Column, Integer, String, Enum, JSON, DateTime, func
    from smos.core.database import Base
    from smos.models.models import EpistemicStatus

    class AgentType(str, enum.Enum):
        HUMAN = "HUMAN"           # User / human operator
        COSMONAUT = "COSMONAUT"   # Cosmonaut actor
        LLM = "LLM"               # LLMProfile agent
        DIGITAL_TWIN = "DIGITAL_TWIN"
        SYSTEM = "SYSTEM"         # automated/system
        UNKNOWN = "UNKNOWN"

        @classmethod
        def _missing_(cls, value):
            if isinstance(value, str):
                upper = value.upper()
                for member in cls:
                    if member.value == upper:
                        return member
            return cls.UNKNOWN

        @classmethod
        def from_str(cls, value):
            if value is None:
                return cls.UNKNOWN
            if isinstance(value, cls):
                return value
            try:
                return cls(str(value).upper())
            except (ValueError, KeyError):
                return cls.UNKNOWN

    class ContextExposure(Base):
        """Context exposure record.

        Records which context was visible to an agent when it generated
        a conclusion.

        IMPORTANT: this is a SIGNAL for convergence analysis,
        NOT evidence of truth.

        Visibility belongs to the exposure/run, not to Context itself.
        The same Context may be:
        - visible to Agent A (context_ids)
        - hidden from Agent B (hidden_context_ids)
        """
        __tablename__ = "context_exposures"

        id = Column(Integer, primary_key=True, index=True)
        agent_type = Column(
            Enum(AgentType),
            default=AgentType.UNKNOWN,
            nullable=False,
        )
        agent_id = Column(Integer, nullable=True)   # polymorphic (see AgentType)
        role = Column(String, nullable=True)         # analytical role in this run
        context_ids = Column(JSON, default=list)     # visible contexts.id
        knowledge_ids = Column(JSON, default=list)   # visible memory_nodes.id
        hidden_context_ids = Column(JSON, default=list)  # hidden contexts.id
        conclusion = Column(JSON, default=dict)      # {text, claim, confidence, ...}
        epistemic_status = Column(
            Enum(EpistemicStatus),
            default=EpistemicStatus.OBSERVED,
            nullable=True,
        )
        provenance = Column(JSON, default=dict)      # origin of THIS exposure record
        created_at = Column(
            DateTime(timezone=True),
            server_default=func.now(),
        )
        updated_at = Column(
            DateTime(timezone=True),
            server_default=func.now(),
            onupdate=func.now(),
        )

        def to_dict(self) -> dict:
            """Serialize ContextExposure to a dictionary."""
            return {
                "id": self.id,
                "agent_type": self.agent_type.value if isinstance(self.agent_type, AgentType) else self.agent_type,
                "agent_id": self.agent_id,
                "role": self.role,
                "context_ids": self.context_ids if self.context_ids is not None else [],
                "knowledge_ids": self.knowledge_ids if self.knowledge_ids is not None else [],
                "hidden_context_ids": self.hidden_context_ids if self.hidden_context_ids is not None else [],
                "conclusion": self.conclusion if self.conclusion is not None else {},
                "epistemic_status": self.epistemic_status.value if isinstance(self.epistemic_status, EpistemicStatus) else self.epistemic_status,
                "provenance": self.provenance if self.provenance is not None else {},
                "created_at": self.created_at.isoformat() if self.created_at else None,
                "updated_at": self.updated_at.isoformat() if self.updated_at else None,
            }

2. smos/services/context_exposure_service.py

    from typing import Any, Dict, List, Optional
    from sqlalchemy.orm import Session
    from smos.models.context_exposure import ContextExposure, AgentType
    from smos.models.models import EpistemicStatus

    class ContextExposureService:
        def __init__(self, db: Session):
            self.db = db

        def record(
            self,
            agent_type,
            agent_id: Optional[int] = None,
            role: Optional[str] = None,
            context_ids: Optional[List[int]] = None,
            knowledge_ids: Optional[List[int]] = None,
            conclusion: Optional[Dict[str, Any]] = None,
            hidden_context_ids: Optional[List[int]] = None,
            epistemic_status=None,
            provenance: Optional[Dict[str, Any]] = None,
        ) -> ContextExposure:
            exposure = ContextExposure(
                agent_type=AgentType.from_str(agent_type),
                agent_id=agent_id,
                role=role,
                context_ids=list(context_ids or []),
                knowledge_ids=list(knowledge_ids or []),
                hidden_context_ids=list(hidden_context_ids or []),
                conclusion=dict(conclusion or {}),
                epistemic_status=epistemic_status or EpistemicStatus.OBSERVED,
                provenance=dict(provenance or {}),
            )
            self.db.add(exposure)
            self.db.commit()
            self.db.refresh(exposure)
            return exposure

        def get(self, exposure_id: int) -> Optional[ContextExposure]:
            return self.db.query(ContextExposure).filter(ContextExposure.id == exposure_id).first()

        def list(
            self,
            agent_type=None,
            agent_id: Optional[int] = None,
            limit: int = 50,
            offset: int = 0,
        ) -> List[ContextExposure]:
            q = self.db.query(ContextExposure)
            if agent_type is not None:
                q = q.filter(ContextExposure.agent_type == AgentType.from_str(agent_type))
            if agent_id is not None:
                q = q.filter(ContextExposure.agent_id == agent_id)
            return q.order_by(ContextExposure.created_at.desc()).offset(offset).limit(limit).all()

        def delete(self, exposure_id: int) -> bool:
            e = self.get(exposure_id)
            if not e:
                return False
            self.db.delete(e)
            self.db.commit()
            return True

        def convergence_signals(
            self,
            conclusion_filter: Optional[Dict[str, Any]] = None,
            limit: int = 100,
        ) -> Dict[str, Any]:
            """Aggregate exposures by shared context fragments.

            Returns a SIGNAL, not evidence of truth.
            """
            exposures = self.list(limit=limit)
            # Group by frozenset(context_ids) for shared fragment detection
            groups: Dict[frozenset, List[ContextExposure]] = {}
            for e in exposures:
                key = frozenset(e.context_ids or [])
                groups.setdefault(key, []).append(e)

            fragments = []
            for key, items in groups.items():
                if len(items) < 2:
                    continue
                fragments.append({
                    "context_ids": sorted(list(key)),
                    "agent_count": len(items),
                    "agents": [
                        {
                            "agent_type": (i.agent_type.value if isinstance(i.agent_type, AgentType) else i.agent_type),
                            "agent_id": i.agent_id,
                            "role": i.role,
                            "conclusion": i.conclusion,
                        }
                        for i in items
                    ],
                })
            fragments.sort(key=lambda f: f["agent_count"], reverse=True)
            return {
                "total_exposures": len(exposures),
                "context_fragments": fragments,
                "note": "This is a SIGNAL, not evidence of truth.",
            }

3. smos/models/__init__.py — export ContextExposure, AgentType
4. smos/services/__init__.py — export ContextExposureService
5. smos/core/init_db.py — import context_exposure  ← CRITICAL (ERRATA-0042)
6. tests/test_context_exposure.py (new)

============================================================
TESTS (tests/test_context_exposure.py)
============================================================

- test_record_context_exposure
- test_serialize_context_exposure (to_dict)
- test_persist_context_exposure
- test_retrieve_context_exposure
- test_list_context_exposures
- test_list_filter_by_agent_type
- test_list_filter_by_agent_id
- test_delete_context_exposure
- test_provenance_survives_persistence (KEY requirement)
- test_provenance_survives_retrieval (KEY requirement)
- test_hidden_context_ids_persist
- test_knowledge_ids_persist
- test_conclusion_json_persists
- test_convergence_signals_aggregates_by_context
- test_convergence_signals_returns_signal_note (documentation assertion)
- test_context_exposure_not_truth (documentation assertion)
- test_context_exposure_does_not_modify_agent_models
- test_context_exposure_does_not_modify_context_model
- test_agent_type_enum
- test_agent_type_from_str
- test_agent_type_unknown_default
- test_epistemic_status_default_observed

FIXTURES: create a few Cosmonaut/LLMProfile records, Context records,
then record exposures and verify aggregation.

LIMIT: ≤1000 changed code lines.

TESTS: pytest tests/ + git diff --check

============================================================
TECHNICAL
============================================================

- Files:
    * smos/models/context_exposure.py (new)
    * smos/services/context_exposure_service.py (new)
    * smos/models/__init__.py (modify — export)
    * smos/services/__init__.py (modify — export)
    * smos/core/init_db.py (modify — import)
    * tests/test_context_exposure.py (new)
- No shell=True
- Reuse EpistemicStatus from TASK 01
- Reuse Phenomenon pattern (to_dict, JSON, server_default)
- Reuse Constraint/Potential enum pattern (_missing_, from_str) for AgentType
- Do NOT modify Cosmonaut, LLMProfile, User, Context, Event, ProvenanceRecord
- Do NOT create separate Conclusion model
- Do NOT create parallel provenance engine
  (ProvenanceRecord remains for memory_nodes; ContextExposure is orthogonal)
- ALL tests must pass locally BEFORE commit (ERRATA-0043)
- init_db.py MUST import context_exposure (ERRATA-0042)

============================================================
CONSTRAINTS
============================================================

- Do not touch .co-smos/, .jules/tasks/, .jules/results/
- Do not weaken existing tests
- STEP 0 audit recorded above
- ContextExposure is NOT truth — convergence is a SIGNAL
- hidden_context_ids is a property of the exposure, not of Context
- Preserve backward compatibility with TASK 01-08
- ALL tests must pass locally BEFORE commit (ERRATA-0043)

============================================================
REPORT FORMAT: AGENTS.md §14
============================================================

FINAL REPORT (expanded):
1. what already existed and was reused
   - STEP 0 audit:
     * Agent models: Cosmonaut, LLMProfile, User, DigitalTwin
     * NO existing Conclusion model → conclusion = JSON
     * NO existing exposure record → create ContextExposure
     * ProvenanceRecord is memory_nodes-specific, orthogonal
   - Decision: create ContextExposure (new model)
2. what was added (ContextExposure, AgentType, ContextExposureService,
   convergence_signals, tests)
3. what was deliberately not changed (Cosmonaut, LLMProfile, User, Context,
   Event, ProvenanceRecord)
4. tests/results
5. remaining gaps

Commit message:
feat: track context exposure provenance

## Status

started


## Final Status

completed

## Result

pull result: applied

Session: 1662265889493228242

Completed at: 2026-09-24T16:55:39Z
